SolUno - Repositorio completo exportado en formato texto (.txt)
BancoSol - InnovaHack 2026
Repositorio online: https://github.com/bookcubers2-ux/soluno-bancosol
App en vivo: https://security-affair-untrimmed.ngrok-free.dev
Exportado: 2026-05-31

INDICE DE ARCHIVOS INCLUIDOS:
  1. README.md
  2. app/server.js
  3. app/public/index.html
  4. app/playbooks_detallados.json
  5. SolUno_n8n_sistema.json
  6. SolUno_n8n_watest.json
  7. SolUno_n8n_workflow.json
  8. SolUno_n8n_flujo_completo.json
  9. SolUno_n8n_cierre_ciclo.json
  10. SolUno_Proyecto_Maestro.md
  11. SolUno_Banco_de_Datos.md
  12. SolUno_n8n_guia.md
  13. generar_informe.py
  14. generar_docx.py
  15. transcribir.py
  16. transcribir2.py
  17. transcribir3.py
  18. .env.example
  19. .gitignore

NOTA: los archivos binarios (informe PDF/Word, captura del workflow de n8n) no se
incluyen en este .txt; estan en el repositorio online indicado arriba.

==========================================================================================
ARCHIVO: README.md
==========================================================================================
# SolUno · Centro de operaciones de atención con IA — BancoSol

> **InnovaHack 2026.** Gestión inteligente de **quejas e incidencias** de BancoSol por WhatsApp, con un agente de IA que **resuelve las quejas de forma autónoma** y **deriva las incidencias al equipo de TI correcto**, todo orquestado con **n8n** y visible en tiempo real en una consola para el equipo de soporte.

---

## 🔗 Accesos rápidos (para los jurados)

| | Link |
|---|---|
| 🖥️ **Probar la APP EN VIVO** (entrá como si fueras el técnico de soporte) | **https://security-affair-untrimmed.ngrok-free.dev** |
| 🔁 **Workflow de n8n** (flujo de automatización, con diagrama) | [SolUno_n8n_sistema.json](SolUno_n8n_sistema.json) · ver [diagrama](#-workflow-de-n8n-flujo-de-automatización) |
| 📄 **Informe del proyecto** (PDF / Word) | [SolUno_Informe.pdf](SolUno_Informe.pdf) · [SolUno_Informe.docx](SolUno_Informe.docx) |
| 📦 **Código y documentación** (este repositorio) | https://github.com/bookcubers2-ux/soluno-bancosol |

> ℹ️ Al abrir la app en vivo, el navegador muestra **una vez** un aviso de ngrok: hacé clic en el botón **"Visit Site"** y entrás a la consola. *(La demo en vivo está disponible durante el período de evaluación, mientras el equipo mantiene el entorno activo.)*

---

## 🎯 El problema

BancoSol recibe un alto volumen de mensajes de clientes por WhatsApp. Hoy se mezclan:
- **Quejas** (casos puntuales con solución conocida: cambio de celular, "pagué y aún figura la deuda", dudas de uso…), y
- **Incidencias** (fallas críticas y generalizadas: QR caído, login caído, app que no carga…).

Atenderlos manualmente es lento, no se prioriza por gravedad, no se sabe a qué equipo derivar cada incidencia, y no queda trazabilidad para reportes (ASFI).

## 💡 La solución — SolUno

Un agente conversacional + una consola de operaciones que, por cada mensaje de WhatsApp:

1. **Clasifica** el mensaje con IA → `queja` · `incidente` · `consulta`.
2. **Resuelve las quejas solo**, respondiendo al cliente con una **solución enumerada paso a paso** (cálida, no rígida).
3. **Deriva las incidencias** al **equipo de TI responsable** (Pagos/QR, Seguridad/Accesos, DevOps Aplicaciones, Infraestructura/TI, Mesa de Incidentes) y las **agrupa por causa raíz** (firma), contando los afectados.
4. **Califica la severidad**: Leve · Medio · Urgente · **Crítico**.
5. **Cataloga** cada incidencia contra un **catálogo de problemas (playbooks)** con código `PRB-###`; si el problema es nuevo, lo **añade automáticamente**.
6. Le muestra al técnico la **solución del playbook desplegada paso a paso** (runbooks profesionales), que puede usar tal cual o personalizar.
7. Al **resolver**: avisa al cliente por WhatsApp, **actualiza el playbook** (manual vivo) y genera un **informe ASFI** editable y aprobable.

---

## 🧩 Arquitectura

```
   Cliente (WhatsApp)
        │  mensaje
        ▼
   Meta WhatsApp Cloud API
        │  webhook (vía ngrok)
        ▼
   Backend Node (app/server.js)  ──►  Consola del operador (app/public/index.html)
        │  proxy                         (bandeja en vivo, incidentes, playbooks, informes ASFI)
        ▼
   n8n (SolUno_n8n_sistema.json)
        │  - Clasificador IA (Groq · llama-3.3-70b)
        │  - Interpretar / agrupar por firma / contar afectados
        │  - Enrutar a equipo · severidad
        │  - Generar respuesta al cliente (enumerada) + recomendación técnica
        ▼
   Respuesta al cliente (WhatsApp)
```

- **Privacidad:** los datos personales se **tokenizan** antes de enviarse a la IA.
- **Sin secretos en el código:** todas las claves se leen de variables de entorno (`$env` en n8n, `process.env` en el backend).

---

## 🔁 Workflow de n8n (flujo de automatización)

📄 **Archivo del workflow:** [`SolUno_n8n_sistema.json`](SolUno_n8n_sistema.json) — se puede **importar en cualquier n8n** (menú *Import from File*) para verlo y ejecutarlo.

### Vista real del workflow en n8n

![Workflow de n8n — SolUno](n8n_workflow.png)

> Arriba: el flujo principal (clasificación + IA + enrutado a equipos + respuesta). Abajo: el **cierre del ciclo** (resolver → playbook vivo → informe ASFI).

### Diagrama simplificado del flujo

```mermaid
flowchart TD
  A([Cliente por WhatsApp]) --> B{Triggers<br/>Meta · Demo}
  B --> C[Normalizar mensaje]
  C --> D[Tokenizar PII]
  D --> E[Clasificar IA · Groq<br/>llama-3.3-70b]
  E --> F[Interpretar clasificacion]
  F --> G[Agrupar por causa raiz<br/>y contar afectados]
  G --> H{Es incidente?}
  H -- No: queja / consulta --> I[Respuesta Queja<br/>Playbook vivo]
  H -- Si --> J[Paquete Incidente<br/>+ severidad]
  J --> K{Enrutar a equipo}
  K --> K1[Equipo Pagos/QR]
  K --> K2[Seguridad/Accesos]
  K --> K3[DevOps Aplicaciones]
  K --> K4[Infraestructura/TI]
  K --> K5[Mesa de Incidentes TI]
  K1 --> L[Registrar estado y metricas]
  K2 --> L
  K3 --> L
  K4 --> L
  K5 --> L
  I --> L
  L --> M[Generar respuesta IA<br/>cliente enumerado + recomendacion tecnica]
  M --> N[Aplicar respuesta IA]
  N --> O{Origen demo?}
  O -- Demo --> P([Responder a la consola])
  O -- WhatsApp --> Q([Enviar por WhatsApp · Meta])
```

> El workflow `SolUno_n8n_watest.json` se encarga del **envío de mensajes a la WhatsApp Cloud API** (texto, botones, plantillas).

---

## 🗂️ Estructura del repositorio

| Ruta | Qué es |
|---|---|
| `app/server.js` | Backend Node **sin dependencias** (WhatsApp ⇄ n8n ⇄ consola, memoria, playbooks, informes ASFI). |
| `app/public/index.html` | **Consola del operador** (HTML/CSS/JS vanilla, identidad BancoSol). |
| `app/playbooks_detallados.json` | **Runbooks completos** paso a paso de cada problema. |
| `SolUno_n8n_sistema.json` | **Workflow principal de n8n** (clasificación + IA + enrutado). |
| `SolUno_n8n_watest.json` | Workflow de envío a WhatsApp (Meta). |
| `SolUno_Proyecto_Maestro.md` · `SolUno_Banco_de_Datos.md` | Documentación del proyecto. |
| `SolUno_Procedimiento_Completo.docx` | Procedimiento detallado del flujo. |
| `.env.example` | Plantilla de variables de entorno. |

---

## ▶️ Cómo ejecutarlo

### Requisitos
- **Node.js 18+**
- **n8n** (se ejecuta con `npx`, no requiere instalación global)
- (Opcional, para WhatsApp real) **ngrok** + una app de **WhatsApp Cloud API** en Meta

### 1) Configurar variables de entorno
Copiá `.env.example` a `.env` y completá tus claves (Groq es gratis: https://console.groq.com).

### 2) Levantar n8n e importar el workflow
```bash
# Importar y activar el workflow principal
npx n8n import:workflow --input=SolUno_n8n_sistema.json
npx n8n import:workflow --input=SolUno_n8n_watest.json

# Iniciar n8n (con las variables de entorno cargadas)
#   GROQ_API_KEY, WHATSAPP_TOKEN, WA_PHONE_ID
npx n8n start
```
n8n queda en **http://localhost:5678**.

### 3) Levantar el backend + la consola
```bash
node app/server.js
```
Abrí la consola en **http://localhost:3000**.

### 4) (Opcional) Conectar WhatsApp real
```bash
ngrok http 3000 --domain=TU_DOMINIO.ngrok-free.dev
```
En Meta → WhatsApp → Configuración de la API, configurá el webhook:
`https://TU_DOMINIO.ngrok-free.dev/webhook/whatsapp` (token de verificación = `WA_VERIFY_TOKEN`).

> 📱 **Nota para los jurados:** con el **número de prueba** de Meta solo se puede responder a números agregados a la lista de destinatarios (máx. 5). Los números de prueba se agregan desde el panel de Meta. *(El equipo facilitará los teléfonos/números habilitados para la demo.)*

---

## ✨ Funcionalidades destacadas

- **Conversación = una queja**: la charla completa se cuenta como un caso; se cierra cuando el cliente agradece.
- **Respuestas enumeradas paso a paso** al cliente (no rígidas, generadas por IA).
- **Catálogo de problemas `PRB-###`** con auto-alta de problemas nuevos.
- **Runbooks completos** (12–18 pasos) basados en buenas prácticas de la industria (SRE / ITIL), adaptados a banca.
- **Centro de derivación de TI**: árbol de equipos con conteo de incidencias activas por equipo.
- **Severidad** Leve/Medio/Urgente/**Crítico** resaltada.
- **Playbook vivo**: la solución se actualiza al resolver.
- **Informe ASFI** generado, editable y aprobable.
- Diseño con identidad **BancoSol** (paleta morado/naranja, tipografía Google).

---

## 🛠️ Stack

`n8n` · `Node.js (http nativo)` · `Groq API (llama-3.3-70b)` · `WhatsApp Cloud API (Meta)` · `HTML/CSS/JS vanilla` · `ngrok`

---

*Proyecto desarrollado para InnovaHack 2026.*

==========================================================================================
ARCHIVO: app/server.js
==========================================================================================
/* ============================================================
   SolUno — Backend (Node, sin dependencias)
   WhatsApp -> IA (n8n) con MEMORIA -> operador (equipo tecnico).
   Incidentes: menu de estado + recomendacion tecnica de la IA +
   al resolver: avisa al cliente, actualiza el playbook y genera
   un informe ASFI editable/aprobable (se guarda en /informes).
   ============================================================ */
const http = require("http");
const fs = require("fs");
const path = require("path");

const PORT = process.env.PORT || 3000;
const N8N = process.env.N8N_URL || "http://localhost:5678";
const VERIFY_TOKEN = process.env.WA_VERIFY_TOKEN || "soluno_verify_123";
const PUBLIC_DIR = path.join(__dirname, "public");
const INFORMES_DIR = path.join(__dirname, "informes");
try { if (!fs.existsSync(INFORMES_DIR)) fs.mkdirSync(INFORMES_DIR); } catch (e) {}

const store = {
  casos: [], inbox: [],
  incidentes: {},        // caso_id -> { estado, producto, categoria, mensaje, ts, users, recomendacion_tecnica, nombre }
  conversaciones: {},    // from -> [{de:'cliente'|'agente', texto}] (memoria para la IA)
  sesiones: {},          // from -> sesion ABIERTA actual (una "queja"/charla)
  sesionesList: [],      // todas las conversaciones cerradas+abiertas (bandeja + metricas)
  informes: {},          // caso_id -> { caso_id, texto, aprobado, ts, users }
  metricas: { total: 0, quejas: 0, incidentes: 0, consultas: 0, afectados: 0 },
  // Playbooks RÍGIDOS por tipo de problema predefinido. El "tipo de problema" es fijo;
  // la SOLUCIÓN (pasos) es lo editable/actualizable. QUEJA -> la resuelve la IA sola.
  // INCIDENTE -> se deriva al equipo responsable.
  playbooks: {
    // ===== INCIDENTES (se derivan al equipo de TI) =====
    fallo_pagos_qr:       { clave: "fallo_pagos_qr",       titulo: "Fallo en pagos / QR",            tipo: "incidente", categoria: "transacciones", equipo: "Equipo Pagos/QR",        pasos: ["Verificar el estado del servicio de pagos/QR", "Revisar los logs del gateway de transacciones", "Validar cobros de prueba en varios dispositivos"], version: 1, origen: "base", actualizado: null },
    error_transferencia:  { clave: "error_transferencia",  titulo: "Error en transferencias",        tipo: "incidente", categoria: "transacciones", equipo: "Equipo Pagos/QR",        pasos: ["Confirmar disponibilidad del core transaccional", "Revisar la cola de transferencias pendientes", "Reprocesar y conciliar las operaciones afectadas"], version: 1, origen: "base", actualizado: null },
    fallo_ach:            { clave: "fallo_ach",            titulo: "Fallo ACH / interbancario",      tipo: "incidente", categoria: "transacciones", equipo: "Equipo Pagos/QR",        pasos: ["Verificar la conexión con la cámara de compensación", "Revisar los lotes ACH rechazados", "Reenviar el lote y notificar a operaciones"], version: 1, origen: "base", actualizado: null },
    caida_login:          { clave: "caida_login",          titulo: "Caída de login / acceso",        tipo: "incidente", categoria: "acceso",        equipo: "Equipo Seguridad/Accesos", pasos: ["Revisar el servicio de autenticación", "Validar la emisión de tokens y sesiones", "Restablecer el nodo de acceso y monitorear"], version: 1, origen: "base", actualizado: null },
    app_no_carga:         { clave: "app_no_carga",         titulo: "App no carga / se cae",          tipo: "incidente", categoria: "general",       equipo: "DevOps Aplicaciones",     pasos: ["Revisar el despliegue y los healthchecks de la app", "Verificar errores en el backend móvil", "Hacer rollback o redeploy si corresponde"], version: 1, origen: "base", actualizado: null },
    lentitud_servicio:    { clave: "lentitud_servicio",    titulo: "Lentitud del servicio",          tipo: "incidente", categoria: "rendimiento",   equipo: "Infraestructura/TI",      pasos: ["Revisar la carga de servidores y red", "Identificar el cuello de botella (BD/red/CPU)", "Escalar recursos o balancear la carga"], version: 1, origen: "base", actualizado: null },
    // ===== QUEJAS (las resuelve la IA, autónomo) =====
    cambio_dispositivo:   { clave: "cambio_dispositivo",   titulo: "Cambio de dispositivo / celular", tipo: "queja", categoria: "acceso",  equipo: null, pasos: ["Confirmar la identidad del cliente", "Indicar reactivar los tokens en el nuevo dispositivo", "Guiar el reescaneo del carnet si hace falta"], version: 1, origen: "base", actualizado: null },
    deuda_no_actualizada: { clave: "deuda_no_actualizada", titulo: "Pagué y aún figura la deuda",     tipo: "queja", categoria: "datos",   equipo: null, pasos: ["Pedir el comprobante de pago", "Explicar el plazo de conciliación del día", "Si no se refleja, escalar a soporte de datos"], version: 1, origen: "base", actualizado: null },
    datos_no_actualizados:{ clave: "datos_no_actualizados",titulo: "Saldo o movimiento no actualizado", tipo: "queja", categoria: "datos", equipo: null, pasos: ["Pedir refrescar la pantalla", "Indicar esperar unos minutos por la actualización", "Si persiste, registrar para revisión de datos"], version: 1, origen: "base", actualizado: null },
    duda_configuracion:   { clave: "duda_configuracion",   titulo: "Duda de configuración / uso",     tipo: "queja", categoria: "general", equipo: null, pasos: ["Identificar la función consultada", "Explicar el paso a paso de configuración", "Confirmar que el cliente pudo realizarlo"], version: 1, origen: "base", actualizado: null },
    duda_limite:          { clave: "duda_limite",          titulo: "Consulta de límites / montos",    tipo: "queja", categoria: "general", equipo: null, pasos: ["Informar el límite vigente del producto", "Explicar cómo solicitar un aumento si aplica", "Derivar a agencia si requiere firma"], version: 1, origen: "base", actualizado: null }
  }
};

const MIME = { ".html": "text/html; charset=utf-8", ".css": "text/css", ".js": "text/javascript", ".json": "application/json", ".png": "image/png", ".svg": "image/svg+xml", ".ico": "image/x-icon", ".txt": "text/plain; charset=utf-8" };

function proxyToN8n(webhookPath, payload) {
  return new Promise((resolve, reject) => {
    const data = Buffer.from(JSON.stringify(payload || {}));
    const u = new URL(N8N + webhookPath);
    const req = http.request({ hostname: u.hostname, port: u.port, path: u.pathname, method: "POST", headers: { "Content-Type": "application/json", "Content-Length": data.length } }, (res) => {
      let body = ""; res.on("data", (c) => (body += c));
      res.on("end", () => { try { resolve(JSON.parse(body)); } catch (e) { resolve({ raw: body }); } });
    });
    req.on("error", reject);
    req.setTimeout(60000, () => { req.destroy(); reject(new Error("timeout n8n")); });
    req.write(data); req.end();
  });
}
function readBody(req) { return new Promise((resolve) => { let b = ""; req.on("data", (c) => (b += c)); req.on("end", () => { try { resolve(b ? JSON.parse(b) : {}); } catch (e) { resolve({}); } }); }); }
function json(res, code, obj) { const s = JSON.stringify(obj); res.writeHead(code, { "Content-Type": "application/json; charset=utf-8" }); res.end(s); }
function recalcMetricas() {
  // metricas POR CONVERSACION (no por mensaje). Una "queja" = una charla completa.
  const m = { total: store.sesionesList.length, quejas: 0, incidentes: 0, consultas: 0, abiertas: 0, afectados: 0, resueltos: 0 };
  for (const s of store.sesionesList) {
    if (s.tipo === "INCIDENTE") { m.incidentes++; if (s.estado_incidente === "Resuelto") m.resueltos++; }
    else if (s.tipo === "QUEJA") m.quejas++;
    else m.consultas++;
    if (s.abierta) m.abiertas++;
  }
  for (const k in store.incidentes) m.afectados += (store.incidentes[k].users || []).length;
  store.metricas = m;
}
async function enviarWA(payload) { try { const r = await proxyToN8n("/webhook/test-wa", payload); return !!(r && r.messages && r.messages[0] && r.messages[0].id); } catch (e) { return false; } }

// ---- memoria de conversacion ----
function recordar(from, de, texto) { store.conversaciones[from] = store.conversaciones[from] || []; store.conversaciones[from].push({ de, texto }); if (store.conversaciones[from].length > 20) store.conversaciones[from] = store.conversaciones[from].slice(-20); }
function historialDe(from) { const h = store.conversaciones[from] || []; return h.slice(-6).map(x => (x.de === "cliente" ? "Cliente" : "Agente") + ": " + x.texto).join(" || "); }

// ---- sesiones: una "queja" = una conversacion completa (cierra cuando el cliente agradece) ----
const RANK = { CONSULTA: 1, QUEJA: 2, INCIDENTE: 3 };
function esCierre(t) {
  const s = (t || "").toLowerCase().trim();
  if (s.length > 70) return false;
  if (/(pero|sigue|sigo|todav|aun|aún|no func|no anda|no me|no puedo|problema|otra cosa|una mas|una más|consulta|ademas|además)/.test(s)) return false;
  return /\bgracias\b/.test(s) || /(eso es todo|eso seria todo|eso sería todo|nada mas|nada más|listo gracias|chau|adios|adiós|hasta luego|todo bien|perfecto gracias|de una gracias)/.test(s);
}
let _sid = 0;
function nuevaSesion(from, nombre) {
  const s = { id: "S-" + (Date.now()) + "-" + (++_sid), from, nombre: nombre || from, abierta: true, tipo: "CONSULTA", caso_id: null, equipo_asignado: null, recomendacion_tecnica: null, severidad: null, afectados: null, categoria: null, clasificado_por: null, estado_incidente: null, mensajes: [], inicio: new Date().toISOString(), fin: null };
  store.sesiones[from] = s; store.sesionesList.unshift(s); if (store.sesionesList.length > 300) store.sesionesList.pop();
  return s;
}
function sesionPorCaso(casoId) { return store.sesionesList.find(s => s.caso_id === casoId); }

// ---- catalogo de problemas: codigo PRB-### + auto-alta cuando llega un problema nuevo ----
(function () { let n = 0; for (const k in store.playbooks) { n++; if (!store.playbooks[k].codigo) store.playbooks[k].codigo = "PRB-" + String(n).padStart(3, "0"); } })();
function siguienteCodigo() { let mx = 0; for (const k in store.playbooks) { const m = /(\d+)\s*$/.exec(store.playbooks[k].codigo || ""); if (m) mx = Math.max(mx, parseInt(m[1])); } return "PRB-" + String(mx + 1).padStart(3, "0"); }
function tituloDeFirma(firma, data) {
  const f = String(firma || "").trim();
  if (!f || f === "otro" || f.indexOf("_") < 0) return "Incidente · " + ((data && data.categoria) || "general") + (data && data.producto && data.producto !== "desconocido" ? " (" + data.producto + ")" : "");
  return f.replace(/_/g, " ").replace(/^\w/, c => c.toUpperCase());
}
// Cargar SOLUCIONES DETALLADAS paso a paso (runbooks completos) si existe el archivo.
try {
  const det = JSON.parse(fs.readFileSync(path.join(__dirname, "playbooks_detallados.json"), "utf8"));
  for (const k in det) {
    const d = det[k]; const pasos = Array.isArray(d) ? d : d.pasos;
    if (store.playbooks[k]) { if (pasos && pasos.length) store.playbooks[k].pasos = pasos; if (d.titulo) store.playbooks[k].titulo = d.titulo; if (d.objetivo) store.playbooks[k].objetivo = d.objetivo; }
    else { store.playbooks[k] = { clave: k, codigo: siguienteCodigo(), titulo: d.titulo || k, tipo: d.tipo || "incidente", categoria: d.categoria || "general", equipo: d.equipo || null, objetivo: d.objetivo || null, pasos: pasos || [], version: 1, origen: "catálogo", actualizado: null }; }
  }
  console.log("Playbooks detallados cargados.");
} catch (e) { /* aún no existe el archivo: se usan los pasos base */ }

// Busca el playbook del problema; si no existe en el catalogo, lo AÑADE con el siguiente codigo.
function playbookParaIncidente(data) {
  const firma = (data && data.firma) || (((data && data.producto) || "x") + "_" + ((data && data.categoria) || "general"));
  let pb = store.playbooks[firma];
  if (!pb) {
    pb = store.playbooks[firma] = { clave: firma, codigo: siguienteCodigo(), titulo: tituloDeFirma(firma, data), tipo: "incidente", categoria: (data && data.categoria) || "general", equipo: (data && data.equipo_asignado) || null, pasos: [], version: 1, origen: "detectado automáticamente (problema nuevo)", actualizado: null };
  }
  return pb;
}

// Tiempo estimado de solucion, en lenguaje simple, segun la gravedad.
function tiempoEstimado(sev) {
  const s = (sev || "").toLowerCase();
  if (/crit|grav/.test(s)) return "lo más rápido posible (calculamos en menos de 1 hora)";
  if (/alta|urg/.test(s)) return "en más o menos 1 a 2 horas";
  if (/med/.test(s)) return "en el transcurso del día de hoy";
  return "dentro de las próximas horas";
}
// Estado del pedido, en lenguaje simple (sin tecnicismos, sin decir "incidente"/"caso").
function progresoTexto(casoId, estado, severidad) {
  if (estado === "Resuelto")
    return "✅ ¡Listo! 🎉 Ya solucionamos lo que te estaba pasando. Ya podés usar todo con normalidad.\n\n¡Gracias por esperar! 💜 — BancoSol";
  return "Estamos trabajando en lo tuyo ahora mismo 👇\n\n✅ Recibido\n🔵 Arreglándolo  ← acá estamos\n⬜ Listo\n\nCalculamos que va a estar solucionado " + tiempoEstimado(severidad) + ". Te avisamos por acá apenas funcione. 🙌 — BancoSol";
}
// Aviso cuando se recibe un problema grande (sin jerga; con tiempo estimado).
function menuEstado(casoId, severidad) {
  return { type: "button", body: { text: "¡Hola! 😊 Ya recibimos tu mensaje y entendemos lo que te está pasando.\n\nNuestro equipo ya se puso a trabajar para solucionarlo. Calculamos que va a estar listo " + tiempoEstimado(severidad) + ".\n\nApenas funcione, te avisamos por acá mismo. 🙌\n\nTu número de seguimiento es " + casoId + ". Si querés ver cómo va, tocá el botón. 💜 — BancoSol" }, action: { buttons: [{ type: "reply", reply: { id: "estado:" + casoId, title: "📋 Ver cómo va" } }] } };
}

// ---- generar informe ASFI (editable) ----
function generarInforme(casoId, inc, sol) {
  const fecha = new Date().toISOString().slice(0, 10);
  const users = (inc && inc.users) || [];
  return [
    "INFORME DE INCIDENCIA — BancoSol (para ASFI)",
    "============================================",
    "Código único de caso: " + casoId,
    "Fecha de emisión: " + fecha,
    "Tipo: INCIDENTE",
    "Canal de ingreso: WhatsApp",
    "Producto: " + ((inc && inc.producto) || "no especificado"),
    "Categoría / tipología: " + ((inc && inc.categoria) || "general"),
    "Clientes afectados: " + users.length,
    "",
    "1. CONTEXTO (reportado por el cliente):",
    "   " + ((inc && inc.mensaje) || "(sin detalle)"),
    "",
    "2. DATOS DEL CLIENTE:",
    "   Nombre: " + ((inc && inc.nombre) || "—"),
    "   Teléfono(s) afectado(s): " + (users.length ? users.join(", ") : "—"),
    "",
    "3. SOLUCIÓN APLICADA (equipo técnico de BancoSol):",
    "   " + sol,
    "",
    "4. ACCIONES PREVENTIVAS:",
    "   La solución fue incorporada al manual operativo (playbook) para resolución",
    "   automática de casos similares en el futuro.",
    "",
    "Estado del caso: RESUELTO",
    "Aprobación: PENDIENTE — editar y aprobar para archivar y enviar a ASFI."
  ].join("\n");
}

// ---- procesar mensaje de texto entrante ----
async function procesarEntrante({ from, nombre, texto }) {
  const ts = new Date().toISOString();
  recordar(from, "cliente", texto);

  // 1) Cierre de conversacion: el cliente agradece / dice "eso es todo".
  //    No abre una queja nueva: cierra la actual. Si vuelve a escribir luego, sera otra queja.
  if (esCierre(texto)) {
    const s = store.sesiones[from];
    const reply = "¡De nada! 😊 Fue un gusto ayudarte. Cualquier cosa, escribinos cuando quieras. — BancoSol";
    if (s && s.abierta) { s.mensajes.push({ de: "cliente", texto, ts }); s.mensajes.push({ de: "agente", texto: reply, ts }); s.abierta = false; s.fin = ts; }
    recordar(from, "agente", reply);
    await enviarWA({ to: from, text: reply });
    recalcMetricas();
    return;
  }

  // 2) Asegurar una sesion abierta (si la anterior se cerro, esta es una NUEVA queja).
  let s = store.sesiones[from];
  if (!s || !s.abierta) s = nuevaSesion(from, nombre);
  if (nombre) s.nombre = nombre;
  const historial = historialDe(from);

  try {
    let data = await proxyToN8n("/webhook/soluno-reporte", { canal: "WhatsApp", producto: "", agencia: "", mensaje: texto, cliente: { telefono: from, nombre: nombre || "" }, remitente: from, historial });
    if (Array.isArray(data)) data = data[0];
    const res = (data && data.resultado) || "CONSULTA";

    // anexar a la conversacion
    s.mensajes.push({ de: "cliente", texto, ts, resultado: res });
    if (data && data.respuesta_cliente) { s.mensajes.push({ de: "agente", texto: data.respuesta_cliente, ts }); recordar(from, "agente", data.respuesta_cliente); }

    // el tipo de la conversacion = el de mayor severidad visto (INCIDENTE > QUEJA > CONSULTA)
    if ((RANK[res] || 1) >= (RANK[s.tipo] || 1)) {
      s.tipo = res;
      s.categoria = (data && data.categoria) || s.categoria;
      s.clasificado_por = (data && data.clasificado_por) || s.clasificado_por;
      if (res === "INCIDENTE") {
        s.caso_id = data.caso_id; s.equipo_asignado = data.equipo_asignado;
        s.recomendacion_tecnica = data.recomendacion_tecnica;
        s.severidad = data.severidad || (data.paquete_para_desarrollo && data.paquete_para_desarrollo.severidad);
        s.afectados = data.afectados || (data.paquete_para_desarrollo && data.paquete_para_desarrollo.afectados);
        if (!s.estado_incidente) s.estado_incidente = "En proceso";
      }
    }
    if (data && data.resultado) { store.casos.unshift(data); if (store.casos.length > 500) store.casos.pop(); }
    recalcMetricas();

    // responder por WhatsApp
    if (res === "INCIDENTE") {
      const cid = data.caso_id;
      if (!store.incidentes[cid]) store.incidentes[cid] = { caso_id: cid, estado: "En proceso", producto: data.producto, categoria: data.categoria, firma: data.firma, severidad: s.severidad, equipo_asignado: data.equipo_asignado, mensaje: texto, ts, users: [], recomendacion_tecnica: data.recomendacion_tecnica, nombre: nombre || from };
      if (!store.incidentes[cid].users.includes(from)) store.incidentes[cid].users.push(from);
      if (data.firma) store.incidentes[cid].firma = data.firma;
      if (s.severidad) store.incidentes[cid].severidad = s.severidad;
      if (data.recomendacion_tecnica) store.incidentes[cid].recomendacion_tecnica = data.recomendacion_tecnica;
      s.firma = data.firma;
      // buscar el problema en el catalogo (o anadirlo si es nuevo) y vincular su codigo
      const pb = playbookParaIncidente(data);
      s.playbook_codigo = pb.codigo; s.playbook_titulo = pb.titulo; s.playbook_nuevo = (pb.version === 1 && (!pb.pasos || !pb.pasos.length));
      store.incidentes[cid].playbook_codigo = pb.codigo;
      s.estado_incidente = store.incidentes[cid].estado;
      s.respondido = await enviarWA({ to: from, interactive: menuEstado(cid, s.severidad) });
    } else if (data && data.respuesta_cliente) {
      s.respondido = await enviarWA({ to: from, text: data.respuesta_cliente });
    }
  } catch (e) { s.error = String(e.message); }
}

async function procesarBoton({ from, buttonId }) {
  let cid = null; const m = /:(.+)$/.exec(buttonId || ""); if (m) cid = m[1];
  if (!cid || !store.incidentes[cid]) { const mine = Object.values(store.incidentes).filter(i => i.users.includes(from)).sort((a, b) => (a.ts < b.ts ? 1 : -1)); if (mine[0]) cid = mine[0].caso_id; }
  const inc = cid && store.incidentes[cid]; const estado = inc ? inc.estado : "En proceso"; const cidShow = cid || "tu caso";
  const txt = progresoTexto(cidShow, estado, inc ? inc.severidad : null);
  await enviarWA({ to: from, text: txt });
  // reflejar la consulta de estado dentro de la conversacion del cliente
  const ts = new Date().toISOString();
  const s = sesionPorCaso(cid) || store.sesiones[from];
  if (s) { s.mensajes.push({ de: "cliente", texto: "📋 (consultó cómo va su pedido)", ts }); s.mensajes.push({ de: "agente", texto: txt, ts }); }
}

// ---- resolver incidente: playbook + aviso + informe ASFI ----
async function resolverIncidente({ caso_id, producto, categoria, solucion }) {
  const inc = store.incidentes[caso_id];
  const sol = (solucion && solucion.trim()) || (inc && inc.recomendacion_tecnica) || "El equipo técnico aplicó la corrección y validó el servicio.";
  try { let r = await proxyToN8n("/webhook/soluno-resolver", { caso_id, producto: producto || (inc && inc.producto) || "desconocido", categoria: categoria || (inc && inc.categoria) || "general", severidad: "alta", afectados: inc ? inc.users.length : 1, solucion: sol }); if (Array.isArray(r)) r = r[0]; } catch (e) {}
  // playbook vivo: actualiza la SOLUCIÓN del playbook del tipo de problema (por firma).
  const cat = categoria || (inc && inc.categoria) || "general";
  const firma = inc && inc.firma;
  let key = (firma && store.playbooks[firma]) ? firma
    : (Object.keys(store.playbooks).find(k => store.playbooks[k].categoria === cat && store.playbooks[k].tipo === "incidente") || firma || cat || "otro");
  const pasos = String(sol).split(/[\n.;]+/).map(s => s.trim()).filter(s => s.length > 3).map(s => s.charAt(0).toUpperCase() + s.slice(1));
  const prev = store.playbooks[key] || {};
  store.playbooks[key] = { clave: key, codigo: prev.codigo || siguienteCodigo(), titulo: prev.titulo || ("Solución · " + cat), tipo: prev.tipo || "incidente", categoria: prev.categoria || cat, equipo: prev.equipo || (inc && inc.equipo_asignado) || null, pasos: pasos.length ? pasos : (prev.pasos || []), version: (prev.version ? prev.version + 1 : 1), origen: "actualizado al resolver " + caso_id, actualizado: new Date().toISOString() };
  // marcar resuelto
  if (inc) inc.estado = "Resuelto";
  const c = store.casos.find(x => x && x.caso_id === caso_id); if (c) c.estado = "Resuelto";
  const ses = sesionPorCaso(caso_id); if (ses) ses.estado_incidente = "Resuelto";
  recalcMetricas();
  // informe ASFI editable
  store.informes[caso_id] = { caso_id, texto: generarInforme(caso_id, inc, sol), aprobado: false, ts: new Date().toISOString(), users: inc ? inc.users : [] };
  // avisar a los afectados por WhatsApp
  let avisados = 0;
  if (inc) for (const u of inc.users) { const ok = await enviarWA({ to: u, text: "✅ ¡Buenas noticias! 🎉 Ya arreglamos lo que te estaba pasando. Ya podés usar todo con normalidad.\n\n¡Gracias por tu paciencia! 💜 — BancoSol" }); if (ok) avisados++; }
  return { ok: true, caso_id, avisados, informe: store.informes[caso_id] };
}

const server = http.createServer(async (req, res) => {
  const url = new URL(req.url, "http://" + req.headers.host); const p = url.pathname;

  if (p === "/webhook/whatsapp" && req.method === "GET") {
    if (url.searchParams.get("hub.mode") === "subscribe" && url.searchParams.get("hub.verify_token") === VERIFY_TOKEN) { res.writeHead(200, { "Content-Type": "text/plain" }); return res.end(url.searchParams.get("hub.challenge") || ""); }
    res.writeHead(403); return res.end("forbidden");
  }
  if (p === "/webhook/whatsapp" && req.method === "POST") {
    const payload = await readBody(req); res.writeHead(200); res.end("EVENT_RECEIVED");
    try {
      const value = payload.entry && payload.entry[0] && payload.entry[0].changes && payload.entry[0].changes[0] && payload.entry[0].changes[0].value;
      const msg = value && value.messages && value.messages[0];
      if (msg) {
        const from = msg.from; const nombre = value.contacts && value.contacts[0] && value.contacts[0].profile && value.contacts[0].profile.name;
        if (msg.type === "text") procesarEntrante({ from, nombre, texto: msg.text.body });
        else if (msg.type === "interactive" && msg.interactive && msg.interactive.button_reply) procesarBoton({ from, buttonId: msg.interactive.button_reply.id });
      }
    } catch (e) { console.error("err webhook:", e.message); }
    return;
  }

  if (p === "/api/reporte" && req.method === "POST") { const b = await readBody(req); try { let d = await proxyToN8n("/webhook/soluno-reporte", b); if (Array.isArray(d)) d = d[0]; if (d && d.resultado) { store.casos.unshift(d); recalcMetricas(); } return json(res, 200, d); } catch (e) { return json(res, 502, { error: String(e.message) }); } }
  if (p === "/api/resolver" && req.method === "POST") { const b = await readBody(req); try { return json(res, 200, await resolverIncidente(b)); } catch (e) { return json(res, 502, { error: String(e.message) }); } }
  if (p === "/api/wa-send" && req.method === "POST") { const b = await readBody(req); try { return json(res, 200, await proxyToN8n("/webhook/test-wa", b)); } catch (e) { return json(res, 502, { error: String(e.message) }); } }
  if (p === "/api/informe" && req.method === "GET") { const c = url.searchParams.get("caso"); return json(res, 200, store.informes[c] || null); }
  if (p === "/api/informe/aprobar" && req.method === "POST") {
    const b = await readBody(req); const inf = store.informes[b.caso_id];
    if (!inf) return json(res, 404, { error: "informe no encontrado" });
    if (typeof b.texto === "string") inf.texto = b.texto;
    inf.aprobado = true; inf.aprobado_ts = new Date().toISOString();
    try { fs.writeFileSync(path.join(INFORMES_DIR, "ASFI-" + b.caso_id + ".txt"), inf.texto, "utf8"); inf.archivo = "informes/ASFI-" + b.caso_id + ".txt"; } catch (e) { inf.archivo_error = String(e.message); }
    return json(res, 200, inf);
  }
  if (p === "/api/informes" && req.method === "GET") return json(res, 200, Object.values(store.informes));
  if (p === "/api/playbooks" && req.method === "GET") return json(res, 200, Object.values(store.playbooks));
  if (p === "/api/conversaciones" && req.method === "GET") return json(res, 200, store.sesionesList);
  if (p === "/api/inbox" && req.method === "GET") return json(res, 200, store.sesionesList);
  if (p === "/api/casos" && req.method === "GET") return json(res, 200, store.casos);
  if (p === "/api/metrics" && req.method === "GET") { recalcMetricas(); return json(res, 200, store.metricas); }
  if (p === "/api/health" && req.method === "GET") return json(res, 200, { ok: true, n8n: N8N });

  let file = p === "/" ? "/index.html" : p;
  const safe = path.normalize(path.join(PUBLIC_DIR, file));
  if (!safe.startsWith(PUBLIC_DIR)) { res.writeHead(403); return res.end("forbidden"); }
  fs.readFile(safe, (err, content) => { if (err) { res.writeHead(404); return res.end("no encontrado"); } res.writeHead(200, { "Content-Type": MIME[path.extname(safe)] || "application/octet-stream", "Cache-Control": "no-cache, no-store, must-revalidate", "Pragma": "no-cache", "Expires": "0" }); res.end(content); });
});
server.listen(PORT, () => { console.log("SolUno backend en http://localhost:" + PORT + " | n8n: " + N8N); });

==========================================================================================
ARCHIVO: app/public/index.html
==========================================================================================
<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>SolUno · Centro de operaciones — BancoSol</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700;900&family=Roboto+Mono:wght@500&display=swap" rel="stylesheet">
<style>
:root{
  /* ============ MARCA BancoSol (identidad 2022: morado + naranja + magenta — "el nuevo Sol") ============ */
  --sol-purple:#5B2A86; --sol-purple-700:#4A2270; --sol-purple-900:#34164F; --sol-purple-deep:#2A1142;
  --sol-magenta:#D6217F; --sol-orange:#F58220; --sol-orange-700:#E06D12;
  --sun:linear-gradient(120deg,#F58220 0%,#E03B82 52%,#7A2E96 100%);     /* amanecer */
  --purple-grad:linear-gradient(165deg,#5B2A86 0%,#3A1A5A 100%);

  /* ============ superficies (fondo lila tenue para que las tarjetas blancas resalten) ============ */
  --bg-app:#F2EEF8; --bg-canvas:#F2EEF8; --bg-surface:#FFFFFF;
  --bg-surface-2:#F0EAF8; --bg-subtle:#EEE7F6; --bg-hover:#F6F1FC; --glass:rgba(255,255,255,.82);

  /* ============ bordes (mas marcados = mas contraste) ============ */
  --border:#DFD6EE; --border-2:#CFC1E6; --border-strong:#B49CD6; --hairline:#E9E1F4;

  /* ============ texto (alto contraste) ============ */
  --tp:#1F0E33; --ts:#43374F; --tm:#6B5E7E; --td:#9788A8; --ton:#FFFFFF;

  /* ============ acento = morado de marca ============ */
  --accent:#5B2A86; --accent-hover:#4A2270; --accent-press:#34164F;
  --accent-subtle:#F1E9FA; --accent-text:#4A2270; --accent-ring:rgba(91,42,134,.32);

  /* ============ whatsapp / IA ============ */
  --wa:#1FAE54; --wa-deep:#0E7C39; --wa-subtle:#E3F7EB; --wa-bubble:#E1F6E9;

  /* ============ semanticos (con marca BancoSol) ============ */
  --success:#138A45; --success-fg:#0E7C39; --success-subtle:#E3F7EB; --success-bd:#9FE2BA;
  --warning:#E06D12; --warning-fg:#B5560C; --warning-subtle:#FCEEDC; --warning-bd:#F6C893;
  --danger:#D6217F;  --danger-fg:#B11468;  --danger-subtle:#FCE6F0; --danger-bd:#F3B2D2;
  --info:#5B2A86;    --info-fg:#4A2270;    --info-subtle:#F1E9FA;   --info-bd:#D9C6EE;

  /* ============ KPI: fondo + borde + color de marca por tarjeta ============ */
  --kpi-purple:linear-gradient(160deg,#F4EDFC 0%,#EADCF8 100%);  --kpi-purple-bd:#D7C2EF;  --kpi-purple-ico:#5B2A86;
  --kpi-magenta:linear-gradient(160deg,#FDE9F2 0%,#FBD6E8 100%); --kpi-magenta-bd:#F3AFD0; --kpi-magenta-ico:#C81E76;
  --kpi-orange:linear-gradient(160deg,#FFF0E0 0%,#FFE1C4 100%);  --kpi-orange-bd:#F6C58F;  --kpi-orange-ico:#D9650F;
  --kpi-blue:linear-gradient(160deg,#E9EFFF 0%,#DAE4FF 100%);    --kpi-blue-bd:#BFCFF7;    --kpi-blue-ico:#3346B8;
  --kpi-green:linear-gradient(160deg,#E5F7EC 0%,#D0F0DD 100%);   --kpi-green-bd:#9FE2BA;   --kpi-green-ico:#0E7C39;

  /* ============ sombras (algo mas marcadas para separar superficies) ============ */
  --shadow:268 45% 16%;
  --xs:0 1px 2px hsl(var(--shadow)/.08);
  --sm:0 2px 5px hsl(var(--shadow)/.09),0 1px 2px hsl(var(--shadow)/.06);
  --md:0 5px 14px hsl(var(--shadow)/.12),0 2px 5px hsl(var(--shadow)/.07);
  --lg:0 14px 32px hsl(var(--shadow)/.16),0 4px 10px hsl(var(--shadow)/.09);
  --lift:0 16px 36px -8px hsl(var(--shadow)/.30);
  --inset:inset 0 0 0 1px hsl(var(--shadow)/.05);

  --r-xs:7px; --r-sm:10px; --r-md:14px; --r-lg:18px; --r-xl:24px; --r-pill:999px;
  --ease:cubic-bezier(.16,1,.3,1); --dur:.22s;

  /* ============ tipografia Google (Roboto) ============ */
  --font:'Roboto','Google Sans','Segoe UI',system-ui,-apple-system,BlinkMacSystemFont,Arial,sans-serif,'Apple Color Emoji','Segoe UI Emoji';
  --mono:'Roboto Mono',ui-monospace,'SF Mono',Menlo,Consolas,monospace;
}
*{box-sizing:border-box;margin:0;padding:0}
html{-webkit-text-size-adjust:100%}
body{font-family:var(--font);background:var(--bg-app);color:var(--ts);font-size:15.5px;line-height:1.55;
  -webkit-font-smoothing:antialiased;-moz-osx-font-smoothing:grayscale;text-rendering:optimizeLegibility;
  font-variant-numeric:tabular-nums}
::selection{background:var(--accent-subtle);color:var(--accent-press)}
button{font-family:inherit}

/* ---------- app shell ---------- */
.app{display:grid;grid-template-columns:272px 1fr;min-height:100vh;background:var(--bg-app)}

/* ---------- sidebar (morado degradado de marca) ---------- */
.sidebar{height:100vh;position:sticky;top:0;background:var(--purple-grad);
  padding:22px 16px;display:flex;flex-direction:column;gap:2px;box-shadow:4px 0 24px hsl(var(--shadow)/.16)}
.brand{display:flex;align-items:center;gap:13px;padding:8px 10px;margin-bottom:20px}
.brand .logo{width:50px;height:50px;display:grid;place-items:center;
  filter:drop-shadow(0 3px 9px rgba(214,33,127,.5))}
.brand .logo svg{width:100%;height:100%;display:block}
.brand b{font-size:19px;font-weight:700;letter-spacing:-.01em;color:#fff;display:block;line-height:1.1}
.brand small{font-size:12px;color:rgba(255,255,255,.72);font-weight:500}
.navgrp{font-size:11px;font-weight:700;letter-spacing:.09em;text-transform:uppercase;
  color:rgba(255,255,255,.55);padding:18px 12px 7px}
.nav{display:flex;align-items:center;gap:13px;padding:11px 13px;border-radius:var(--r-sm);
  font-size:14.5px;font-weight:500;color:rgba(255,255,255,.85);cursor:pointer;border:none;background:transparent;
  width:100%;text-align:left;position:relative;transition:background var(--dur) var(--ease),color var(--dur) var(--ease)}
.nav .ic{font-size:17px;width:22px;text-align:center}
.nav:hover{background:rgba(255,255,255,.12);color:#fff}
.nav.active{background:rgba(255,255,255,.16);color:#fff;font-weight:700}
.nav.active::before{content:"";position:absolute;left:-16px;top:50%;transform:translateY(-50%);
  width:4px;height:24px;border-radius:0 4px 4px 0;background:var(--sol-orange);box-shadow:0 0 12px rgba(245,130,32,.7)}
.nav .cnt{margin-left:auto;font-size:12px;font-weight:700;color:#fff;background:rgba(255,255,255,.18);
  border-radius:var(--r-pill);min-width:23px;height:22px;display:grid;place-items:center;padding:0 8px;
  font-variant-numeric:tabular-nums;transition:.16s var(--ease)}
.nav.active .cnt{background:#fff;color:var(--sol-purple)}
.sb-foot{margin-top:auto;padding:14px 12px;border-top:1px solid rgba(255,255,255,.14);
  font-size:12px;color:rgba(255,255,255,.7);display:flex;align-items:center}

/* live dot pulsante */
.dot-live{display:inline-block;width:9px;height:9px;border-radius:50%;background:#3DDC84;margin-right:8px;
  vertical-align:middle;box-shadow:0 0 0 0 rgba(61,220,132,.6);animation:pulse 2s var(--ease) infinite}
@keyframes pulse{0%{box-shadow:0 0 0 0 rgba(61,220,132,.55)}70%{box-shadow:0 0 0 8px rgba(61,220,132,0)}100%{box-shadow:0 0 0 0 rgba(61,220,132,0)}}

/* ---------- main / topbar ---------- */
.main{display:flex;flex-direction:column;min-width:0;background:var(--bg-canvas)}
.topbar{min-height:78px;display:flex;align-items:center;gap:16px;padding:14px 36px;
  background:var(--glass);backdrop-filter:saturate(180%) blur(16px);-webkit-backdrop-filter:saturate(180%) blur(16px);
  border-bottom:1px solid var(--border);position:sticky;top:0;z-index:20}
.topbar::after{content:"";position:absolute;left:0;right:0;bottom:-1px;height:3px;background:var(--sun);opacity:.9}
.topbar h1{font-size:24px;font-weight:800;letter-spacing:-.025em;color:var(--tp);line-height:1.2}
.topbar p{font-size:14px;color:var(--tm);margin-top:2px;max-width:760px}
.live-pill{margin-left:auto;display:inline-flex;align-items:center;gap:8px;font-size:13.5px;font-weight:700;
  color:var(--wa-deep);background:var(--wa-subtle);padding:9px 16px;border-radius:var(--r-pill);
  border:1.5px solid var(--success-bd);font-variant-numeric:tabular-nums;white-space:nowrap}
.content{padding:30px 36px 64px;display:flex;flex-direction:column;gap:26px;max-width:1320px;width:100%}

/* ============================================================ KPI CARDS ============================================================ */
.kpis{display:grid;gap:18px;grid-template-columns:repeat(auto-fit,minmax(206px,1fr))}
.kpi{position:relative;overflow:hidden;border-radius:var(--r-lg);padding:20px 22px;
  --c-bg:var(--kpi-purple);--c-bd:var(--kpi-purple-bd);--c-ico:var(--kpi-purple-ico);
  background:var(--c-bg);border:1.5px solid var(--c-bd);border-top:5px solid var(--c-ico);
  display:flex;flex-direction:column;gap:9px;box-shadow:var(--sm);
  transition:transform var(--dur) var(--ease),box-shadow var(--dur) var(--ease)}
.kpi::before{content:"";position:absolute;top:-50px;right:-44px;width:140px;height:140px;border-radius:50%;
  background:var(--c-ico);opacity:.10;filter:blur(8px);pointer-events:none}
.kpi:hover{transform:translateY(-4px);box-shadow:var(--lift)}
.kpi.dang{--c-bg:var(--kpi-magenta);--c-bd:var(--kpi-magenta-bd);--c-ico:var(--kpi-magenta-ico)}
.kpi.warn{--c-bg:var(--kpi-orange);--c-bd:var(--kpi-orange-bd);--c-ico:var(--kpi-orange-ico)}
.kpi.acc{--c-bg:var(--kpi-blue);--c-bd:var(--kpi-blue-bd);--c-ico:var(--kpi-blue-ico)}
.kpi:nth-child(5){--c-bg:var(--kpi-green);--c-bd:var(--kpi-green-bd);--c-ico:var(--kpi-green-ico)}
.kpi .lab{position:relative;z-index:1;font-size:13.5px;font-weight:700;color:var(--c-ico);
  display:flex;align-items:center;gap:8px;letter-spacing:.005em}
.kpi .val{position:relative;z-index:1;font-size:44px;line-height:1;font-weight:900;letter-spacing:-.03em;
  color:var(--c-ico);font-variant-numeric:tabular-nums}
.kpi .sub{position:relative;z-index:1;font-size:12.5px;color:var(--tm);font-weight:500}

/* ============================================================ SEGMENTED ============================================================ */
.seg{display:inline-flex;gap:4px;padding:4px;background:var(--bg-surface);border:1.5px solid var(--border-2);
  border-radius:var(--r-md);width:fit-content;box-shadow:var(--xs)}
.seg button{border:none;background:transparent;cursor:pointer;padding:9px 19px;border-radius:9px;
  font-size:14px;font-weight:600;color:var(--tm);font-family:inherit;letter-spacing:-.01em;
  transition:color var(--dur) var(--ease),background var(--dur) var(--ease),box-shadow var(--dur) var(--ease)}
.seg button:hover{color:var(--accent-text)}
.seg button.active{background:var(--accent);color:#fff;font-weight:700;box-shadow:0 2px 8px rgba(91,42,134,.3)}

/* ============================================================ CONVERSATION CARD ============================================================ */
.feed{display:flex;flex-direction:column;gap:20px}
.conv{background:var(--bg-surface);border:1.5px solid var(--border-2);border-radius:var(--r-lg);
  box-shadow:var(--sm);overflow:hidden;animation:fade .45s var(--ease) both;
  transition:box-shadow var(--dur) var(--ease),border-color var(--dur) var(--ease),transform var(--dur) var(--ease)}
.conv:hover{box-shadow:var(--md);border-color:var(--border-strong);transform:translateY(-2px)}
@keyframes fade{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
.conv-h{display:flex;align-items:center;gap:14px;padding:17px 22px;border-bottom:1.5px solid var(--hairline);
  background:linear-gradient(180deg,#FBF9FE,#FFFFFF)}
.av{width:46px;height:46px;border-radius:50%;display:grid;place-items:center;font-weight:700;font-size:17px;
  color:#fff;flex:none;letter-spacing:-.02em;box-shadow:inset 0 0 0 1px rgba(255,255,255,.25),0 2px 6px hsl(var(--shadow)/.2)}
.conv-h .who b{font-size:16px;font-weight:700;color:var(--tp);display:block;letter-spacing:-.01em}
.conv-h .who span{font-size:13px;color:var(--tm);font-variant-numeric:tabular-nums}
.conv-h .tags{margin-left:auto;display:flex;align-items:center;gap:9px;flex-wrap:wrap;justify-content:flex-end}
.badge{display:inline-flex;align-items:center;gap:7px;padding:5px 12px;border-radius:var(--r-pill);
  font-size:12.5px;font-weight:700;line-height:1;white-space:nowrap;letter-spacing:.01em;border:1.5px solid transparent}
.badge::before{content:"";width:7px;height:7px;border-radius:50%;background:currentColor}
.b-inc{color:var(--danger-fg);background:var(--danger-subtle);border-color:var(--danger-bd)}
.b-qja{color:var(--warning-fg);background:var(--warning-subtle);border-color:var(--warning-bd)}
.b-con{color:var(--info-fg);background:var(--info-subtle);border-color:var(--info-bd)}
.b-ok{color:var(--success-fg);background:var(--success-subtle);border-color:var(--success-bd)}
.b-live{color:var(--wa-deep);background:var(--wa-subtle);border-color:var(--success-bd)}
.b-live::before{animation:pulse 1.6s var(--ease) infinite}
.b-gray{color:var(--tm);background:var(--bg-surface-2);border-color:var(--border-2)}
.code{font-family:var(--mono);font-size:12px;font-weight:600;color:var(--accent-text);background:var(--accent-subtle);
  padding:4px 10px;border-radius:var(--r-xs);border:1.5px solid var(--info-bd);letter-spacing:-.01em}
/* chat thread */
.chat{display:flex;flex-direction:column;gap:9px;padding:20px 22px;background:var(--bg-canvas);
  background-image:radial-gradient(hsl(var(--shadow)/.04) 1px,transparent 0);background-size:24px 24px}
.msg{max-width:76%;padding:11px 15px;font-size:14.5px;line-height:1.5;border-radius:16px;
  box-shadow:0 1px 2px hsl(var(--shadow)/.07);word-wrap:break-word;animation:bub .3s var(--ease) both}
@keyframes bub{from{opacity:0;transform:translateY(4px) scale(.985)}to{opacity:1;transform:none}}
.msg.in{align-self:flex-start;background:var(--bg-surface);color:var(--tp);border:1.5px solid var(--border-2);
  border-bottom-left-radius:6px}
.msg.out{align-self:flex-end;background:var(--wa-bubble);color:#093A28;border:1.5px solid var(--success-bd);
  border-bottom-right-radius:6px}
.msg .mt{display:block;margin-top:5px;font-size:11px;color:var(--tm);opacity:.9;font-variant-numeric:tabular-nums}
.msg.out .mt{text-align:right;color:var(--wa-deep)}
.msg.out .mt::before{content:"IA · ";font-weight:700}

/* ============================================================ INCIDENT PANEL ============================================================ */
.panel{padding:20px 22px;border-top:1.5px solid var(--hairline);background:var(--bg-surface);
  display:flex;flex-direction:column;gap:15px}
.meta-row{display:flex;flex-wrap:wrap;gap:9px}
.chip{font-size:12.5px;color:var(--ts);background:var(--bg-surface-2);border:1.5px solid var(--border-2);
  padding:6px 13px;border-radius:var(--r-pill);font-weight:500}
.chip b{color:var(--accent-text);font-weight:700}
/* "Solucion recomendada por la IA" — morado de marca */
.rec{position:relative;background:linear-gradient(180deg,#F7F1FD 0%,#F1E7FA 100%);border:1.5px solid var(--info-bd);
  border-radius:var(--r-md);padding:15px 17px 15px 20px;overflow:hidden}
.rec::before{content:"";position:absolute;left:0;top:0;bottom:0;width:4px;background:var(--sun)}
.rec .h{font-size:11.5px;font-weight:800;letter-spacing:.05em;text-transform:uppercase;color:var(--accent-text);
  display:flex;align-items:center;gap:7px;margin-bottom:8px}
.rec p{font-size:14.5px;color:var(--ts);line-height:1.55}
.field label{font-size:13.5px;font-weight:700;color:var(--ts);display:block;margin-bottom:8px}
textarea{width:100%;background:var(--bg-surface);border:1.5px solid var(--border-2);border-radius:var(--r-sm);
  padding:13px 15px;font-size:14.5px;color:var(--tp);font-family:inherit;line-height:1.55;resize:vertical;min-height:90px;
  transition:border-color .15s var(--ease),box-shadow .15s var(--ease)}
textarea::placeholder{color:var(--td)}
textarea:hover{border-color:var(--border-strong)}
textarea:focus{outline:none;border-color:var(--accent);box-shadow:0 0 0 3px var(--accent-ring)}
.hint{font-size:12.5px;color:var(--tm);line-height:1.5}
/* botones */
.btn{display:inline-flex;align-items:center;justify-content:center;gap:9px;height:46px;padding:0 22px;
  border-radius:var(--r-pill);font-size:14.5px;font-weight:700;letter-spacing:-.01em;cursor:pointer;
  border:none;font-family:inherit;
  transition:background .16s var(--ease),box-shadow .16s var(--ease),transform .06s var(--ease),filter .16s}
.btn:active{transform:translateY(1px)}
.btn:disabled{opacity:.6;cursor:default}
.btn-p{background:var(--accent);color:#fff;box-shadow:0 3px 10px rgba(91,42,134,.32)}
.btn-p:hover{background:var(--accent-hover);box-shadow:0 5px 16px rgba(91,42,134,.36)}
.btn-ok{background:linear-gradient(135deg,#1FAE54,#0E7C39);color:#fff;box-shadow:0 3px 10px rgba(14,124,57,.32)}
.btn-ok:hover{filter:brightness(1.07);box-shadow:0 5px 16px rgba(14,124,57,.36)}
.btn-s{background:var(--bg-surface);color:var(--ts);border:1.5px solid var(--border-2);box-shadow:var(--xs)}
.btn-s:hover{background:var(--bg-hover);border-color:var(--border-strong)}
.resolved{display:flex;align-items:center;gap:12px;font-size:14.5px;color:var(--success-fg);
  background:linear-gradient(180deg,#EFFBF3,#E3F7EB);border:1.5px solid var(--success-bd);
  border-radius:var(--r-md);padding:15px 17px}
.resolved b{font-weight:700;color:var(--tp)}
.resolved a{color:var(--accent);font-weight:700;text-decoration:none;margin-left:auto;white-space:nowrap}
.resolved a:hover{color:var(--accent-hover);text-decoration:underline}

/* ============================================================ SEVERIDAD ============================================================ */
.sev{display:inline-flex;align-items:center;gap:6px;padding:5px 12px;border-radius:var(--r-pill);
  font-size:12.5px;font-weight:800;line-height:1;letter-spacing:.02em;border:1.5px solid transparent;white-space:nowrap}
.sev-leve{color:#0E7C39;background:#E3F7EB;border-color:#9FE2BA}
.sev-med{color:#B5560C;background:#FCEEDC;border-color:#F6C893}
.sev-urg{color:#C2410C;background:#FFE6D8;border-color:#FABF98}
.sev-crit{color:#fff;background:linear-gradient(135deg,#E11D48,#B11468);border-color:transparent;
  box-shadow:0 2px 8px rgba(225,29,72,.45);animation:sevpulse 1.5s var(--ease) infinite}
@keyframes sevpulse{0%,100%{box-shadow:0 2px 8px rgba(225,29,72,.4)}50%{box-shadow:0 2px 16px rgba(225,29,72,.75)}}
.conv-crit{border-color:#F3B2D2!important;box-shadow:0 0 0 1.5px #F3B2D2,var(--md)}

/* ============================================================ DERIVACIÓN (banner + árbol de equipos) ============================================================ */
.route{display:flex;align-items:center;gap:9px;font-size:14.5px;color:var(--accent-text);
  background:linear-gradient(180deg,#F7F1FD,#F1E7FA);border:1.5px solid var(--info-bd);
  border-radius:var(--r-md);padding:13px 16px;font-weight:600}
.route b{color:var(--accent-press);font-weight:800}
.pbref{display:flex;align-items:center;gap:9px;font-size:13.5px;color:var(--ts);background:var(--bg-surface-2);
  border:1.5px solid var(--border-2);border-radius:var(--r-sm);padding:10px 14px;font-weight:500}
.pbref b{font-family:var(--mono);font-size:13px;font-weight:700;color:#fff;background:var(--accent);
  padding:3px 10px;border-radius:var(--r-xs);letter-spacing:.02em}
.pbref .nuevo{margin-left:auto;font-size:11.5px;font-weight:700;color:var(--warning-fg);
  background:var(--warning-subtle);border:1.5px solid var(--warning-bd);padding:3px 10px;border-radius:var(--r-pill)}
.nuevo{display:inline-block;font-size:11px;font-weight:700;color:var(--warning-fg);background:var(--warning-subtle);
  border:1.5px solid var(--warning-bd);padding:2px 9px;border-radius:var(--r-pill);margin-left:6px}
.tree{background:var(--bg-surface);border:1.5px solid var(--border-2);border-radius:var(--r-lg);
  box-shadow:var(--sm);padding:8px;margin-bottom:22px;overflow:hidden}
.tree-h{padding:15px 16px 13px;font-size:16px;font-weight:800;color:var(--tp);
  display:flex;flex-direction:column;gap:3px;border-bottom:1.5px solid var(--hairline);letter-spacing:-.01em}
.tree-h small{font-size:12.5px;font-weight:500;color:var(--tm);letter-spacing:0}
.tr-row{display:flex;align-items:center;gap:14px;padding:13px 14px;border-radius:var(--r-sm);
  transition:background .15s var(--ease)}
.tr-row:hover{background:var(--bg-hover)}
.tr-ic{font-size:19px;width:38px;height:38px;flex:none;display:grid;place-items:center;
  background:var(--accent-subtle);border-radius:11px;border:1.5px solid var(--info-bd)}
.tr-main{display:flex;flex-direction:column;min-width:0}
.tr-main b{font-size:14.5px;color:var(--tp);font-weight:700}
.tr-main span{font-size:12.5px;color:var(--tm)}
.tr-cnt{margin-left:auto;font-size:15px;font-weight:800;color:var(--tm);background:var(--bg-surface-2);
  border:1.5px solid var(--border-2);min-width:34px;height:34px;border-radius:10px;display:grid;place-items:center;
  font-variant-numeric:tabular-nums;transition:.15s var(--ease)}
.tr-cnt.on{color:#fff;background:var(--accent);border-color:var(--accent);box-shadow:0 2px 8px rgba(91,42,134,.3)}

/* ============================================================ PLAYBOOKS ============================================================ */
.grid{display:grid;gap:20px;grid-template-columns:repeat(auto-fill,minmax(312px,1fr))}
.pb{position:relative;background:var(--bg-surface);border:1.5px solid var(--border-2);border-radius:var(--r-lg);
  box-shadow:var(--sm);padding:22px;overflow:hidden;
  transition:box-shadow var(--dur) var(--ease),transform var(--dur) var(--ease),border-color var(--dur) var(--ease)}
.pb::before{content:"";position:absolute;inset:0 0 auto 0;height:5px;background:var(--sun)}
.pb.pb-q::before{background:linear-gradient(90deg,#1FAE54,#0E7C39)}
.pb-tag{display:inline-flex;align-items:center;gap:7px;padding:5px 12px;border-radius:var(--r-pill);
  font-size:12px;font-weight:700;line-height:1;margin-top:4px}
.pb-inc{color:var(--danger-fg);background:var(--danger-subtle);border:1.5px solid var(--danger-bd)}
.pb-qja{color:var(--success-fg);background:var(--success-subtle);border:1.5px solid var(--success-bd)}
.pb-sol{margin:16px 0 0;font-size:11px;font-weight:800;letter-spacing:.07em;text-transform:uppercase;color:var(--td)}
.pb-code{display:inline-flex;align-items:center;font-family:var(--mono);font-size:13px;font-weight:700;
  color:var(--accent-text);background:var(--accent-subtle);border:1.5px solid var(--info-bd);
  padding:4px 12px;border-radius:var(--r-xs);letter-spacing:.09em;flex:none}
.pb.pb-q .pb-code{color:#0E7C39;background:#E3F7EB;border-color:#9FE2BA}
.search{width:100%;max-width:580px;margin-bottom:22px;background:var(--bg-surface);border:1.5px solid var(--border-2);
  border-radius:var(--r-pill);padding:13px 20px;font-size:15px;color:var(--tp);font-family:inherit;box-shadow:var(--xs);
  transition:border-color .15s var(--ease),box-shadow .15s var(--ease)}
.search::placeholder{color:var(--td)}
.search:focus{outline:none;border-color:var(--accent);box-shadow:0 0 0 3px var(--accent-ring)}
.pb:hover{box-shadow:var(--md);border-color:var(--border-strong)}
/* ---- acordeon: solo titulo+codigo visible; al tocar se despliega ---- */
.pb-head{cursor:pointer;padding-top:2px}
.pb-top{display:flex;align-items:center;gap:10px;margin-bottom:11px}
.pb h3{font-size:16.5px;font-weight:700;color:var(--tp);letter-spacing:-.015em;margin:0;line-height:1.3}
.pb .v{font-size:11px;font-weight:700;color:#fff;background:var(--accent);flex:none;
  padding:3px 9px;border-radius:var(--r-pill);letter-spacing:.02em;box-shadow:0 2px 6px rgba(91,42,134,.3)}
.pb-ver{margin-left:auto;display:inline-flex;align-items:center;gap:6px;font-size:12.5px;font-weight:600;
  color:var(--accent-text);white-space:nowrap}
.pb.pb-q .pb-ver{color:#0E7C39}
.pb-chev{flex:none;font-size:12px;transition:transform .25s var(--ease)}
.pb.open .pb-chev{transform:rotate(180deg)}
.pb-body{display:none}
.pb.open .pb-body{display:block;animation:fade .3s var(--ease)}
.pb-obj{margin-top:14px;font-size:13.5px;color:var(--ts);line-height:1.5;background:var(--bg-surface-2);
  border:1.5px solid var(--border-2);border-left:4px solid var(--accent);border-radius:var(--r-sm);padding:11px 14px}
.pb ol{margin:14px 0 0;padding:0;list-style:none;counter-reset:step;display:flex;flex-direction:column;gap:11px}
.pb li{position:relative;padding-left:34px;font-size:14.5px;color:var(--ts);line-height:1.5;counter-increment:step}
.pb li::before{content:counter(step);position:absolute;left:0;top:-1px;width:24px;height:24px;border-radius:50%;
  background:var(--accent-subtle);color:var(--accent-text);font-size:12px;font-weight:800;
  display:grid;place-items:center;font-variant-numeric:tabular-nums;border:1.5px solid var(--info-bd)}
.pb .orig{margin-top:18px;font-size:12px;color:var(--td);border-top:1.5px solid var(--hairline);padding-top:13px;font-weight:500}
/* ---- catalogar + solucion del playbook desplegada dentro del incidente ---- */
.catsel{width:100%;background:var(--bg-surface);border:1.5px solid var(--border-2);border-radius:var(--r-sm);
  padding:11px 13px;font-size:14px;color:var(--tp);font-family:inherit;font-weight:600;cursor:pointer}
.catsel:focus{outline:none;border-color:var(--accent);box-shadow:0 0 0 3px var(--accent-ring)}
.pbinc{border:1.5px solid var(--info-bd);border-radius:var(--r-md);overflow:hidden;background:#FBF8FE}
.pbinc-h{padding:13px 15px;font-size:14px;color:var(--accent-text);font-weight:600;cursor:pointer;
  display:flex;align-items:center;gap:9px}
.pbinc-h b{color:#fff;background:var(--accent);font-family:var(--mono);font-size:12px;font-weight:800;
  padding:2px 8px;border-radius:var(--r-xs)}
.pbinc-h .chev{margin-left:auto;font-size:12px;color:var(--accent-text);transition:transform .25s var(--ease)}
.pbinc.open .pbinc-h .chev{transform:rotate(180deg)}
.pbinc-b{display:none;padding:4px 16px 15px;border-top:1.5px solid var(--hairline)}
.pbinc.open .pbinc-b{display:block;animation:fade .3s var(--ease)}
.pbinc-b ol{margin:13px 0;padding:0 0 0 4px;list-style:none;counter-reset:s;display:flex;flex-direction:column;gap:8px}
.pbinc-b li{position:relative;padding-left:30px;font-size:13.5px;color:var(--ts);line-height:1.5;counter-increment:s}
.pbinc-b li::before{content:counter(s);position:absolute;left:0;top:-1px;width:21px;height:21px;border-radius:50%;
  background:var(--accent-subtle);color:var(--accent-text);font-size:11px;font-weight:800;display:grid;place-items:center;
  border:1.5px solid var(--info-bd)}

/* ============================================================ INFORMES ASFI ============================================================ */
.inf{background:var(--bg-surface);border:1.5px solid var(--border-2);border-radius:var(--r-lg);
  box-shadow:var(--sm);overflow:hidden;transition:box-shadow var(--dur) var(--ease)}
.inf:hover{box-shadow:var(--md)}
.inf-h{display:flex;align-items:center;gap:13px;padding:17px 22px;border-bottom:1.5px solid var(--hairline);
  background:linear-gradient(180deg,#FBF9FE,#FFFFFF)}
.inf-h b{font-size:15.5px;color:var(--tp);font-weight:700;letter-spacing:-.01em}
.inf-b{padding:20px 22px}
.inf-b textarea{min-height:340px;font-family:var(--mono);font-size:13px;line-height:1.65;
  background:var(--bg-canvas);border-color:var(--border-2)}
.inf-f{padding:15px 22px;border-top:1.5px solid var(--hairline);display:flex;align-items:center;gap:14px;
  flex-wrap:wrap;background:var(--bg-surface)}

/* ============================================================ EMPTY STATES ============================================================ */
.empty{text-align:center;color:var(--tm);padding:80px 24px;animation:fade .4s var(--ease) both}
.empty .ic{font-size:46px;margin-bottom:16px;opacity:.5}
.empty p{font-size:16px;font-weight:700;color:var(--ts)}
.empty small{font-size:13.5px;color:var(--td);display:block;margin-top:5px}
.hidden{display:none!important}

@media (prefers-reduced-motion:reduce){*{animation-duration:.001ms!important;animation-iteration-count:1!important;
  transition-duration:.001ms!important}}
@media(max-width:1000px){.kpis{grid-template-columns:repeat(2,1fr)}}
@media(max-width:900px){.app{grid-template-columns:1fr}.sidebar{display:none}
  .content{padding:22px 18px 44px}.topbar{padding:12px 20px}}
</style>
</head>
<body>
<div class="app">
  <aside class="sidebar">
    <div class="brand"><div class="logo"><svg viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg" aria-label="BancoSol"><defs><linearGradient id="solg" x1="8%" y1="8%" x2="92%" y2="96%"><stop offset="0" stop-color="#FAC400"/><stop offset=".18" stop-color="#F89A00"/><stop offset=".36" stop-color="#F5731A"/><stop offset=".52" stop-color="#F14A2B"/><stop offset=".66" stop-color="#EC2B57"/><stop offset=".8" stop-color="#DD1C82"/><stop offset=".9" stop-color="#A92A99"/><stop offset="1" stop-color="#6E2E96"/></linearGradient></defs><path fill="url(#solg)" d="M50 5C74 5 93 22 95 45C97 66 85 80 66 80C56 80 51 73 45 79C41 83 41 92 35 94C28 96 21 91 16 84C7 72 5 58 6 45C9 22 27 5 50 5Z"/></svg></div><div><b>SolUno</b><small>BancoSol · Operaciones</small></div></div>
    <div class="navgrp">Atención</div>
    <button class="nav active" data-go="all"><span class="ic">📥</span> Bandeja <span class="cnt" id="c-all">0</span></button>
    <button class="nav" data-go="INCIDENTE"><span class="ic">🚨</span> Incidentes <span class="cnt" id="c-inc">0</span></button>
    <button class="nav" data-go="QUEJA"><span class="ic">📣</span> Quejas <span class="cnt" id="c-qja">0</span></button>
    <button class="nav" data-go="CONSULTA"><span class="ic">💬</span> Consultas <span class="cnt" id="c-con">0</span></button>
    <div class="navgrp">Conocimiento</div>
    <button class="nav" data-go="playbooks"><span class="ic">📘</span> Playbooks <span class="cnt" id="c-pb">0</span></button>
    <div class="navgrp">Documentos</div>
    <button class="nav" data-go="informes"><span class="ic">📄</span> Informes ASFI <span class="cnt" id="c-inf">0</span></button>
    <div class="sb-foot"><span class="dot-live"></span>Conectado a WhatsApp · IA en vivo</div>
  </aside>

  <main class="main">
    <header class="topbar">
      <div><h1 id="title">Bandeja de atención</h1><p id="subtitle">Conversaciones de clientes por WhatsApp, clasificadas y resueltas por la IA en tiempo real.</p></div>
      <span class="live-pill"><span class="dot-live"></span><span id="livecnt">0</span> en conversación</span>
    </header>
    <div class="content">
      <!-- BANDEJA -->
      <section id="v-bandeja">
        <div class="kpis" style="margin-bottom:26px">
          <div class="kpi"><div class="lab">💬 Conversaciones</div><div class="val" id="k-total">0</div><div class="sub">charlas completas atendidas</div></div>
          <div class="kpi dang"><div class="lab">🚨 Incidentes</div><div class="val" id="k-inc">0</div><div class="sub" id="k-inc-sub">derivados al equipo</div></div>
          <div class="kpi warn"><div class="lab">📣 Quejas</div><div class="val" id="k-qja">0</div><div class="sub">resueltas por la IA</div></div>
          <div class="kpi acc"><div class="lab">💬 Consultas</div><div class="val" id="k-con">0</div><div class="sub">informativas</div></div>
          <div class="kpi"><div class="lab">👥 Clientes afectados</div><div class="val" id="k-afe">0</div><div class="sub">por incidentes activos</div></div>
        </div>
        <div class="seg" id="seg" style="margin-bottom:20px">
          <button class="active" data-f="all">Todas</button>
          <button data-f="INCIDENTE">🚨 Incidentes</button>
          <button data-f="QUEJA">📣 Quejas</button>
          <button data-f="CONSULTA">💬 Consultas</button>
        </div>
        <div class="feed" id="feed"></div>
      </section>
      <!-- PLAYBOOKS -->
      <section id="v-playbooks" class="hidden"><input id="pbsearch" class="search" type="text" placeholder="🔎 Buscar un problema o solución (ej. QR, login, transferencia, PRB-001)…" oninput="pbBuscar(this.value)"><div class="grid" id="pbgrid"></div></section>
      <!-- INFORMES -->
      <section id="v-informes" class="hidden"><div class="feed" id="infList"></div></section>
    </div>
  </main>
</div>

<script>
/* Bypass del aviso de ngrok: que la API devuelva JSON en cualquier PC (no la pagina de advertencia) */
const _origFetch=window.fetch.bind(window);
window.fetch=(u,o)=>{o=o||{};o.headers=Object.assign({"ngrok-skip-browser-warning":"true"},o.headers||{});return _origFetch(u,o);};
let convs=[], metrics={}, playbooks=[], informes=[], filtro="all", vista="bandeja", pausa=false;
let lastFeed="", lastPb="", lastInf="";  // firmas: solo re-render si cambió (evita parpadeo)
const $=id=>document.getElementById(id);
const esc=s=>String(s==null?"":s).replace(/[&<>"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]));
const AVCOL=["#5B2A86","#C81E76","#D9650F","#3346B8","#0E7C39","#7A2E96","#B11468","#4A2270"];
function avColor(s){let h=0;for(const c of (s||""))h=(h*31+c.charCodeAt(0))>>>0;return AVCOL[h%AVCOL.length];}
function ini(n){const p=(n||"?").trim().split(/\s+/);return ((p[0]||"?")[0]+(p[1]?p[1][0]:"")).toUpperCase();}
function hora(ts){try{const d=new Date(ts);return d.toLocaleTimeString("es-BO",{hour:"2-digit",minute:"2-digit"});}catch(e){return"";}}
/* severidad -> Leve / Medio / Urgente / Crítico (este aparte, resaltado) */
function sevInfo(sev){
  const s=(sev||"").toLowerCase();
  if(/crit|grav/.test(s)) return {l:"⚠ Crítico", c:"sev-crit", k:"crit"};
  if(/alta|urg/.test(s)) return {l:"Urgente", c:"sev-urg", k:"urg"};
  if(/med/.test(s)) return {l:"Medio", c:"sev-med", k:"med"};
  return {l:"Leve", c:"sev-leve", k:"leve"};
}
/* directorio de equipos de TI (a donde se derivan los incidentes) */
const EQUIPOS=[
  {n:"Equipo Pagos/QR",        d:"Pagos, QR, transferencias y ACH", ic:"💳"},
  {n:"Equipo Seguridad/Accesos", d:"Login, accesos y tokens",       ic:"🔐"},
  {n:"DevOps Aplicaciones",    d:"AppSol, Solnet y despliegues",    ic:"📱"},
  {n:"Infraestructura/TI",     d:"Servidores, red y rendimiento",   ic:"🖥️"},
  {n:"Mesa de Incidentes TI",  d:"Coordinación y casos generales",  ic:"🎛️"}
];
function teamTreeHtml(){
  const act=convs.filter(c=>c.tipo==="INCIDENTE" && c.estado_incidente!=="Resuelto");
  const rows=EQUIPOS.map(e=>{
    const n=act.filter(c=>(c.equipo_asignado||"Mesa de Incidentes TI")===e.n).length;
    return '<div class="tr-row"><span class="tr-ic">'+e.ic+'</span><div class="tr-main"><b>'+esc(e.n)+
      '</b><span>'+esc(e.d)+'</span></div><span class="tr-cnt'+(n?' on':'')+'">'+n+'</span></div>';
  }).join("");
  return '<div class="tree"><div class="tree-h">🌐 Centro de derivación TI'+
    '<small>Cada incidente se enruta automáticamente al equipo responsable. Activos por equipo:</small></div>'+rows+'</div>';
}

async function poll(){
  if(pausa) return;
  try{
    const [rc,rm]=await Promise.all([fetch("/api/conversaciones"),fetch("/api/metrics")]);
    if(rc.ok) convs=await rc.json();
    if(rm.ok) metrics=await rm.json();
  }catch(e){}
  if(!playbooks.length){ try{const rp=await fetch("/api/playbooks"); if(rp.ok){playbooks=await rp.json();$("c-pb").textContent=playbooks.length;}}catch(e){} }
  renderKPIs(); if(vista==="bandeja") render();
}
function renderKPIs(){
  const inc=convs.filter(c=>c.tipo==="INCIDENTE").length, qja=convs.filter(c=>c.tipo==="QUEJA").length, con=convs.filter(c=>c.tipo==="CONSULTA").length;
  $("k-total").textContent=metrics.total||convs.length;
  $("k-inc").textContent=metrics.incidentes||inc;
  $("k-qja").textContent=metrics.quejas||qja;
  $("k-con").textContent=metrics.consultas||con;
  $("k-afe").textContent=metrics.afectados||0;
  const resuelt=metrics.resueltos||0; $("k-inc-sub").textContent=(metrics.incidentes||inc)?((resuelt)+" resueltos · derivados al equipo"):"derivados al equipo";
  $("c-all").textContent=convs.length; $("c-inc").textContent=metrics.incidentes||inc;
  $("c-qja").textContent=metrics.quejas||qja; $("c-con").textContent=metrics.consultas||con;
  $("livecnt").textContent=metrics.abiertas||convs.filter(c=>c.abierta).length;
}
function badgeTipo(t){return t==="INCIDENTE"?'<span class="badge b-inc">Incidente</span>':t==="QUEJA"?'<span class="badge b-qja">Queja</span>':'<span class="badge b-con">Consulta</span>';}
function badgeEstado(c){
  if(c.tipo==="INCIDENTE"){return c.estado_incidente==="Resuelto"?'<span class="badge b-ok">Resuelto</span>':'<span class="badge b-gray">En proceso</span>';}
  return c.abierta?'<span class="badge b-live">En conversación</span>':'<span class="badge b-ok">Cerrada</span>';
}
function thread(ms){
  return (ms||[]).map(m=>{
    const out=m.de==="agente";
    return '<div class="msg '+(out?"out":"in")+'">'+esc(m.texto)+'<span class="mt">'+hora(m.ts)+'</span></div>';
  }).join("");
}
function convCard(c){
  const cid=c.caso_id||c.id;
  const isInc=c.tipo==="INCIDENTE";
  const sev=isInc?sevInfo(c.severidad):null;
  const crit=sev&&sev.k==="crit"&&c.estado_incidente!=="Resuelto";
  let h='<div class="conv'+(crit?" conv-crit":"")+'"><div class="conv-h">';
  h+='<div class="av" style="background:'+avColor(c.from)+'">'+ini(c.nombre)+'</div>';
  h+='<div class="who"><b>'+esc(c.nombre||"Cliente")+'</b><span>'+esc(c.from)+'</span></div>';
  h+='<div class="tags">';
  if(c.caso_id) h+='<span class="code">'+esc(c.caso_id)+'</span>';
  h+=badgeTipo(c.tipo);
  if(sev) h+='<span class="sev '+sev.c+'">'+sev.l+'</span>';
  h+=badgeEstado(c)+'</div></div>';
  h+='<div class="chat">'+thread(c.mensajes)+'</div>';
  // panel para incidentes
  if(isInc){
    if(c.estado_incidente==="Resuelto"){
      h+='<div class="panel"><div class="resolved">✅ <b>Incidente resuelto.</b> Se avisó al cliente por WhatsApp y se generó el informe ASFI.<a href="#" onclick="irA(\'informes\');return false">Ver informe →</a></div></div>';
    }else{
      h+='<div class="panel">';
      h+='<div class="route">➡️ Derivado a <b>'+esc(c.equipo_asignado||"Mesa de Incidentes TI")+'</b> · Severidad <b>'+sev.l.replace("⚠ ","")+'</b></div>';
      h+='<div class="meta-row">';
      if(c.categoria) h+='<span class="chip">Categoría: <b>'+esc(c.categoria)+'</b></span>';
      h+='<span class="chip">Afectados: <b>'+((c.afectados)||1)+'</b></span>';
      h+='</div>';
      h+='<div class="field"><label>📘 Catalogado como problema'+(c.playbook_nuevo?' <span class="nuevo">＋ añadido al catálogo</span>':'')+'</label>'+
         '<select class="catsel" id="cat-'+cid+'" onchange="recatalogar(\''+cid+'\')">'+playbookOptions(c.playbook_codigo)+'</select></div>';
      h+='<div id="pbsol-'+cid+'">'+pbSolHtml(c.playbook_codigo,cid)+'</div>';
      if(c.recomendacion_tecnica) h+='<div class="rec"><div class="h">💡 Recomendación rápida de la IA</div><p>'+esc(c.recomendacion_tecnica)+'</p></div>';
      h+='<div class="field"><label>Solución a aplicar (del playbook o personalizada)</label>'+
         '<textarea id="sol-'+cid+'" placeholder="Usá la solución del playbook con el botón de arriba, o escribí una personalizada...">'+esc(c.recomendacion_tecnica||"")+'</textarea>'+
         '<p class="hint" style="margin-top:6px">Al resolver: se avisa al cliente por WhatsApp, se actualiza el playbook (casos futuros = queja) y se genera el informe ASFI.</p></div>';
      h+='<div><button class="btn btn-ok" onclick="resolverCaso(\''+c.caso_id+'\')">✓ Resolver y avisar al cliente</button></div>';
      h+='</div>';
    }
  }
  h+='</div>';
  return h;
}
function render(){
  const list=convs.filter(c=>filtro==="all"||c.tipo===filtro);
  const f=$("feed");
  let html=list.length?list.map(convCard).join(""):'<div class="empty"><div class="ic">📭</div><p>Sin conversaciones todavía</p><small>Escribí al WhatsApp de BancoSol para ver la magia en vivo.</small></div>';
  if(filtro==="INCIDENTE") html=teamTreeHtml()+html;   // arbol de equipos arriba en la vista Incidentes
  if(html===lastFeed) return;   // sin cambios -> no tocar el DOM -> sin parpadeo ni re-animacion
  lastFeed=html; f.innerHTML=html;
}
async function resolverCaso(cid){
  const ta=$("sol-"+cid); const sol=ta?ta.value:"";
  const btn=event.target; btn.disabled=true; btn.textContent="Resolviendo...";
  try{
    const r=await fetch("/api/resolver",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({caso_id:cid,solucion:sol})});
    const d=await r.json();
    pausa=false; await poll(); pollInformes(); pollPlaybooks();
  }catch(e){btn.disabled=false;btn.textContent="✓ Resolver y avisar al cliente";}
}
/* playbooks */
async function pollPlaybooks(){try{const r=await fetch("/api/playbooks");if(r.ok){playbooks=await r.json();$("c-pb").textContent=playbooks.length;if(vista==="playbooks")renderPlaybooks();}}catch(e){}}
let pbquery="", pbOpen={};
function pbBuscar(v){pbquery=(v||"").toLowerCase().trim();lastPb="";renderPlaybooks();}
function togglePb(cod){pbOpen[cod]=!pbOpen[cod];lastPb="";renderPlaybooks();}
function renderPlaybooks(){
  let lista=playbooks.slice().sort((a,b)=>String(a.codigo||"").localeCompare(String(b.codigo||"")));
  if(pbquery) lista=lista.filter(p=>((p.codigo||"")+" "+(p.titulo||p.categoria||"")+" "+(p.equipo||"")+" "+(p.categoria||"")+" "+(p.pasos||[]).join(" ")).toLowerCase().includes(pbquery));
  const html=lista.length?lista.map(p=>{
    const inc=p.tipo==="incidente";
    const open=pbquery?true:!!pbOpen[p.codigo];
    const tag=inc?'<span class="pb-tag pb-inc">🚨 Incidente · deriva a '+esc(p.equipo||"equipo TI")+'</span>'
                 :'<span class="pb-tag pb-qja">🤖 Queja · la resuelve la IA</span>';
    const pasos=(p.pasos||[]);
    return '<div class="pb '+(inc?"pb-i":"pb-q")+(open?" open":"")+'">'+
      '<div class="pb-head" onclick="togglePb(\''+esc(p.codigo)+'\')">'+
        '<div class="pb-top"><span class="pb-code">'+esc(p.codigo||"PRB-?")+'</span>'+
          '<span class="v">v'+p.version+'</span>'+
          '<span class="pb-ver">'+(open?"Ocultar":"Ver procedimiento")+' <span class="pb-chev">▾</span></span></div>'+
        '<h3>'+esc(p.titulo||p.categoria)+'</h3>'+
      '</div>'+
      '<div class="pb-body">'+tag+
        (p.objetivo?'<div class="pb-obj">🎯 '+esc(p.objetivo)+'</div>':'')+
        '<div class="pb-sol">Solución paso a paso</div>'+
        (pasos.length?'<ol>'+pasos.map(s=>'<li>'+esc(s)+'</li>').join("")+'</ol>'
          :'<div class="orig" style="color:var(--warning-fg);font-weight:600">⚠ Sin solución todavía — se completará cuando el equipo resuelva un incidente de este tipo.</div>')+
        '<div class="orig">'+(p.actualizado?"🔄 Solución actualizada por la IA · "+esc(p.origen):"Solución base · se actualiza sola al resolver un incidente")+'</div>'+
      '</div></div>';
  }).join(""):'<div class="empty"><div class="ic">🔎</div><p>Sin resultados para “'+esc(pbquery)+'”</p><small>Probá con otra palabra: QR, login, transferencia, lentitud, datos…</small></div>';
  if(html===lastPb) return; lastPb=html; $("pbgrid").innerHTML=html;
}
/* helpers de solucion de playbook dentro del incidente */
function playbookPorCodigo(cod){return (playbooks||[]).find(p=>p.codigo===cod);}
function playbookOptions(sel){return (playbooks||[]).slice().sort((a,b)=>String(a.codigo||"").localeCompare(String(b.codigo||""))).map(p=>'<option value="'+esc(p.codigo)+'"'+(p.codigo===sel?" selected":"")+'>'+esc(p.codigo)+' · '+esc(p.titulo||p.categoria)+'</option>').join("");}
function pbSolHtml(cod,cid){
  const p=playbookPorCodigo(cod);
  if(!p) return '<div class="hint">Cargando catálogo de soluciones…</div>';
  const pasos=p.pasos||[];
  if(!pasos.length) return '<div class="pbinc"><div class="pbinc-h">📘 <b>'+esc(p.codigo)+'</b> '+esc(p.titulo)+' — <span style="color:var(--warning-fg);font-weight:700;margin-left:4px">sin solución aún</span></div></div>';
  return '<div class="pbinc"><div class="pbinc-h" onclick="this.parentNode.classList.toggle(\'open\')">📘 Solución del playbook <b>'+esc(p.codigo)+'</b> · '+esc(p.titulo)+' <span class="chev">▾</span></div>'+
    '<div class="pbinc-b"><ol>'+pasos.map(s=>'<li>'+esc(s)+'</li>').join("")+'</ol>'+
    '<button class="btn btn-s" type="button" onclick="usarSolucion(\''+cid+'\')">⬇ Usar esta solución como aplicada</button></div></div>';
}
function recatalogar(cid){const sel=document.getElementById("cat-"+cid),cont=document.getElementById("pbsol-"+cid);if(sel&&cont)cont.innerHTML=pbSolHtml(sel.value,cid);}
function usarSolucion(cid){const sel=document.getElementById("cat-"+cid),ta=document.getElementById("sol-"+cid);const p=sel?playbookPorCodigo(sel.value):null;if(p&&ta){ta.value=(p.pasos||[]).join("\n");ta.focus();}}
/* informes */
async function pollInformes(){try{const r=await fetch("/api/informes");if(r.ok){informes=await r.json();$("c-inf").textContent=informes.length;if(vista==="informes")renderInformes();}}catch(e){}}
function renderInformes(){
  const l=$("infList");
  const html=!informes.length?'<div class="empty"><div class="ic">📄</div><p>Sin informes ASFI todavía</p><small>Se generan automáticamente cuando resolvés un incidente.</small></div>':informes.map(inf=>'<div class="inf"><div class="inf-h">📄 <b>ASFI · '+esc(inf.caso_id)+'</b>'+
    (inf.aprobado?'<span class="badge b-ok" style="margin-left:auto">Aprobado · archivado</span>':'<span class="badge b-qja" style="margin-left:auto">Borrador editable</span>')+'</div>'+
    '<div class="inf-b"><textarea id="inf-'+inf.caso_id+'">'+esc(inf.texto)+'</textarea></div>'+
    '<div class="inf-f">'+(inf.aprobado?'<span class="hint">✅ Guardado en <b>'+esc(inf.archivo||"informes/")+'</b> · listo para enviar a ASFI</span>':
      '<button class="btn btn-p" onclick="aprobarInforme(\''+inf.caso_id+'\')">✓ Aprobar y archivar</button><span class="hint">Editá el texto antes de aprobar. Al aprobar se guarda en la carpeta de informes.</span>')+'</div></div>').join("");
  if(html===lastInf) return; lastInf=html; l.innerHTML=html;
}
async function aprobarInforme(cid){
  const ta=$("inf-"+cid); const texto=ta?ta.value:"";
  const btn=event.target; btn.disabled=true; btn.textContent="Archivando...";
  try{await fetch("/api/informe/aprobar",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({caso_id:cid,texto})});pausa=false;await pollInformes();}
  catch(e){btn.disabled=false;btn.textContent="✓ Aprobar y archivar";}
}
/* navegacion */
function mostrarVista(v){$("v-bandeja").classList.toggle("hidden",v!=="bandeja");$("v-playbooks").classList.toggle("hidden",v!=="playbooks");$("v-informes").classList.toggle("hidden",v!=="informes");}
function setT(t,s){$("title").textContent=t;$("subtitle").textContent=s;}
function irA(go){document.querySelector('.nav[data-go="'+go+'"]').click();}
document.querySelectorAll(".nav").forEach(b=>b.addEventListener("click",()=>{
  const go=b.dataset.go;
  document.querySelectorAll(".nav").forEach(x=>x.classList.remove("active"));b.classList.add("active");
  if(go==="playbooks"){vista="playbooks";mostrarVista("playbooks");setT("Playbooks · Manual vivo","Las soluciones que usa la IA. Se actualizan solas cuando el equipo resuelve un incidente.");pollPlaybooks();}
  else if(go==="informes"){vista="informes";mostrarVista("informes");setT("Informes ASFI","Documentos generados al resolver cada incidente. Editalos y aprobalos para archivar y enviar a ASFI.");pollInformes();}
  else{vista="bandeja";mostrarVista("bandeja");
    filtro=go;document.querySelectorAll("#seg button").forEach(x=>x.classList.toggle("active",x.dataset.f===go));
    const titulos={all:["Bandeja de atención","Conversaciones de clientes por WhatsApp, clasificadas y resueltas por la IA en tiempo real."],
      INCIDENTE:["Incidentes","Casos derivados al equipo técnico. Cada uno con la solución recomendada por la IA."],
      QUEJA:["Quejas","Charlas completas que la IA resolvió sola. Una queja = una conversación."],
      CONSULTA:["Consultas","Conversaciones informativas atendidas por la IA."]};
    setT(titulos[go][0],titulos[go][1]);render();}
}));
document.querySelectorAll("#seg button").forEach(b=>b.addEventListener("click",()=>{
  filtro=b.dataset.f;document.querySelectorAll("#seg button").forEach(x=>x.classList.remove("active"));b.classList.add("active");
  document.querySelectorAll(".nav").forEach(x=>x.classList.toggle("active",x.dataset.go===filtro));render();
}));
/* no refrescar mientras se edita un textarea */
document.addEventListener("focusin",e=>{if(e.target.tagName==="TEXTAREA")pausa=true;});
document.addEventListener("focusout",e=>{if(e.target.tagName==="TEXTAREA")setTimeout(()=>pausa=false,400);});
/* init */
poll();pollPlaybooks();pollInformes();
setInterval(poll,3000);setInterval(()=>{if(vista==="playbooks")pollPlaybooks();if(vista==="informes")pollInformes();},4000);
</script>
</body>
</html>

==========================================================================================
ARCHIVO: app/playbooks_detallados.json
==========================================================================================
{
  "fallo_pagos_qr": {
    "titulo": "Fallo en pagos / QR",
    "objetivo": "Restaurar la generación, lectura y liquidación de pagos QR (AltokE) en AppSol, minimizando el impacto en comercios y usuarios.",
    "pasos": [
      "1. Confirmá la alerta: revisá el dashboard de monitoreo del servicio QR/AltokE y verificá si la tasa de errores de transacciones QR supera el umbral del 2% en los últimos 5 minutos.",
      "2. Clasificá la severidad: marcá S1 si el QR está caído a nivel nacional o afecta liquidación de fondos, S2 si es degradación parcial o regional, S3 si es intermitente sin pérdida de fondos.",
      "3. Abrí el incidente en la herramienta de gestión (ticket) con código, hora de inicio (hora Bolivia UTC-4), severidad y servicio afectado, y asigná al Equipo Pagos/QR como responsable.",
      "4. Verificá el estado de salud (health check) de los microservicios de QR: servicio de generación de QR, servicio de validación y servicio de liquidación, e identificá cuál responde con error o timeout.",
      "5. Revisá los logs de los últimos 30 minutos del servicio afectado y filtrá por códigos de error, excepciones y timeouts para ubicar el patrón de falla.",
      "6. Comprobá la conectividad con el switch transaccional y con la red de QR interoperable (ATC/BCB), validando latencia y disponibilidad del endpoint.",
      "7. Verificá el estado de la base de datos transaccional: conexiones activas, bloqueos (locks), uso de CPU y espacio en disco, descartando saturación.",
      "8. Correlacioná el inicio de la falla con cambios recientes (despliegues, parches, cambios de configuración) ocurridos en las últimas 24 horas para aislar la causa raíz.",
      "9. Si la causa es un despliegue reciente, ejecutá el rollback a la última versión estable conforme al procedimiento de despliegue y registrá la versión restaurada.",
      "10. Si la causa es saturación o un nodo caído, aplicá failover al nodo o réplica saludable y, de ser necesario, escalá recursos (instancias/conexiones).",
      "11. Si la falla proviene del proveedor externo (red interoperable o switch), activá el modo degradado: deshabilitá temporalmente QR interoperable manteniendo QR intrabancario operativo.",
      "12. Validá el servicio ejecutando una transacción QR de prueba (cobro y pago) en ambiente productivo controlado y confirmá liquidación correcta.",
      "13. Monitoreá durante 15 minutos que la tasa de error vuelva por debajo del 1% y que la latencia se normalice antes de declarar mitigado.",
      "14. Comunicá a usuarios y comercios el estado mediante el canal oficial (banner en AppSol, redes y call center) con mensaje claro y tiempo estimado de solución.",
      "15. Notificá a stakeholders internos (Gerencia de Tecnología, Riesgo Operativo y, si aplica por indisponibilidad relevante, al área de cumplimiento ante ASFI) según matriz de escalamiento.",
      "16. Escalá al líder de Pagos/QR y al proveedor del switch si tras 30 minutos en S1 no hay mitigación, activando el puente de comunicación (war room).",
      "17. Confirmá la resolución: verificá que las transacciones encoladas o reversadas se hayan procesado o devuelto correctamente y que no existan fondos en estado inconsistente.",
      "18. Cerrá el incidente registrando línea de tiempo, causa raíz, acciones y duración, y agendá el post-mortem dentro de las 48 horas para S1/S2."
    ]
  },
  "error_transferencia": {
    "titulo": "Error en transferencias",
    "objetivo": "Diagnosticar y resolver fallos en transferencias entre cuentas propias y a terceros dentro de BancoSol, asegurando la integridad de los fondos.",
    "pasos": [
      "1. Confirmá la alerta verificando en el dashboard la tasa de transferencias fallidas o rechazadas en los últimos 5 minutos contra el umbral definido (2%).",
      "2. Clasificá la severidad: S1 si están bloqueadas todas las transferencias o hay riesgo de doble débito, S2 si afecta un tipo o segmento, S3 si es intermitente sin afectación de fondos.",
      "3. Registrá el incidente con hora de inicio (UTC-4), severidad, servicio afectado y asigná al Equipo Pagos/QR como responsable.",
      "4. Verificá el health check del servicio de transferencias y del orquestador de pagos, identificando el componente que responde con error.",
      "5. Revisá los logs de los últimos 30 minutos filtrando por código de error, transacciones reversadas y timeouts para detectar el patrón.",
      "6. Comprobá el estado del core bancario y de la cola de mensajería que procesa los movimientos, validando que no exista acumulación (backlog) de mensajes.",
      "7. Verificá la base de datos transaccional: bloqueos, transacciones colgadas, integridad de saldos y consistencia entre débito y crédito.",
      "8. Identificá si el error afecta solo transferencias internas o también a terceros, y aislá si la causa está en validación de cuenta, límites o disponibilidad de saldo.",
      "9. Correlacioná con cambios recientes (despliegues, reglas de validación, parámetros de límites) de las últimas 24 horas para ubicar la causa raíz.",
      "10. Si la causa es un despliegue, ejecutá rollback a la versión estable y verificá que el orquestador retome el procesamiento normal.",
      "11. Si hay backlog en la cola, reiniciá de forma controlada los consumidores (workers) y monitoreá el drenaje de mensajes sin generar duplicados.",
      "12. Detené proactivamente nuevas transferencias del flujo afectado si existe riesgo de doble débito, hasta confirmar la integridad del sistema.",
      "13. Ejecutá una transferencia de prueba (cuenta propia y a tercero) por monto mínimo y confirmá débito, crédito y reflejo correcto del saldo.",
      "14. Reconciliá las transacciones del período afectado: identificá débitos sin crédito o duplicados y generá los reversos o ajustes necesarios.",
      "15. Validá durante 15 minutos que la tasa de error caiga por debajo del 1% antes de declarar el servicio mitigado.",
      "16. Comunicá a los usuarios por banner en AppSol y call center, e informá a Gerencia de Tecnología, Riesgo Operativo y Contabilidad para la conciliación.",
      "17. Escalá al líder de Pagos/QR si en S1 no hay mitigación en 30 minutos, activando el war room con Core Bancario.",
      "18. Cerrá el incidente con línea de tiempo, causa raíz, evidencia de reconciliación y duración, y agendá post-mortem dentro de 48 horas para S1/S2."
    ]
  },
  "fallo_ach": {
    "titulo": "Fallo ACH / interbancario",
    "objetivo": "Restablecer el envío y recepción de transferencias interbancarias (ACH) garantizando el procesamiento y la conciliación con la cámara de compensación.",
    "pasos": [
      "1. Confirmá la alerta revisando el estado de la conexión con la cámara de compensación interbancaria (ACH) y la tasa de operaciones interbancarias fallidas o no confirmadas.",
      "2. Clasificá la severidad: S1 si no se envían ni reciben operaciones ACH o hay riesgo de incumplir ventana de liquidación, S2 si hay demora parcial, S3 si es intermitente.",
      "3. Registrá el incidente con hora de inicio (UTC-4), severidad y servicio afectado, asignando al Equipo Pagos/QR como responsable.",
      "4. Verificá el health check del servicio ACH y la conectividad (VPN/enlace dedicado) con la cámara interbancaria y el BCB, midiendo latencia y disponibilidad.",
      "5. Revisá los logs del servicio ACH de los últimos 30 minutos filtrando por errores de envío, rechazos de la cámara y archivos no procesados.",
      "6. Comprobá el estado de los lotes (batch) de envío y recepción: archivos pendientes, archivos rechazados y confirmaciones (ACK) no recibidas.",
      "7. Validá la ventana horaria de procesamiento ACH vigente para descartar que el fallo se deba a operación fuera de ciclo o cierre de cámara.",
      "8. Identificá si el problema es de origen (BancoSol no envía), de destino (no se reciben confirmaciones) o de la propia cámara, para aislar la causa raíz.",
      "9. Verificá certificados de seguridad, llaves y credenciales del canal ACH, descartando expiración o rechazo de autenticación.",
      "10. Correlacioná con cambios recientes en el servicio ACH, en la red o en el formato de archivos de las últimas 24 horas.",
      "11. Si la causa es un cambio interno, ejecutá rollback al estado estable; si es de conectividad, activá el enlace de contingencia hacia la cámara.",
      "12. Si la cámara está indisponible, encolá de forma segura las operaciones salientes y marcá las entrantes como pendientes, evitando reenvíos duplicados.",
      "13. Coordiná con el operador de la cámara interbancaria y el BCB para confirmar el estado del servicio y la reanudación del ciclo.",
      "14. Una vez restablecido, reenviá los lotes pendientes en orden y confirmá la recepción de los ACK por cada archivo procesado.",
      "15. Reconciliá las operaciones interbancarias del período: validá que cada envío tenga confirmación y cada recepción su acreditación, generando ajustes si corresponde.",
      "16. Comunicá a usuarios afectados los tiempos de acreditación y notificá a Gerencia de Tecnología, Tesorería, Riesgo Operativo y Cumplimiento (reporte a ASFI si aplica).",
      "17. Escalá al líder de Pagos/QR y a Tesorería si en S1 no hay mitigación en 30 minutos o si se compromete la ventana de liquidación.",
      "18. Cerrá el incidente con línea de tiempo, causa raíz, evidencia de conciliación de lotes y duración, y agendá post-mortem dentro de 48 horas para S1/S2."
    ]
  },
  "caida_login": {
    "titulo": "Caída de login / acceso",
    "objetivo": "Restaurar el acceso seguro de los usuarios a AppSol y Solnet, garantizando autenticación correcta sin comprometer la seguridad.",
    "pasos": [
      "1. Confirmá la alerta verificando en el dashboard la tasa de inicios de sesión fallidos y el tiempo de respuesta del servicio de autenticación en los últimos 5 minutos.",
      "2. Clasificá la severidad: S1 si ningún usuario puede ingresar a AppSol/Solnet, S2 si afecta a un segmento o canal, S3 si es intermitente o lento.",
      "3. Registrá el incidente con hora de inicio (UTC-4), severidad y canal afectado, asignando al Equipo Seguridad/Accesos como responsable.",
      "4. Verificá el health check del servicio de autenticación, del proveedor de identidad (IdP) y del servicio de tokens/OTP.",
      "5. Revisá los logs de autenticación de los últimos 30 minutos filtrando por errores de credenciales, expiración de sesión y fallos de token.",
      "6. Comprobá la disponibilidad del directorio de usuarios y de la base de datos de credenciales, validando conexiones y latencia.",
      "7. Verificá el estado del servicio de envío de OTP (SMS/push) y la conectividad con el proveedor de mensajería, descartando fallo en el segundo factor.",
      "8. Validá los certificados TLS y las llaves de firma de tokens, descartando expiración o rotación incorrecta como causa raíz.",
      "9. Distinguí si la falla es de autenticación (credenciales rechazadas) o de autorización/sesión (login válido pero sin acceso a funciones).",
      "10. Correlacioná con cambios recientes en el IdP, políticas de seguridad, parches o despliegues de las últimas 24 horas.",
      "11. Si la causa es un despliegue o cambio de configuración, ejecutá rollback al estado estable y validá la reanudación de logins.",
      "12. Si un nodo de autenticación está saturado o caído, aplicá failover al nodo saludable y escalá instancias si la demanda lo requiere.",
      "13. Verificá que no se trate de un ataque (fuerza bruta, credential stuffing); si se confirma, coordiná con el SOC el bloqueo y reglas de mitigación.",
      "14. Ejecutá una prueba de login en AppSol y Solnet con usuario de prueba, validando autenticación, OTP y acceso a la sesión.",
      "15. Monitoreá durante 15 minutos que la tasa de error de login vuelva por debajo del 1% antes de declarar mitigado.",
      "16. Comunicá a los usuarios por banner y call center, e informá a Gerencia de Tecnología, Seguridad de la Información y Riesgo Operativo.",
      "17. Escalá al líder de Seguridad/Accesos y al SOC si en S1 no hay mitigación en 30 minutos o si se confirma incidente de seguridad.",
      "18. Cerrá el incidente con línea de tiempo, causa raíz, acciones y duración, y agendá post-mortem dentro de 48 horas para S1/S2."
    ]
  },
  "app_no_carga": {
    "titulo": "App no carga / se cae",
    "objetivo": "Restablecer la disponibilidad y estabilidad de AppSol para que los usuarios puedan abrir y operar la aplicación móvil sin caídas.",
    "pasos": [
      "1. Confirmá la alerta revisando el dashboard de disponibilidad de AppSol: tasa de errores HTTP 5xx, crashes reportados y tiempo de carga inicial.",
      "2. Clasificá la severidad: S1 si la app no abre para todos los usuarios, S2 si falla en una versión/plataforma específica, S3 si es intermitente.",
      "3. Registrá el incidente con hora de inicio (UTC-4), severidad, versión de app y plataforma (iOS/Android), asignando a DevOps Aplicaciones.",
      "4. Verificá el health check del backend (API Gateway y microservicios) que consume AppSol al iniciar, identificando endpoints con error.",
      "5. Revisá los logs y la herramienta de reporte de crashes de los últimos 30 minutos para identificar la pantalla o llamada que provoca la caída.",
      "6. Comprobá el estado de la CDN y de los servicios de configuración remota que la app carga al inicio (feature flags, parámetros).",
      "7. Verificá métricas de infraestructura del backend: CPU, memoria, contenedores reiniciándose (restarts) y estado del balanceador de carga.",
      "8. Determiná si el fallo está en el cliente (versión de app, dispositivos específicos) o en el backend (servicios no disponibles).",
      "9. Correlacioná con publicaciones recientes: despliegue de backend, cambio de configuración remota o liberación de nueva versión de app en las últimas 24 horas.",
      "10. Si una versión nueva de app provocó el fallo, activá la versión mínima soportada o desactivá la función problemática vía feature flag.",
      "11. Si la causa es un despliegue de backend, ejecutá rollback a la versión estable y verificá que los endpoints de inicio respondan correctamente.",
      "12. Si hay contenedores en reinicio o saturación, aplicá failover/escalado horizontal y reiniciá de forma controlada los pods afectados.",
      "13. Limpiá o invalidá la caché de la CDN/configuración remota si la app está cargando parámetros corruptos o desactualizados.",
      "14. Validá abriendo AppSol en dispositivos de prueba (iOS y Android) y confirmá carga del home, login y una operación básica.",
      "15. Monitoreá durante 15 minutos que la tasa de crashes y errores 5xx vuelva a la línea base antes de declarar mitigado.",
      "16. Comunicá a usuarios por redes y call center; si requiere actualizar la app, publicá instrucciones claras de actualización en tiendas.",
      "17. Escalá al líder de DevOps Aplicaciones si en S1 no hay mitigación en 30 minutos, e involucrá al equipo de backend correspondiente.",
      "18. Cerrá el incidente con línea de tiempo, causa raíz, acciones y duración, y agendá post-mortem dentro de 48 horas para S1/S2."
    ]
  },
  "lentitud_servicio": {
    "titulo": "Lentitud del servicio",
    "objetivo": "Identificar y eliminar la causa de la degradación de rendimiento en los servicios de BancoSol para restaurar tiempos de respuesta normales.",
    "pasos": [
      "1. Confirmá la alerta revisando latencia (p95/p99) y tiempos de respuesta de los servicios críticos contra la línea base en los últimos 10 minutos.",
      "2. Clasificá la severidad: S1 si la lentitud impide operar transacciones, S2 si degrada la experiencia sin bloquear, S3 si es puntual o de bajo impacto.",
      "3. Registrá el incidente con hora de inicio (UTC-4), severidad y servicios afectados, asignando a Infraestructura/TI como responsable.",
      "4. Identificá el alcance: determiná si la lentitud es generalizada o concentrada en un servicio, módulo o región específica.",
      "5. Verificá métricas de infraestructura: CPU, memoria, I/O de disco y uso de red en los servidores y contenedores de los servicios afectados.",
      "6. Revisá el estado de la base de datos: consultas lentas (slow queries), bloqueos, conexiones saturadas y crecimiento anómalo de tablas.",
      "7. Comprobá las colas de mensajería y los servicios de caché, validando que no exista acumulación de mensajes ni fallos de caché (cache miss elevado).",
      "8. Analizá los logs y trazas (tracing) para ubicar el componente o dependencia que aporta mayor latencia en la cadena de llamadas.",
      "9. Verificá las integraciones externas (switch, ACH, proveedores) midiendo su latencia para descartar que la demora sea de un tercero.",
      "10. Correlacioná con cambios recientes (despliegues, migraciones, jobs programados) y con picos de tráfico de las últimas 24 horas.",
      "11. Si la causa es un job o proceso batch consumiendo recursos, pausalo o reprogramalo fuera del horario pico.",
      "12. Si la causa es saturación, escalá recursos (vertical/horizontal) y aplicá failover para distribuir la carga.",
      "13. Si una consulta o índice degrada la base de datos, aplicá la optimización correspondiente o reiniciá conexiones colgadas de forma controlada.",
      "14. Si un despliegue reciente introdujo la lentitud, ejecutá rollback a la versión estable y revalidá el rendimiento.",
      "15. Validá midiendo nuevamente la latencia p95/p99 y confirmá que retorna a la línea base con transacciones de prueba.",
      "16. Monitoreá durante 20 minutos la estabilidad del rendimiento antes de declarar el incidente mitigado.",
      "17. Comunicá el estado a usuarios si hubo impacto perceptible y notificá a Gerencia de Tecnología y Riesgo Operativo; escalá al líder de Infraestructura/TI si en S1 no hay mitigación en 30 minutos.",
      "18. Cerrá el incidente con línea de tiempo, causa raíz, acciones y duración, y agendá post-mortem dentro de 48 horas para S1/S2."
    ]
  },
  "cambio_dispositivo": {
    "titulo": "Cambio de dispositivo / celular",
    "objetivo": "Guiar al cliente para habilitar AppSol en su nuevo dispositivo de forma segura, validando su identidad y desvinculando el equipo anterior.",
    "pasos": [
      "1. Saludá al cliente de forma cordial e identificá claramente que el motivo es el cambio o vinculación de un nuevo dispositivo.",
      "2. Verificá la identidad del cliente solicitando número de documento de identidad (CI), nombre completo y un dato de validación adicional (fecha de nacimiento o últimos dígitos de cuenta).",
      "3. Confirmá mediante una pregunta de seguridad o el envío de un código OTP al número celular registrado que efectivamente es el titular.",
      "4. Preguntá al cliente qué dispositivo nuevo está utilizando (marca y sistema operativo) y si ya descargó AppSol desde la tienda oficial.",
      "5. Si no la tiene instalada, indicale descargar AppSol únicamente desde Google Play o App Store, evitando enlaces de terceros.",
      "6. Pedile que abra AppSol y seleccione la opción de iniciar sesión o vincular un nuevo dispositivo con su usuario registrado.",
      "7. Indicale ingresar sus credenciales y confirmá que reciba el código OTP de verificación en su número registrado.",
      "8. Si el cliente no recibe el OTP, verificá que el número en el sistema sea correcto y que tenga señal; reenvía el código si es necesario.",
      "9. Por seguridad, confirmá con el cliente la desvinculación del dispositivo anterior para que solo el equipo nuevo quede activo.",
      "10. Guialo para crear o confirmar su PIN/biometría en el nuevo dispositivo, explicando que no debe compartir estos datos con nadie.",
      "11. Pedile que verifique dentro de AppSol que puede ver su saldo y movimientos correctamente como prueba de vinculación exitosa.",
      "12. Solicitale que realice una operación de bajo riesgo (consulta de saldo o un pago QR de prueba) para confirmar el funcionamiento completo.",
      "13. Recordale las recomendaciones de seguridad: no compartir su PIN ni OTP, y que BancoSol nunca le pedirá esos datos por llamada o mensaje.",
      "14. Confirmá con el cliente que el problema quedó resuelto y preguntá si necesita ayuda adicional.",
      "15. Registrá en el caso el detalle de la atención, la verificación de identidad realizada y la desvinculación del dispositivo anterior, y cerrá el ticket."
    ]
  },
  "deuda_no_actualizada": {
    "titulo": "Pagué y aún figura la deuda",
    "objetivo": "Verificar y resolver con el cliente la situación de un pago realizado que aún se refleja como deuda pendiente, regularizando su estado.",
    "pasos": [
      "1. Saludá al cliente y confirmá que el motivo es un pago realizado que todavía figura como deuda pendiente.",
      "2. Verificá la identidad del cliente con número de documento (CI), nombre completo y un dato adicional de validación.",
      "3. Solicitá los datos del pago: fecha, hora aproximada, monto, canal utilizado (AppSol, QR, agencia, Solnet) y a qué obligación corresponde.",
      "4. Pedile el número de comprobante o de transacción del pago para ubicarlo en el sistema.",
      "5. Verificá en el sistema si el débito se realizó efectivamente de la cuenta del cliente y si la transacción figura como exitosa.",
      "6. Confirmá si el pago corresponde a una obligación con BancoSol o a un servicio de terceros, ya que la acreditación puede tener tiempos distintos.",
      "7. Si el pago figura exitoso pero la deuda no se actualizó, verificá si se trata de un retraso normal de procesamiento dentro de la ventana esperada.",
      "8. Si está dentro de la ventana de actualización, explicá al cliente el tiempo estimado en que se reflejará y dejá registro del seguimiento.",
      "9. Si el pago fue exitoso y ya pasó la ventana, escalá internamente al área de conciliación para aplicar el pago a la obligación correspondiente.",
      "10. Si el débito no se concretó o fue reversado, explicá al cliente que el pago no se completó y guialo para realizarlo nuevamente.",
      "11. Si hubo doble débito o cobro duplicado, registrá el caso para la devolución y explicá al cliente el plazo de reverso.",
      "12. Indicale al cliente cómo verificar en AppSol el estado actualizado de la obligación una vez aplicada la regularización.",
      "13. Confirmá con el cliente los próximos pasos y el tiempo estimado de solución según el caso identificado.",
      "14. Verificá, antes de cerrar, que el cliente comprenda la situación y entregá un número de caso para su seguimiento.",
      "15. Registrá en el ticket el diagnóstico, la acción tomada o el escalamiento realizado, y cerrá o dejá en seguimiento según corresponda."
    ]
  },
  "datos_no_actualizados": {
    "titulo": "Saldo o movimiento no actualizado",
    "objetivo": "Ayudar al cliente a visualizar correctamente su saldo o movimientos en AppSol cuando aparecen desactualizados, descartando errores de visualización o procesamiento.",
    "pasos": [
      "1. Saludá al cliente y confirmá que el motivo es un saldo o movimiento que no se refleja correctamente en AppSol.",
      "2. Verificá la identidad del cliente con número de documento (CI), nombre completo y un dato adicional de validación.",
      "3. Solicitá detalles: qué operación esperaba ver (depósito, transferencia, pago), fecha, hora aproximada y monto involucrado.",
      "4. Pedile al cliente que cierre y vuelva a abrir AppSol, y que actualice la pantalla de saldo/movimientos para refrescar la información.",
      "5. Indicale verificar que tenga buena conexión a internet, ya que una señal débil puede impedir la actualización de datos.",
      "6. Pedile que confirme si ya cerró sesión y volvió a ingresar, lo que fuerza la sincronización con el sistema.",
      "7. Verificá en el sistema el saldo y los últimos movimientos reales de la cuenta para compararlos con lo que el cliente observa.",
      "8. Si el sistema muestra el dato correcto pero la app no, identificá un problema de visualización/caché y guiá al cliente a reinstalar o actualizar AppSol.",
      "9. Confirmá si la operación esperada está dentro de la ventana de procesamiento (por ejemplo, acreditaciones interbancarias que demoran).",
      "10. Si está dentro de la ventana, explicá al cliente el tiempo estimado en que el movimiento se reflejará.",
      "11. Si el movimiento debería estar acreditado y no aparece ni en el sistema, escalá internamente para verificación de la transacción.",
      "12. Indicale al cliente cómo consultar el detalle del movimiento una vez actualizado y cómo descargar su comprobante.",
      "13. Confirmá con el cliente que el saldo o movimiento ya se visualiza correctamente tras los pasos aplicados.",
      "14. Si quedó un escalamiento pendiente, entregá un número de caso y el tiempo estimado de respuesta.",
      "15. Registrá en el ticket el diagnóstico, la acción tomada y el resultado, y cerrá o dejá en seguimiento según corresponda."
    ]
  },
  "duda_configuracion": {
    "titulo": "Duda de configuración / uso",
    "objetivo": "Orientar al cliente para configurar y usar correctamente las funciones de AppSol, resolviendo sus dudas de manera autónoma y clara.",
    "pasos": [
      "1. Saludá al cliente y consultá de forma específica qué función o configuración de AppSol necesita resolver.",
      "2. Verificá la identidad del cliente con número de documento (CI) y un dato adicional si la consulta involucra datos de su cuenta.",
      "3. Confirmá qué versión de AppSol y qué tipo de dispositivo utiliza para dar instrucciones precisas.",
      "4. Identificá la categoría de la duda: notificaciones, biometría/PIN, registro de cuentas frecuentes, pagos QR, límites u otra.",
      "5. Explicá paso a paso, en lenguaje sencillo, la ruta dentro de AppSol para llegar a la función consultada (menú y opciones).",
      "6. Si es configuración de biometría o PIN, guialo por la sección de seguridad y recordale no compartir estos datos.",
      "7. Si es registro de cuentas frecuentes o beneficiarios, indicale cómo agregarlos y validarlos de forma segura.",
      "8. Si es configuración de notificaciones, guialo para activarlas tanto en AppSol como en los permisos del sistema del dispositivo.",
      "9. Si la consulta es sobre pagos QR (AltokE), explicá cómo generar un cobro y cómo escanear para pagar.",
      "10. Pedile al cliente que realice el paso junto a vos en tiempo real para confirmar que sigue las instrucciones correctamente.",
      "11. Verificá que la configuración haya quedado aplicada pidiéndole que la revise en pantalla.",
      "12. Reforzá las recomendaciones de seguridad relacionadas con la función configurada.",
      "13. Preguntá al cliente si tiene otra duda de uso o configuración pendiente.",
      "14. Confirmá con el cliente que la consulta quedó resuelta y que puede usar la función sin inconvenientes.",
      "15. Registrá en el ticket la consulta atendida y la orientación brindada, y cerrá el caso."
    ]
  },
  "duda_limite": {
    "titulo": "Consulta de límites / montos",
    "objetivo": "Informar al cliente sobre sus límites y montos de operación en AppSol y orientarlo sobre cómo gestionarlos según las políticas del banco.",
    "pasos": [
      "1. Saludá al cliente y confirmá que su consulta es sobre límites o montos de operación (transferencias, QR, pagos).",
      "2. Verificá la identidad del cliente con número de documento (CI), nombre completo y un dato adicional de validación.",
      "3. Identificá sobre qué tipo de operación consulta el límite: transferencias internas, a terceros, interbancarias (ACH) o pagos QR.",
      "4. Verificá en el sistema los límites vigentes asociados al perfil y tipo de cuenta del cliente.",
      "5. Informá al cliente de forma clara su límite diario y por transacción para la operación consultada.",
      "6. Explicá cómo el cliente puede consultar sus límites directamente en la sección correspondiente de AppSol.",
      "7. Si el cliente alcanzó su límite, explicá cuándo se restablece (por ejemplo, al inicio del siguiente día) y por qué existe ese control.",
      "8. Indicá si el límite consultado puede modificarse y bajo qué condiciones según las políticas del banco y la normativa ASFI.",
      "9. Si solicita un aumento de límite, explicá el procedimiento, requisitos y canales habilitados para gestionarlo.",
      "10. Si la solicitud requiere validación adicional o presencial, indicale acercarse a una agencia o el canal correspondiente.",
      "11. Aclará cualquier diferencia entre límites por canal (AppSol, Solnet, agencia) para evitar confusiones.",
      "12. Recordale las recomendaciones de seguridad asociadas a operar dentro de límites controlados.",
      "13. Confirmá con el cliente que entendió la información sobre sus límites y las opciones disponibles.",
      "14. Si quedó una gestión pendiente (solicitud de aumento), entregá un número de caso y el tiempo estimado de respuesta.",
      "15. Registrá en el ticket la consulta atendida y la información o gestión brindada, y cerrá o dejá en seguimiento según corresponda."
    ]
  }
}

==========================================================================================
ARCHIVO: SolUno_n8n_sistema.json
==========================================================================================
{
  "id": "soluno-sys",
  "name": "SolUno - Sistema Completo (IA + Ciclo Vivo)",
  "active": false,
  "nodes": [
    { "parameters": { "content": "## SolUno - Sistema Completo\n\n**Reporte:** Canales -> Tokenizar -> IA (Groq) -> Agrupar semantico -> Queja(autoservicio)/Incidente(equipo) -> Respuesta.\n**Cierre del ciclo:** Resolver -> Playbook Vivo (compartido) -> Informe ASFI.\n\nEl playbook que actualiza 'Resolver' lo USA 'Respuesta Queja' => ciclo cerrado real.", "height": 240, "width": 380 }, "id": "nota-config", "name": "Notas", "type": "n8n-nodes-base.stickyNote", "typeVersion": 1, "position": [-560, 20] },
    { "parameters": { "updates": ["messages"] }, "id": "trig-wa", "name": "WA Trigger (produccion)", "type": "n8n-nodes-base.whatsAppTrigger", "typeVersion": 1, "position": [-140, 80], "disabled": true },
    { "parameters": { "httpMethod": "POST", "path": "meta-incoming", "responseMode": "onReceived", "options": {} }, "id": "trig-meta", "name": "Meta Webhook (produccion)", "type": "n8n-nodes-base.webhook", "typeVersion": 2, "position": [-140, 280], "webhookId": "meta-incoming" },
    { "parameters": { "httpMethod": "POST", "path": "soluno-reporte", "responseMode": "responseNode", "options": {} }, "id": "trig-demo", "name": "Webhook Demo", "type": "n8n-nodes-base.webhook", "typeVersion": 2, "position": [-140, 480], "webhookId": "soluno-reporte" },
    { "parameters": { "jsCode": "const j = $input.item.json;\nlet mensaje = '', remitente = '';\ntry {\n  const m = (j.messages && j.messages[0]) || (j.entry && j.entry[0].changes[0].value.messages[0]);\n  mensaje = m.text ? m.text.body : (m.body || '');\n  remitente = m.from || '';\n} catch (e) { mensaje = j.message || ''; }\nreturn { json: { origen: 'whatsapp', canal: 'WhatsApp', producto: '', agencia: '', mensaje, cliente: { telefono: remitente }, remitente } };" }, "id": "norm-wa", "name": "Norm. WhatsApp", "type": "n8n-nodes-base.code", "typeVersion": 2, "position": [120, 80] },
    { "parameters": { "jsCode": "const b = $input.item.json.body || $input.item.json;\nlet canal = b.object === 'instagram' ? 'Instagram' : 'Facebook';\nlet mensaje = '', remitente = '';\ntry {\n  const ev = b.entry[0].messaging[0];\n  mensaje = ev.message ? (ev.message.text || '') : '';\n  remitente = ev.sender ? ev.sender.id : '';\n} catch (e) { mensaje = b.mensaje || ''; canal = b.canal || canal; }\nreturn { json: { origen: 'meta', canal, producto: '', agencia: '', mensaje, cliente: { id: remitente }, remitente } };" }, "id": "norm-meta", "name": "Norm. Meta", "type": "n8n-nodes-base.code", "typeVersion": 2, "position": [120, 280] },
    { "parameters": { "jsCode": "const b = $input.item.json.body || $input.item.json;\nconst cli = b.cliente || {};\nreturn { json: { origen: 'demo', canal: b.canal || 'Demo', producto: b.producto || '', agencia: b.agencia || '', mensaje: b.mensaje || '', cliente: cli, remitente: b.remitente || cli.telefono || cli.id || '', historial: b.historial || '' } };" }, "id": "norm-demo", "name": "Norm. Demo", "type": "n8n-nodes-base.code", "typeVersion": 2, "position": [120, 480] },
    { "parameters": { "jsCode": "const j = $input.item.json;\nconst cliente = j.cliente || {};\nlet texto = (j.mensaje || '').toString();\nconst mapa = {};\nfunction tk(v, p) { if (!v) return; const t = '{' + p + '_001}'; mapa[t] = v; if (texto.includes(v)) texto = texto.split(v).join(t); }\ntk(cliente.nombre, 'NAME'); tk(cliente.ci, 'CI'); tk(cliente.cuenta, 'ACCT'); tk(cliente.telefono, 'PHONE');\nreturn { json: { ...j, texto_tokenizado: texto, mapa_tokens: mapa, cliente_visible: { nombre: cliente.nombre || null, ci: cliente.ci || null } } };" }, "id": "tokenizar", "name": "Tokenizar (PII)", "type": "n8n-nodes-base.code", "typeVersion": 2, "position": [360, 280] },
    { "parameters": { "method": "POST", "url": "https://api.groq.com/openai/v1/chat/completions", "sendHeaders": true, "headerParameters": { "parameters": [ { "name": "Authorization", "value": "=Bearer {{ $env.GROQ_API_KEY }}" }, { "name": "content-type", "value": "application/json" } ] }, "sendBody": true, "specifyBody": "json", "jsonBody": "={{ JSON.stringify({ model: 'llama-3.3-70b-versatile', max_tokens: 300, temperature: 0, messages: [{ role: 'system', content: 'Eres un clasificador de incidencias de BancoSol. Responde SOLO con un JSON valido, sin texto extra, forma exacta: {\"tipo\":\"queja|incidente|consulta\",\"producto\":\"altoke|appSol|Solnet|desconocido\",\"categoria\":\"acceso|transacciones|rendimiento|datos|general\",\"severidad\":\"alta|media|baja\",\"firma\":\"slug\"}. INCIDENTE si falla una funcion critica de forma generalizada (login, transferencia, QR, ACH, app caida, nadie puede entrar). QUEJA si es un caso particular de bajo impacto con solucion conocida (cambio de dispositivo y reactivar tokens; pago y aun figura deuda; dudas de configuracion). CONSULTA si es un saludo, pregunta general o mensaje vago sin un problema concreto (hola, buenas, una consulta, info). El campo \"firma\" DEBE ser EXACTAMENTE UNA de esta lista cerrada (elige la mas adecuada, NO inventes otras): fallo_pagos_qr, error_transferencia, fallo_ach, caida_login, app_no_carga, lentitud_servicio, datos_no_actualizados, deuda_no_actualizada, cambio_dispositivo, duda_configuracion, duda_limite, otro. Mensajes del MISMO problema raiz DEBEN compartir la misma firma aunque esten redactados distinto.' }, { role: 'user', content: ($json.texto_tokenizado || '') }] }) }}", "options": {} }, "id": "clasif-ia", "name": "Clasificar IA (Groq)", "type": "n8n-nodes-base.httpRequest", "typeVersion": 4.2, "position": [600, 180], "onError": "continueRegularOutput" },
    { "parameters": { "jsCode": "// Usa la IA si devolvio JSON valido; si no, REGLAS.\nconst base = $('Tokenizar (PII)').item.json;\nconst resp = $input.item.json;\nlet tipo, producto, categoria, severidad, firma, fuente = 'reglas';\ntry {\n  let txt = '';\n  if (resp && resp.choices && resp.choices[0] && resp.choices[0].message) txt = resp.choices[0].message.content || '';\n  else if (resp && resp.content && resp.content[0] && resp.content[0].text) txt = resp.content[0].text;\n  if (txt) { const a = txt.indexOf('{'), b = txt.lastIndexOf('}'); if (a>=0 && b>a) { const c = JSON.parse(txt.slice(a,b+1)); if (c.tipo) { tipo=c.tipo; producto=c.producto; categoria=c.categoria; severidad=c.severidad; firma=c.firma; fuente='ia (groq)'; } } }\n} catch(e) {}\nif (!tipo) {\n  const t = (base.texto_tokenizado||'').toLowerCase();\n  producto = base.producto || 'desconocido';\n  if (t.includes('altoke')||t.includes('qr')) producto='altoke'; else if (t.includes('appsol')) producto='appSol'; else if (t.includes('solnet')) producto='Solnet';\n  tipo='queja'; severidad='baja'; categoria='general';\n  const con=[['cambie de celular','cambié de celular','cambie de dispositivo','nuevo celular','nuevo dispositivo'],['escanear','carnet','extranjero'],['sigue saliendo la deuda','sigue la deuda','no me han devuelto','ya pague','pague y sigue']];\n  const cat=['acceso','acceso','transacciones']; let ok=false;\n  for (let i=0;i<con.length;i++){ if(con[i].some(x=>t.includes(x))){ok=true;categoria=cat[i];break;} }\n  if(!ok){ const se=['no puedo entrar','no puedo iniciar','no inicia','no carga','no funciona','caido','caida','no puedo transferir','qr no','no procesa','error general','se cae','app caida','no puedo acceder','no anda','nadie puede']; const cr=['login','iniciar sesion','transferencia','transferir','qr','ach'];\n    if(se.some(k=>t.includes(k))){tipo='incidente';severidad='alta';}\n    if(cr.some(k=>t.includes(k))&&(t.includes('no ')||t.includes('error')||t.includes('falla'))){tipo='incidente';severidad='alta';}\n    if(t.includes('transfer')||t.includes('pago')||t.includes('qr'))categoria='transacciones'; else if(t.includes('iniciar')||t.includes('login')||t.includes('entrar')||t.includes('sesion')||t.includes('acceso'))categoria='acceso'; else if(t.includes('lento')||t.includes('demora')||t.includes('timeout'))categoria='rendimiento'; else if(t.includes('saldo')||t.includes('movimiento')||t.includes('comprobante'))categoria='datos';\n  }\n}\nif (tipo!=='queja' && tipo!=='incidente') tipo='queja';\nif (!firma) firma = (producto||'x') + '_' + (categoria||'general');\nfirma = String(firma).toLowerCase().replace(/[^a-z0-9_]+/g,'_').replace(/^_+|_+$/g,'');\nreturn { json: { ...base, tipo, severidad: severidad||'baja', producto: producto||'desconocido', categoria: categoria||'general', firma, clasificado_por: fuente } };" }, "id": "interpretar", "name": "Interpretar Clasificacion", "type": "n8n-nodes-base.code", "typeVersion": 2, "position": [840, 280] },
    { "parameters": { "jsCode": "// AGRUPAR por FIRMA (semantico) + CONTAR + ESCALAR severidad por afectados\nconst j = $input.item.json;\nconst sd = $getWorkflowStaticData('global');\nsd.incidentes = sd.incidentes || {};\nif (j.tipo === 'incidente') {\n  const clave = (j.producto||'x') + '|' + (j.firma||'general');\n  if (!sd.incidentes[clave]) { sd.incidentes[clave] = { caso_id: 'INC-' + Date.now().toString().slice(-5), afectados: 0 }; }\n  sd.incidentes[clave].afectados += 1;\n  j.caso_id = sd.incidentes[clave].caso_id;\n  j.afectados = sd.incidentes[clave].afectados;\n  j.incidente_nuevo = sd.incidentes[clave].afectados === 1;\n  j.clave_incidente = clave;\n  // Escalar severidad segun impacto real\n  if (j.afectados >= 10) j.severidad = 'critica';\n  else if (j.afectados >= 3 && j.severidad === 'baja') j.severidad = 'media';\n} else {\n  j.caso_id = 'QJA-' + Date.now().toString().slice(-5);\n  j.afectados = 1;\n}\nreturn { json: j };" }, "id": "agrupar", "name": "Agrupar (semantico) y Contar", "type": "n8n-nodes-base.code", "typeVersion": 2, "position": [1080, 280] },
    { "parameters": { "conditions": { "options": { "caseSensitive": true, "typeValidation": "loose" }, "combinator": "and", "conditions": [ { "id": "c-inc", "leftValue": "={{ $json.tipo }}", "rightValue": "incidente", "operator": { "type": "string", "operation": "equals" } } ] }, "options": {} }, "id": "if-inc", "name": "Es Incidente?", "type": "n8n-nodes-base.if", "typeVersion": 2.2, "position": [1300, 280] },
    { "parameters": { "jsCode": "const j = $input.item.json;\nlet equipo = 'Mesa de Incidentes TI';\nif (j.categoria === 'transacciones' || j.producto === 'altoke') equipo = 'Equipo Pagos/QR';\nelse if (j.categoria === 'acceso') equipo = 'Equipo Seguridad/Accesos';\nelse if (j.categoria === 'rendimiento') equipo = 'Infraestructura/TI';\nelse if (j.producto === 'appSol' || j.producto === 'Solnet') equipo = 'DevOps Aplicaciones';\nreturn { json: { ...j, resultado: 'INCIDENTE', estado: 'En análisis', equipo_asignado: equipo, paquete_para_desarrollo: { que_falla: (j.categoria||'general')+' en '+(j.producto||'desconocido'), firma: j.firma, producto: j.producto, severidad: j.severidad, afectados: j.afectados, incidente_nuevo: j.incidente_nuevo, datos_para_reproducir: { canal: j.canal, producto: j.producto, agencia: j.agencia, texto_tokenizado: j.texto_tokenizado }, equipo_asignado: equipo, estado: 'En análisis' }, respuesta_cliente: 'Recibimos tu reporte. Lo vinculamos al incidente '+j.caso_id+' (afectados: '+j.afectados+'). Lo derivamos a '+equipo+'. Te avisaremos por este mismo canal. Estado: En análisis.' } };" }, "id": "paquete", "name": "Paquete Incidente", "type": "n8n-nodes-base.code", "typeVersion": 2, "position": [1520, 120] },
    { "parameters": { "rules": { "values": [ { "conditions": { "options": { "caseSensitive": true, "leftValue": "", "typeValidation": "loose" }, "combinator": "and", "conditions": [ { "id": "t1", "leftValue": "={{ $json.equipo_asignado }}", "rightValue": "Equipo Pagos/QR", "operator": { "type": "string", "operation": "equals" } } ] }, "renameOutput": true, "outputKey": "Pagos/QR" }, { "conditions": { "options": { "caseSensitive": true, "leftValue": "", "typeValidation": "loose" }, "combinator": "and", "conditions": [ { "id": "t2", "leftValue": "={{ $json.equipo_asignado }}", "rightValue": "Equipo Seguridad/Accesos", "operator": { "type": "string", "operation": "equals" } } ] }, "renameOutput": true, "outputKey": "Seguridad" }, { "conditions": { "options": { "caseSensitive": true, "leftValue": "", "typeValidation": "loose" }, "combinator": "and", "conditions": [ { "id": "t3", "leftValue": "={{ $json.equipo_asignado }}", "rightValue": "DevOps Aplicaciones", "operator": { "type": "string", "operation": "equals" } } ] }, "renameOutput": true, "outputKey": "DevOps" }, { "conditions": { "options": { "caseSensitive": true, "leftValue": "", "typeValidation": "loose" }, "combinator": "and", "conditions": [ { "id": "t4", "leftValue": "={{ $json.equipo_asignado }}", "rightValue": "Infraestructura/TI", "operator": { "type": "string", "operation": "equals" } } ] }, "renameOutput": true, "outputKey": "Infraestructura/TI" } ] }, "options": { "fallbackOutput": "extra" } }, "id": "switch-equipo", "name": "Enrutar a Equipo", "type": "n8n-nodes-base.switch", "typeVersion": 3.2, "position": [1740, 120] },
    { "parameters": {}, "id": "team-pagos", "name": "Equipo Pagos/QR", "type": "n8n-nodes-base.noOp", "typeVersion": 1, "position": [1980, -100] },
    { "parameters": {}, "id": "team-seg", "name": "Equipo Seguridad/Accesos", "type": "n8n-nodes-base.noOp", "typeVersion": 1, "position": [1980, 40] },
    { "parameters": {}, "id": "team-devops", "name": "DevOps Aplicaciones", "type": "n8n-nodes-base.noOp", "typeVersion": 1, "position": [1980, 180] },
    { "parameters": {}, "id": "team-infra", "name": "Infraestructura/TI", "type": "n8n-nodes-base.noOp", "typeVersion": 1, "position": [1980, 320] },
    { "parameters": {}, "id": "team-mesa", "name": "Mesa de Incidentes TI", "type": "n8n-nodes-base.noOp", "typeVersion": 1, "position": [1980, 460] },
    { "parameters": { "jsCode": "// RESPUESTA QUEJA - LEE EL PLAYBOOK VIVO (compartido con Resolver)\nconst j = $input.item.json;\nconst sd = $getWorkflowStaticData('global');\nsd.playbook = sd.playbook || {};\nconst defaults = {\n  acceso: { pasos: ['1. Cierra y vuelve a abrir la app','2. Si cambiaste de dispositivo, reactiva tus tokens','3. Si persiste, vuelve a escanear tu carnet'], respuesta: 'Para resolver tu acceso: reactiva tus tokens o vuelve a escanear tu carnet siguiendo los pasos.' },\n  transacciones: { pasos: ['1. Verifica tu comprobante','2. Espera el transcurso del día (conciliación)','3. Si no se refleja, te contactamos'], respuesta: 'Tu operación está en proceso. Espera el transcurso del día; se reflejará tras la conciliación.' },\n  rendimiento: { pasos: ['1. Revisa tu conexión','2. Cierra otras apps','3. Actualiza la app'], respuesta: 'Puede ser conexión o versión. Sigue los pasos; si continúa, avísanos.' },\n  datos: { pasos: ['1. Refresca la pantalla','2. Espera unos minutos','3. Si no aparece, te ayudamos'], respuesta: 'Tu información puede tardar en actualizarse. Refresca; si no aparece, escríbenos.' },\n  general: { pasos: ['1. Revisa tu conexión','2. Actualiza la app'], respuesta: 'Gracias por tu consulta. Sigue los pasos; si continúa, escríbenos.' }\n};\nconst clave = (j.producto||'x') + '|' + (j.categoria||'general');\nconst vivo = sd.playbook[clave];\nlet pasos, respuesta, version;\nif (vivo) { pasos = vivo.pasos; respuesta = vivo.respuesta; version = 'v' + vivo.version + ' (vivo)'; }\nelse { const pb = defaults[j.categoria] || defaults.general; pasos = pb.pasos; respuesta = pb.respuesta; version = 'v1 (base)'; }\nreturn { json: { ...j, resultado: (j.tipo === 'consulta' ? 'CONSULTA' : 'QUEJA'), estado: (j.tipo === 'consulta' ? 'Respondido' : 'Resuelto (autoservicio)'), equipo_asignado: 'Ninguno (autoservicio)', playbook_version: version, respuesta_cliente: respuesta, pasos } };" }, "id": "queja", "name": "Respuesta Queja (Playbook Vivo)", "type": "n8n-nodes-base.code", "typeVersion": 2, "position": [1520, 600] },
    { "parameters": { "jsCode": "const j = $input.item.json;\nconst sd = $getWorkflowStaticData('global');\nsd.casos = sd.casos || [];\nsd.metricas = sd.metricas || { total: 0, quejas: 0, incidentes: 0 };\nsd.metricas.total += 1;\nif (j.resultado === 'INCIDENTE') sd.metricas.incidentes += 1; else sd.metricas.quejas += 1;\nsd.casos.push({ caso_id: j.caso_id, tipo: j.resultado, producto: j.producto, categoria: j.categoria, firma: j.firma, estado: j.estado, canal: j.canal, afectados: j.afectados, equipo: j.equipo_asignado, ia: j.clasificado_por, ts: new Date().toISOString() });\nif (sd.casos.length > 500) sd.casos = sd.casos.slice(-500);\nj.metricas = sd.metricas;\nreturn { json: j };" }, "id": "registrar", "name": "Registrar Estado y Metricas", "type": "n8n-nodes-base.code", "typeVersion": 2, "position": [2240, 280] },
    { "parameters": { "method": "POST", "url": "https://api.groq.com/openai/v1/chat/completions", "sendHeaders": true, "headerParameters": { "parameters": [ { "name": "Authorization", "value": "=Bearer {{ $env.GROQ_API_KEY }}" }, { "name": "content-type", "value": "application/json" } ] }, "sendBody": true, "specifyBody": "json", "jsonBody": "={{ JSON.stringify({ model: 'llama-3.3-70b-versatile', max_tokens: 500, temperature: 0.4, messages: [ { role: 'system', content: 'Sos el agente de atencion al cliente de BancoSol (deci siempre BancoSol, nunca solo banco) por WhatsApp. Tene SIEMPRE en cuenta TODO el contexto previo de la conversacion para no perder el hilo aunque el cliente cambie de tema o use palabras genericas. Responde EXACTAMENTE en este formato y NADA mas: primero el MENSAJE PARA EL CLIENTE, luego una linea con solo tres guiones ---, luego la PARTE TECNICA. No uses JSON, comillas de envoltura, ni etiquetas o titulos (NO escribas las palabras MENSAJE PARA EL CLIENTE ni PARTE TECNICA): escribi directamente el contenido de cada parte. MENSAJE PARA EL CLIENTE: escribi SUPER simple, como si le hablaras a un nino de 6 anos o a alguien que no entiende NADA de tecnologia; frases cortas, calidas y claras; NUNCA uses ni menciones las palabras queja, incidente, incidencia, consulta, caso, derivar, severidad, ticket, equipo de TI ni ningun termino tecnico. Si saluda o charla, responde natural y amable (ej: Muy bien gracias! Vos como andas? En que te ayudo?). Si tiene un problema que se puede resolver, ayudalo vos: empeza con UNA frase calida y empatica, despues dale la solucion en pasos NUMERADOS (1. 2. 3. 4.), cada paso en su propia linea y con palabras muy simples, basandote en los pasos del manual, y cerra ofreciendo seguir ayudando (que NO suene rigido ni de plantilla, adaptalo al caso real). Si es un problema grande que no podes resolver vos, deci de forma simple y tranquilizadora que ya lo estan arreglando y que le avisaras por aca apenas funcione, sin usar palabras tecnicas. PARTE TECNICA: si es INCIDENTE una recomendacion para el equipo de soporte de BancoSol con posible causa y pasos de diagnostico y solucion; si NO es incidente dejala vacia. No inventes datos ni cifras.' }, { role: 'user', content: 'CONTEXTO PREVIO de la conversacion: ' + ($json.historial || '(sin mensajes previos)') + '. MENSAJE ACTUAL del cliente: ' + ($json.texto_tokenizado || '') + '. Clasificacion: ' + ($json.tipo || 'queja') + ' / ' + ($json.categoria || 'general') + ($json.tipo === 'incidente' ? ('. Caso ' + ($json.caso_id || '') + ', derivado a ' + ($json.equipo_asignado || '')) : ('. Pasos del manual: ' + (Array.isArray($json.pasos) ? $json.pasos.join(' ') : ''))) } ] }) }}", "options": {} }, "id": "gen-resp", "name": "Generar Respuesta IA", "type": "n8n-nodes-base.httpRequest", "typeVersion": 4.2, "position": [2440, 80], "onError": "continueRegularOutput" },
    { "parameters": { "jsCode": "const base = $('Registrar Estado y Metricas').item.json;\nconst resp = $input.item.json;\nlet txt = '';\ntry { if (resp && resp.choices && resp.choices[0] && resp.choices[0].message) txt = (resp.choices[0].message.content || '').trim(); } catch (e) {}\nif (txt) {\n  let t = txt.replace(/```/g, '').trim(); let cliente = t, tecnico = ''; const idx = t.indexOf('---'); if (idx >= 0) { cliente = t.slice(0, idx); tecnico = t.slice(idx + 3); } cliente = cliente.replace(/^\\s*(mensaje\\s+para\\s+el\\s+cliente|cliente)\\s*:?\\s*/i, '').trim(); tecnico = tecnico.replace(/^\\s*(parte\\s+tecnica|tecnico)\\s*:?\\s*/i, '').trim(); if (cliente) base.respuesta_cliente = cliente; base.respuesta_ia = true; if (tecnico) base.recomendacion_tecnica = tecnico;\n}\nreturn { json: base };" }, "id": "aplicar-resp", "name": "Aplicar Respuesta IA", "type": "n8n-nodes-base.code", "typeVersion": 2, "position": [2640, 80] },
    { "parameters": { "conditions": { "options": { "caseSensitive": true, "typeValidation": "loose" }, "combinator": "and", "conditions": [ { "id": "c-demo", "leftValue": "={{ $json.origen }}", "rightValue": "demo", "operator": { "type": "string", "operation": "equals" } } ] }, "options": {} }, "id": "if-demo", "name": "origen = demo?", "type": "n8n-nodes-base.if", "typeVersion": 2.2, "position": [2460, 280] },
    { "parameters": { "conditions": { "options": { "caseSensitive": true, "typeValidation": "loose" }, "combinator": "and", "conditions": [ { "id": "c-wa", "leftValue": "={{ $json.origen }}", "rightValue": "whatsapp", "operator": { "type": "string", "operation": "equals" } } ] }, "options": {} }, "id": "if-wa", "name": "origen = whatsapp?", "type": "n8n-nodes-base.if", "typeVersion": 2.2, "position": [2680, 420] },
    { "parameters": { "options": {} }, "id": "respond-demo", "name": "Responder Demo", "type": "n8n-nodes-base.respondToWebhook", "typeVersion": 1.1, "position": [2680, 180] },
    { "parameters": { "operation": "send", "phoneNumberId": "PEGAR_PHONE_NUMBER_ID", "recipientPhoneNumber": "={{ $json.remitente }}", "messageType": "text", "textBody": "={{ $json.respuesta_cliente }}", "additionalFields": {} }, "id": "send-wa", "name": "Enviar WhatsApp", "type": "n8n-nodes-base.whatsApp", "typeVersion": 1, "position": [2900, 340] },
    { "parameters": { "method": "POST", "url": "https://graph.facebook.com/v21.0/PEGAR_PAGE_ID/messages", "sendHeaders": true, "headerParameters": { "parameters": [ { "name": "Authorization", "value": "Bearer PEGAR_META_TOKEN" }, { "name": "Content-Type", "value": "application/json" } ] }, "sendBody": true, "specifyBody": "json", "jsonBody": "={{ JSON.stringify({ recipient: { id: $json.remitente }, message: { text: $json.respuesta_cliente } }) }}", "options": {} }, "id": "send-meta", "name": "Enviar Meta", "type": "n8n-nodes-base.httpRequest", "typeVersion": 4.2, "position": [2900, 500] },

    { "parameters": { "content": "## CIERRE DEL CICLO\nEl equipo resuelve -> pasos -> Playbook Vivo (MISMA base que usa Respuesta Queja) -> Informe ASFI.\nPOST /webhook/soluno-resolver", "height": 180, "width": 360 }, "id": "nota-cierre", "name": "Nota Cierre", "type": "n8n-nodes-base.stickyNote", "typeVersion": 1, "position": [-560, 760] },
    { "parameters": { "httpMethod": "POST", "path": "soluno-resolver", "responseMode": "responseNode", "options": {} }, "id": "wh-resolver", "name": "Resolver (Dev)", "type": "n8n-nodes-base.webhook", "typeVersion": 2, "position": [-140, 920], "webhookId": "soluno-resolver" },
    { "parameters": { "jsCode": "const j = $input.item.json.body || $input.item.json;\nconst sol = (j.solucion || '').toString().trim();\nlet pasos = sol.split(/[\\n.;]+/).map(s => s.trim()).filter(s => s.length > 3).map((s, i) => (i + 1) + '. ' + s.charAt(0).toUpperCase() + s.slice(1));\nif (pasos.length === 0) pasos = ['1. ' + (sol || 'Sin detalle')];\nreturn { json: { caso_id: j.caso_id || 'INC-?', producto: j.producto || 'desconocido', categoria: j.categoria || 'general', severidad: j.severidad || 'alta', afectados: j.afectados || 0, duracion_min: j.duracion_min || null, solucion_original: sol, pasos } };" }, "id": "convertir", "name": "Convertir en Pasos", "type": "n8n-nodes-base.code", "typeVersion": 2, "position": [120, 920] },
    { "parameters": { "jsCode": "// PUBLICAR AL PLAYBOOK VIVO (misma static data que lee Respuesta Queja)\nconst j = $input.item.json;\nconst sd = $getWorkflowStaticData('global');\nsd.playbook = sd.playbook || {};\nconst clave = (j.producto||'x') + '|' + (j.categoria||'general');\nconst prev = sd.playbook[clave];\nconst version = prev ? prev.version + 1 : 1;\nconst respuesta = 'Solución validada por el equipo: ' + j.pasos.join(' ');\nsd.playbook[clave] = { version, pasos: j.pasos, respuesta, actualizado: new Date().toISOString(), caso_id: j.caso_id };\nreturn { json: { ...j, playbook: { clave, version, pasos: j.pasos, respuesta, estado: version > 1 ? 'actualizado (v'+version+')' : 'creado (v1)' } } };" }, "id": "publicar", "name": "Publicar al Playbook Vivo", "type": "n8n-nodes-base.code", "typeVersion": 2, "position": [360, 920] },
    { "parameters": { "jsCode": "const j = $input.item.json;\nconst fecha = new Date().toISOString().slice(0,10);\nconst informe = { numero: 'ASFI-'+(j.caso_id||'').replace('INC-','')+'-'+fecha.replace(/-/g,''), fecha, tipo_incidente: (j.categoria||'general')+' en '+(j.producto||'desconocido'), severidad: j.severidad, clientes_afectados: j.afectados, duracion_minutos: j.duracion_min, que_paso: 'Incidente '+j.caso_id+' de tipo '+j.categoria+' en '+j.producto+', severidad '+j.severidad+', con '+j.afectados+' afectado(s).', como_se_resolvio: j.solucion_original, acciones_preventivas: j.pasos, estado: 'BORRADOR — pendiente de aprobación humana' };\nreturn { json: { resultado: 'CICLO CERRADO', caso_id: j.caso_id, playbook: j.playbook, informe_asfi: informe, mensaje: 'Solución publicada al Playbook '+j.playbook.estado+' y borrador de informe ASFI generado ('+informe.numero+').' } };" }, "id": "informe", "name": "Generar Informe ASFI", "type": "n8n-nodes-base.code", "typeVersion": 2, "position": [600, 920] },
    { "parameters": { "options": {} }, "id": "respond-resol", "name": "Responder Resolver", "type": "n8n-nodes-base.respondToWebhook", "typeVersion": 1.1, "position": [840, 920] }
  ],
  "connections": {
    "WA Trigger (produccion)": { "main": [[{ "node": "Norm. WhatsApp", "type": "main", "index": 0 }]] },
    "Meta Webhook (produccion)": { "main": [[{ "node": "Norm. Meta", "type": "main", "index": 0 }]] },
    "Webhook Demo": { "main": [[{ "node": "Norm. Demo", "type": "main", "index": 0 }]] },
    "Norm. WhatsApp": { "main": [[{ "node": "Tokenizar (PII)", "type": "main", "index": 0 }]] },
    "Norm. Meta": { "main": [[{ "node": "Tokenizar (PII)", "type": "main", "index": 0 }]] },
    "Norm. Demo": { "main": [[{ "node": "Tokenizar (PII)", "type": "main", "index": 0 }]] },
    "Tokenizar (PII)": { "main": [[{ "node": "Clasificar IA (Groq)", "type": "main", "index": 0 }]] },
    "Clasificar IA (Groq)": { "main": [[{ "node": "Interpretar Clasificacion", "type": "main", "index": 0 }]] },
    "Interpretar Clasificacion": { "main": [[{ "node": "Agrupar (semantico) y Contar", "type": "main", "index": 0 }]] },
    "Agrupar (semantico) y Contar": { "main": [[{ "node": "Es Incidente?", "type": "main", "index": 0 }]] },
    "Es Incidente?": { "main": [[{ "node": "Paquete Incidente", "type": "main", "index": 0 }], [{ "node": "Respuesta Queja (Playbook Vivo)", "type": "main", "index": 0 }]] },
    "Paquete Incidente": { "main": [[{ "node": "Enrutar a Equipo", "type": "main", "index": 0 }]] },
    "Enrutar a Equipo": { "main": [[{ "node": "Equipo Pagos/QR", "type": "main", "index": 0 }], [{ "node": "Equipo Seguridad/Accesos", "type": "main", "index": 0 }], [{ "node": "DevOps Aplicaciones", "type": "main", "index": 0 }], [{ "node": "Infraestructura/TI", "type": "main", "index": 0 }], [{ "node": "Mesa de Incidentes TI", "type": "main", "index": 0 }]] },
    "Equipo Pagos/QR": { "main": [[{ "node": "Registrar Estado y Metricas", "type": "main", "index": 0 }]] },
    "Equipo Seguridad/Accesos": { "main": [[{ "node": "Registrar Estado y Metricas", "type": "main", "index": 0 }]] },
    "DevOps Aplicaciones": { "main": [[{ "node": "Registrar Estado y Metricas", "type": "main", "index": 0 }]] },
    "Infraestructura/TI": { "main": [[{ "node": "Registrar Estado y Metricas", "type": "main", "index": 0 }]] },
    "Mesa de Incidentes TI": { "main": [[{ "node": "Registrar Estado y Metricas", "type": "main", "index": 0 }]] },
    "Respuesta Queja (Playbook Vivo)": { "main": [[{ "node": "Registrar Estado y Metricas", "type": "main", "index": 0 }]] },
    "Registrar Estado y Metricas": { "main": [[{ "node": "Generar Respuesta IA", "type": "main", "index": 0 }]] },
    "Generar Respuesta IA": { "main": [[{ "node": "Aplicar Respuesta IA", "type": "main", "index": 0 }]] },
    "Aplicar Respuesta IA": { "main": [[{ "node": "origen = demo?", "type": "main", "index": 0 }]] },
    "origen = demo?": { "main": [[{ "node": "Responder Demo", "type": "main", "index": 0 }], [{ "node": "origen = whatsapp?", "type": "main", "index": 0 }]] },
    "origen = whatsapp?": { "main": [[{ "node": "Enviar WhatsApp", "type": "main", "index": 0 }], [{ "node": "Enviar Meta", "type": "main", "index": 0 }]] },
    "Resolver (Dev)": { "main": [[{ "node": "Convertir en Pasos", "type": "main", "index": 0 }]] },
    "Convertir en Pasos": { "main": [[{ "node": "Publicar al Playbook Vivo", "type": "main", "index": 0 }]] },
    "Publicar al Playbook Vivo": { "main": [[{ "node": "Generar Informe ASFI", "type": "main", "index": 0 }]] },
    "Generar Informe ASFI": { "main": [[{ "node": "Responder Resolver", "type": "main", "index": 0 }]] }
  },
  "settings": { "executionOrder": "v1" },
  "pinData": {}
}

==========================================================================================
ARCHIVO: SolUno_n8n_watest.json
==========================================================================================
{
  "id": "soluno-watest",
  "name": "SolUno - Test Envio WhatsApp",
  "active": false,
  "nodes": [
    { "parameters": { "httpMethod": "POST", "path": "test-wa", "responseMode": "responseNode", "options": {} }, "id": "wh", "name": "Disparar envio", "type": "n8n-nodes-base.webhook", "typeVersion": 2, "position": [240, 300], "webhookId": "test-wa" },
    { "parameters": { "method": "POST", "url": "=https://graph.facebook.com/v22.0/{{ $env.WA_PHONE_ID }}/messages", "sendHeaders": true, "headerParameters": { "parameters": [ { "name": "Authorization", "value": "=Bearer {{ $env.WHATSAPP_TOKEN }}" }, { "name": "Content-Type", "value": "application/json" } ] }, "sendBody": true, "specifyBody": "json", "jsonBody": "={{ JSON.stringify( ($json.body && $json.body.interactive) ? { messaging_product:'whatsapp', to:$json.body.to, type:'interactive', interactive:$json.body.interactive } : { messaging_product:'whatsapp', to:(($json.body && $json.body.to) ? $json.body.to : '59169176200'), type:(($json.body && $json.body.text) ? 'text' : 'template'), text:{ body:(($json.body && $json.body.text) ? $json.body.text : 'SolUno') }, template:{ name:'hello_world', language:{ code:'en_US' } } } ) }}", "options": {} }, "id": "send", "name": "Enviar a Meta", "type": "n8n-nodes-base.httpRequest", "typeVersion": 4.2, "position": [480, 300], "onError": "continueRegularOutput" },
    { "parameters": { "options": {} }, "id": "resp", "name": "Responder", "type": "n8n-nodes-base.respondToWebhook", "typeVersion": 1.1, "position": [720, 300] }
  ],
  "connections": {
    "Disparar envio": { "main": [[{ "node": "Enviar a Meta", "type": "main", "index": 0 }]] },
    "Enviar a Meta": { "main": [[{ "node": "Responder", "type": "main", "index": 0 }]] }
  },
  "settings": { "executionOrder": "v1" },
  "pinData": {}
}

==========================================================================================
ARCHIVO: SolUno_n8n_workflow.json
==========================================================================================
{
  "id": "soluno-wf-001",
  "name": "SolUno - Ingesta y Clasificacion (v1 reglas)",
  "active": false,
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "soluno-reporte",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "node-webhook",
      "name": "1. Webhook (Ingesta)",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [240, 300],
      "webhookId": "soluno-reporte"
    },
    {
      "parameters": {
        "jsCode": "// === 2. TOKENIZACION (on-premise) ===\n// Reemplaza PII por tokens ANTES de cualquier IA.\n// Oculta lo irrelevante; conserva nombre+CI solo para el revisor interno.\nconst body = $input.item.json.body || $input.item.json;\nconst cliente = body.cliente || {};\nlet texto = (body.mensaje || '').toString();\nconst mapa = {};\n\nfunction tokenizar(valor, prefijo) {\n  if (!valor) return;\n  const token = '{' + prefijo + '_001}';\n  mapa[token] = valor;\n  if (texto.includes(valor)) texto = texto.split(valor).join(token);\n}\n\n// PII tokenizada (no va a la IA en claro)\ntokenizar(cliente.nombre, 'NAME');\ntokenizar(cliente.ci, 'CI');\ntokenizar(cliente.cuenta, 'ACCT');\ntokenizar(cliente.telefono, 'PHONE');\n\nreturn {\n  json: {\n    canal: body.canal || 'desconocido',\n    producto: body.producto || '',\n    agencia: body.agencia || '',\n    mensaje_original: body.mensaje || '',\n    texto_tokenizado: texto,\n    mapa_tokens: mapa,\n    // El revisor interno solo necesita esto (segun el banco):\n    cliente_visible: { nombre: cliente.nombre || null, ci: cliente.ci || null }\n  }\n};"
      },
      "id": "node-tokenizar",
      "name": "2. Tokenizar (PII)",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [460, 300]
    },
    {
      "parameters": {
        "jsCode": "// === 3. CLASIFICACION POR REGLAS ===\n// (Luego se reemplaza por una llamada a Claude para entender el contexto.)\nconst j = $input.item.json;\nconst t = (j.texto_tokenizado || '').toLowerCase();\n\n// Producto (capa por canal/producto)\nlet producto = j.producto || 'desconocido';\nif (t.includes('altoke') || t.includes('qr')) producto = 'altoke';\nelse if (t.includes('appsol')) producto = 'appSol';\nelse if (t.includes('solnet')) producto = 'Solnet';\n\nlet tipo = 'queja';\nlet severidad = 'baja';\nlet categoria = 'general';\n\n// --- PASO 1: QUEJAS CONOCIDAS (tienen prioridad; son casos del playbook) ---\n// El banco las definio como bajo impacto con solucion operativa conocida.\nconst quejasConocidas = [\n  { patron: ['cambie de celular','cambié de celular','cambie de dispositivo','cambié de dispositivo','nuevo celular','nuevo dispositivo','cambie mi telefono'], categoria: 'acceso' },\n  { patron: ['escanear','carnet','extranjero','re-escanear','volver a escanear'], categoria: 'acceso' },\n  { patron: ['sigue saliendo la deuda','sigue la deuda','no me han devuelto','ya pague','ya pagué','pague y sigue','pagué y sigue'], categoria: 'transacciones' }\n];\nlet esQuejaConocida = false;\nfor (const q of quejasConocidas) {\n  if (q.patron.some(p => t.includes(p))) { esQuejaConocida = true; categoria = q.categoria; break; }\n}\n\nif (!esQuejaConocida) {\n  // --- PASO 2: INCIDENTE (falla generalizada de funcion critica) ---\n  const senalesIncidente = [\n    'no puedo entrar','no puedo iniciar','no inicia','no carga','no funciona',\n    'caido','caida','caída','no puedo transferir','no me deja transferir','qr no',\n    'no procesa','error general','se cae','app caida','no puedo acceder','no anda','nadie puede'\n  ];\n  const criticas = ['login','iniciar sesion','iniciar sesión','transferencia','transferir','qr','ach'];\n  if (senalesIncidente.some(k => t.includes(k))) { tipo = 'incidente'; severidad = 'alta'; }\n  if (criticas.some(k => t.includes(k)) && (t.includes('no ') || t.includes('error') || t.includes('falla'))) { tipo = 'incidente'; severidad = 'alta'; }\n\n  // categoria operativa (del Excel de categorizacion)\n  if (t.includes('transfer') || t.includes('pago') || t.includes('qr')) categoria = 'transacciones';\n  else if (t.includes('iniciar') || t.includes('login') || t.includes('entrar') || t.includes('sesion') || t.includes('acceso')) categoria = 'acceso';\n  else if (t.includes('lento') || t.includes('demora') || t.includes('timeout')) categoria = 'rendimiento';\n  else if (t.includes('saldo') || t.includes('movimiento') || t.includes('comprobante')) categoria = 'datos';\n}\n\nreturn { json: { ...j, tipo, severidad, producto, categoria } };"
      },
      "id": "node-clasificar",
      "name": "3. Clasificar (reglas)",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [680, 300]
    },
    {
      "parameters": {
        "conditions": {
          "options": { "caseSensitive": true, "typeValidation": "loose" },
          "combinator": "and",
          "conditions": [
            {
              "id": "cond-incidente",
              "leftValue": "={{ $json.tipo }}",
              "rightValue": "incidente",
              "operator": { "type": "string", "operation": "equals" }
            }
          ]
        },
        "options": {}
      },
      "id": "node-if",
      "name": "4. ¿Es Incidente?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [900, 300]
    },
    {
      "parameters": {
        "jsCode": "// === 5a. CAMINO INCIDENTE: armar Paquete de Incidente ===\nconst j = $input.item.json;\nconst id = 'INC-' + Date.now().toString().slice(-5);\nconst equipo = j.producto === 'altoke' ? 'Equipo Pagos/QR' : (j.producto === 'appSol' ? 'DevOps appSol' : 'DevOps');\n\nreturn { json: {\n  resultado: 'INCIDENTE',\n  caso_id: id,\n  paquete_para_desarrollo: {\n    que_falla: j.categoria + ' en ' + j.producto,\n    producto: j.producto,\n    severidad: j.severidad,\n    afectados: 1,                         // se incrementa al agrupar casos iguales\n    datos_para_reproducir: {\n      canal: j.canal,\n      producto: j.producto,\n      agencia: j.agencia,\n      texto_tokenizado: j.texto_tokenizado\n    },\n    equipo_asignado: equipo,\n    estado: 'En análisis'\n  },\n  respuesta_cliente: 'Recibimos tu reporte. Lo vinculamos al incidente ' + id + '. Estamos trabajando en ello y te avisaremos por este mismo canal. Estado: En análisis.'\n} };"
      },
      "id": "node-incidente",
      "name": "5a. Paquete de Incidente",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1140, 200]
    },
    {
      "parameters": {
        "jsCode": "// === 5b. CAMINO QUEJA: respuesta desde Playbook Vivo ===\n// (Playbook embebido para el demo; luego sale de la base versionada.)\nconst j = $input.item.json;\nconst playbook = {\n  acceso: {\n    pasos: ['1. Cierra y vuelve a abrir la app', '2. Si cambiaste de dispositivo, reactiva tus tokens', '3. Si persiste, vuelve a escanear tu carnet'],\n    respuesta: 'Para resolver tu acceso: reactiva tus tokens o vuelve a escanear tu carnet siguiendo los pasos. Si continúa, escríbenos.'\n  },\n  transacciones: {\n    pasos: ['1. Verifica tu comprobante', '2. Espera el transcurso del día (conciliación)', '3. Si no se refleja, te contactamos'],\n    respuesta: 'Tu operación está en proceso. Por favor espera el transcurso del día; se reflejará tras la conciliación.'\n  },\n  rendimiento: {\n    pasos: ['1. Revisa tu conexión a internet', '2. Cierra otras apps', '3. Actualiza la app a la última versión'],\n    respuesta: 'Puede ser un tema de conexión o versión. Sigue los pasos; si continúa, avísanos.'\n  },\n  datos: {\n    pasos: ['1. Refresca la pantalla', '2. Espera unos minutos por actualización', '3. Si no aparece, te ayudamos'],\n    respuesta: 'Tu información puede tardar en actualizarse. Refresca la pantalla; si no aparece, escríbenos.'\n  },\n  general: {\n    pasos: ['1. Revisa tu conexión', '2. Actualiza la app'],\n    respuesta: 'Gracias por tu consulta. Sigue los pasos indicados; si continúa, escríbenos.'\n  }\n};\nconst pb = playbook[j.categoria] || playbook.general;\nconst id = 'QJA-' + Date.now().toString().slice(-5);\n\nreturn { json: {\n  resultado: 'QUEJA',\n  caso_id: id,\n  producto: j.producto,\n  categoria: j.categoria,\n  playbook_version: 'v1',\n  canal: j.canal,\n  respuesta_cliente: pb.respuesta,   // MISMA respuesta en todos los canales\n  pasos: pb.pasos,\n  estado: 'Resuelto (autoservicio)'\n} };"
      },
      "id": "node-queja",
      "name": "5b. Respuesta Queja (Playbook)",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1140, 400]
    },
    {
      "parameters": { "options": {} },
      "id": "node-respond",
      "name": "6. Responder",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [1380, 300]
    }
  ],
  "connections": {
    "1. Webhook (Ingesta)": { "main": [[{ "node": "2. Tokenizar (PII)", "type": "main", "index": 0 }]] },
    "2. Tokenizar (PII)": { "main": [[{ "node": "3. Clasificar (reglas)", "type": "main", "index": 0 }]] },
    "3. Clasificar (reglas)": { "main": [[{ "node": "4. ¿Es Incidente?", "type": "main", "index": 0 }]] },
    "4. ¿Es Incidente?": { "main": [[{ "node": "5a. Paquete de Incidente", "type": "main", "index": 0 }], [{ "node": "5b. Respuesta Queja (Playbook)", "type": "main", "index": 0 }]] },
    "5a. Paquete de Incidente": { "main": [[{ "node": "6. Responder", "type": "main", "index": 0 }]] },
    "5b. Respuesta Queja (Playbook)": { "main": [[{ "node": "6. Responder", "type": "main", "index": 0 }]] }
  },
  "settings": { "executionOrder": "v1" },
  "pinData": {}
}

==========================================================================================
ARCHIVO: SolUno_n8n_flujo_completo.json
==========================================================================================
{
  "id": "soluno-main",
  "name": "SolUno - Flujo Completo (WhatsApp + Meta)",
  "active": false,
  "nodes": [
    {
      "parameters": { "content": "## SolUno - Flujo Completo\n\n**Canales:** WhatsApp + Meta (FB/IG) + Demo.\n**QUEJA** => autoservicio (Playbook).\n**INCIDENTE** => ruteo al EQUIPO correcto.\n\nEl Webhook Demo funciona sin credenciales.", "height": 260, "width": 340 },
      "id": "nota-config",
      "name": "Config / Notas",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [-540, 40]
    },
    {
      "parameters": { "content": "### AGENTE IA (Claude)\nClasifica con IA real.\nLee la API key de la variable de entorno ANTHROPIC_API_KEY.\n\n**Si NO hay key** => cae automaticamente a reglas (el demo sigue funcionando).\n**Con key** => Claude entiende el contexto (mucho mejor que reglas).", "height": 220, "width": 320 },
      "id": "nota-ia",
      "name": "Nota Agente IA",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [560, -140]
    },
    {
      "parameters": { "updates": ["messages"] },
      "id": "trig-wa",
      "name": "WA Trigger (produccion)",
      "type": "n8n-nodes-base.whatsAppTrigger",
      "typeVersion": 1,
      "position": [-120, 80],
      "disabled": true
    },
    {
      "parameters": { "httpMethod": "POST", "path": "meta-incoming", "responseMode": "onReceived", "options": {} },
      "id": "trig-meta",
      "name": "Meta Webhook (produccion)",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [-120, 280],
      "webhookId": "meta-incoming"
    },
    {
      "parameters": { "httpMethod": "POST", "path": "soluno-reporte", "responseMode": "responseNode", "options": {} },
      "id": "trig-demo",
      "name": "Webhook Demo",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [-120, 480],
      "webhookId": "soluno-reporte"
    },
    {
      "parameters": { "jsCode": "const j = $input.item.json;\nlet mensaje = '', remitente = '';\ntry {\n  const m = (j.messages && j.messages[0]) || (j.entry && j.entry[0].changes[0].value.messages[0]);\n  mensaje = m.text ? m.text.body : (m.body || '');\n  remitente = m.from || '';\n} catch (e) { mensaje = j.message || ''; }\nreturn { json: { origen: 'whatsapp', canal: 'WhatsApp', producto: '', agencia: '', mensaje, cliente: { telefono: remitente }, remitente } };" },
      "id": "norm-wa",
      "name": "Norm. WhatsApp",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [140, 80]
    },
    {
      "parameters": { "jsCode": "const b = $input.item.json.body || $input.item.json;\nlet canal = b.object === 'instagram' ? 'Instagram' : 'Facebook';\nlet mensaje = '', remitente = '';\ntry {\n  const ev = b.entry[0].messaging[0];\n  mensaje = ev.message ? (ev.message.text || '') : '';\n  remitente = ev.sender ? ev.sender.id : '';\n} catch (e) { mensaje = b.mensaje || ''; canal = b.canal || canal; }\nreturn { json: { origen: 'meta', canal, producto: '', agencia: '', mensaje, cliente: { id: remitente }, remitente } };" },
      "id": "norm-meta",
      "name": "Norm. Meta",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [140, 280]
    },
    {
      "parameters": { "jsCode": "const b = $input.item.json.body || $input.item.json;\nconst cli = b.cliente || {};\nreturn { json: { origen: 'demo', canal: b.canal || 'Demo', producto: b.producto || '', agencia: b.agencia || '', mensaje: b.mensaje || '', cliente: cli, remitente: b.remitente || cli.telefono || cli.id || '' } };" },
      "id": "norm-demo",
      "name": "Norm. Demo",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [140, 480]
    },
    {
      "parameters": { "jsCode": "// === 2. TOKENIZACION (on-premise) ===\nconst j = $input.item.json;\nconst cliente = j.cliente || {};\nlet texto = (j.mensaje || '').toString();\nconst mapa = {};\nfunction tk(v, p) { if (!v) return; const t = '{' + p + '_001}'; mapa[t] = v; if (texto.includes(v)) texto = texto.split(v).join(t); }\ntk(cliente.nombre, 'NAME');\ntk(cliente.ci, 'CI');\ntk(cliente.cuenta, 'ACCT');\ntk(cliente.telefono, 'PHONE');\nreturn { json: { ...j, texto_tokenizado: texto, mapa_tokens: mapa, cliente_visible: { nombre: cliente.nombre || null, ci: cliente.ci || null } } };" },
      "id": "tokenizar",
      "name": "Tokenizar (PII)",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [380, 280]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://api.groq.com/openai/v1/chat/completions",
        "sendHeaders": true,
        "headerParameters": { "parameters": [
          { "name": "Authorization", "value": "=Bearer {{ $env.GROQ_API_KEY }}" },
          { "name": "content-type", "value": "application/json" }
        ] },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ model: 'llama-3.3-70b-versatile', max_tokens: 300, temperature: 0, messages: [{ role: 'system', content: 'Eres un clasificador de incidencias de BancoSol. Responde SOLO con un JSON valido, sin texto extra, con esta forma exacta: {\"tipo\":\"queja|incidente\",\"producto\":\"altoke|appSol|Solnet|desconocido\",\"categoria\":\"acceso|transacciones|rendimiento|datos|general\",\"severidad\":\"alta|media|baja\"}. Es INCIDENTE si falla una funcion critica de forma generalizada (login, transferencia, QR, ACH, app caida, nadie puede entrar). Es QUEJA si es un caso particular de bajo impacto con solucion conocida (cambio de dispositivo y debe reactivar tokens; pago y aun figura la deuda; dudas de configuracion).' }, { role: 'user', content: ($json.texto_tokenizado || '') }] }) }}",
        "options": {}
      },
      "id": "clasif-ia",
      "name": "Clasificar IA (Groq)",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [620, 180],
      "onError": "continueRegularOutput"
    },
    {
      "parameters": { "jsCode": "// === 3. INTERPRETAR: usa Claude si respondio JSON valido; si no, REGLAS ===\nconst base = $('Tokenizar (PII)').item.json;\nconst resp = $input.item.json;\nlet tipo, producto, categoria, severidad, fuente = 'reglas';\n\ntry {\n  let txt = '';\n  if (resp && resp.choices && resp.choices[0] && resp.choices[0].message) txt = resp.choices[0].message.content || '';\n  else if (resp && resp.content && resp.content[0] && resp.content[0].text) txt = resp.content[0].text;\n  else if (typeof resp === 'string') txt = resp;\n  if (txt) {\n    const a = txt.indexOf('{'); const b = txt.lastIndexOf('}');\n    if (a >= 0 && b > a) { const c = JSON.parse(txt.slice(a, b + 1)); if (c.tipo) { tipo = c.tipo; producto = c.producto; categoria = c.categoria; severidad = c.severidad; fuente = 'ia (groq)'; } }\n  }\n} catch (e) {}\n\nif (!tipo) {\n  const t = (base.texto_tokenizado || '').toLowerCase();\n  producto = base.producto || 'desconocido';\n  if (t.includes('altoke') || t.includes('qr')) producto = 'altoke';\n  else if (t.includes('appsol')) producto = 'appSol';\n  else if (t.includes('solnet')) producto = 'Solnet';\n  tipo = 'queja'; severidad = 'baja'; categoria = 'general';\n  const conocidas = [['cambie de celular','cambié de celular','cambie de dispositivo','nuevo celular','nuevo dispositivo'], ['escanear','carnet','extranjero'], ['sigue saliendo la deuda','sigue la deuda','no me han devuelto','ya pague','pague y sigue']];\n  const cats = ['acceso','acceso','transacciones'];\n  let conocida = false;\n  for (let i = 0; i < conocidas.length; i++) { if (conocidas[i].some(x => t.includes(x))) { conocida = true; categoria = cats[i]; break; } }\n  if (!conocida) {\n    const senales = ['no puedo entrar','no puedo iniciar','no inicia','no carga','no funciona','caido','caida','no puedo transferir','qr no','no procesa','error general','se cae','app caida','no puedo acceder','no anda','nadie puede'];\n    const criticas = ['login','iniciar sesion','transferencia','transferir','qr','ach'];\n    if (senales.some(k => t.includes(k))) { tipo = 'incidente'; severidad = 'alta'; }\n    if (criticas.some(k => t.includes(k)) && (t.includes('no ') || t.includes('error') || t.includes('falla'))) { tipo = 'incidente'; severidad = 'alta'; }\n    if (t.includes('transfer') || t.includes('pago') || t.includes('qr')) categoria = 'transacciones';\n    else if (t.includes('iniciar') || t.includes('login') || t.includes('entrar') || t.includes('sesion') || t.includes('acceso')) categoria = 'acceso';\n    else if (t.includes('lento') || t.includes('demora') || t.includes('timeout')) categoria = 'rendimiento';\n    else if (t.includes('saldo') || t.includes('movimiento') || t.includes('comprobante')) categoria = 'datos';\n  }\n}\nif (tipo !== 'queja' && tipo !== 'incidente') tipo = 'queja';\nreturn { json: { ...base, tipo, severidad: severidad || 'baja', producto: producto || 'desconocido', categoria: categoria || 'general', clasificado_por: fuente } };" },
      "id": "interpretar",
      "name": "Interpretar Clasificacion",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [840, 280]
    },
    {
      "parameters": { "jsCode": "// === 4. AGRUPAR INCIDENTES + CONTAR AFECTADOS ===\nconst j = $input.item.json;\nconst sd = $getWorkflowStaticData('global');\nsd.incidentes = sd.incidentes || {};\nif (j.tipo === 'incidente') {\n  const clave = (j.producto || 'x') + '|' + (j.categoria || 'general');\n  if (!sd.incidentes[clave]) { sd.incidentes[clave] = { caso_id: 'INC-' + Date.now().toString().slice(-5), afectados: 0 }; }\n  sd.incidentes[clave].afectados += 1;\n  j.caso_id = sd.incidentes[clave].caso_id;\n  j.afectados = sd.incidentes[clave].afectados;\n  j.incidente_nuevo = sd.incidentes[clave].afectados === 1;\n} else {\n  j.caso_id = 'QJA-' + Date.now().toString().slice(-5);\n  j.afectados = 1;\n}\nreturn { json: j };" },
      "id": "agrupar",
      "name": "Agrupar y Contar",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1060, 280]
    },
    {
      "parameters": {
        "conditions": { "options": { "caseSensitive": true, "typeValidation": "loose" }, "combinator": "and", "conditions": [ { "id": "c-inc", "leftValue": "={{ $json.tipo }}", "rightValue": "incidente", "operator": { "type": "string", "operation": "equals" } } ] },
        "options": {}
      },
      "id": "if-inc",
      "name": "Es Incidente?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [1280, 280]
    },
    {
      "parameters": { "jsCode": "// === 6a. PAQUETE DE INCIDENTE + asignar EQUIPO ===\nconst j = $input.item.json;\nlet equipo = 'Mesa de Incidentes TI';\nif (j.categoria === 'transacciones' || j.producto === 'altoke') equipo = 'Equipo Pagos/QR';\nelse if (j.categoria === 'acceso') equipo = 'Equipo Seguridad/Accesos';\nelse if (j.categoria === 'rendimiento') equipo = 'Infraestructura/TI';\nelse if (j.producto === 'appSol' || j.producto === 'Solnet') equipo = 'DevOps Aplicaciones';\nreturn { json: { ...j, resultado: 'INCIDENTE', estado: 'En análisis', equipo_asignado: equipo, paquete_para_desarrollo: { que_falla: (j.categoria || 'general') + ' en ' + (j.producto || 'desconocido'), producto: j.producto, severidad: j.severidad, afectados: j.afectados, incidente_nuevo: j.incidente_nuevo, datos_para_reproducir: { canal: j.canal, producto: j.producto, agencia: j.agencia, texto_tokenizado: j.texto_tokenizado }, equipo_asignado: equipo, estado: 'En análisis' }, respuesta_cliente: 'Recibimos tu reporte. Lo vinculamos al incidente ' + j.caso_id + ' (afectados: ' + j.afectados + '). Lo derivamos a ' + equipo + '. Te avisaremos por este mismo canal. Estado: En análisis.' } };" },
      "id": "paquete",
      "name": "Paquete Incidente",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1500, 120]
    },
    {
      "parameters": { "content": "### RUTEO AL EQUIPO CORRECTO\nCada salida = aviso real (Slack/Teams/Email/HTTP) al equipo.", "height": 140, "width": 300 },
      "id": "nota-equipos",
      "name": "Nota Ruteo Equipos",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [1680, -200]
    },
    {
      "parameters": {
        "rules": {
          "values": [
            { "conditions": { "options": { "caseSensitive": true, "leftValue": "", "typeValidation": "loose" }, "combinator": "and", "conditions": [ { "id": "t1", "leftValue": "={{ $json.equipo_asignado }}", "rightValue": "Equipo Pagos/QR", "operator": { "type": "string", "operation": "equals" } } ] }, "renameOutput": true, "outputKey": "Pagos/QR" },
            { "conditions": { "options": { "caseSensitive": true, "leftValue": "", "typeValidation": "loose" }, "combinator": "and", "conditions": [ { "id": "t2", "leftValue": "={{ $json.equipo_asignado }}", "rightValue": "Equipo Seguridad/Accesos", "operator": { "type": "string", "operation": "equals" } } ] }, "renameOutput": true, "outputKey": "Seguridad" },
            { "conditions": { "options": { "caseSensitive": true, "leftValue": "", "typeValidation": "loose" }, "combinator": "and", "conditions": [ { "id": "t3", "leftValue": "={{ $json.equipo_asignado }}", "rightValue": "DevOps Aplicaciones", "operator": { "type": "string", "operation": "equals" } } ] }, "renameOutput": true, "outputKey": "DevOps" },
            { "conditions": { "options": { "caseSensitive": true, "leftValue": "", "typeValidation": "loose" }, "combinator": "and", "conditions": [ { "id": "t4", "leftValue": "={{ $json.equipo_asignado }}", "rightValue": "Infraestructura/TI", "operator": { "type": "string", "operation": "equals" } } ] }, "renameOutput": true, "outputKey": "Infraestructura/TI" }
          ]
        },
        "options": { "fallbackOutput": "extra" }
      },
      "id": "switch-equipo",
      "name": "Enrutar a Equipo",
      "type": "n8n-nodes-base.switch",
      "typeVersion": 3.2,
      "position": [1720, 120]
    },
    { "parameters": {}, "id": "team-pagos", "name": "Equipo Pagos/QR", "type": "n8n-nodes-base.noOp", "typeVersion": 1, "position": [1960, -100] },
    { "parameters": {}, "id": "team-seg", "name": "Equipo Seguridad/Accesos", "type": "n8n-nodes-base.noOp", "typeVersion": 1, "position": [1960, 40] },
    { "parameters": {}, "id": "team-devops", "name": "DevOps Aplicaciones", "type": "n8n-nodes-base.noOp", "typeVersion": 1, "position": [1960, 180] },
    { "parameters": {}, "id": "team-infra", "name": "Infraestructura/TI", "type": "n8n-nodes-base.noOp", "typeVersion": 1, "position": [1960, 320] },
    { "parameters": {}, "id": "team-mesa", "name": "Mesa de Incidentes TI", "type": "n8n-nodes-base.noOp", "typeVersion": 1, "position": [1960, 460] },
    {
      "parameters": { "jsCode": "// === 6b. RESPUESTA QUEJA (Playbook Vivo) - se resuelve sola ===\nconst j = $input.item.json;\nconst playbook = {\n  acceso: { pasos: ['1. Cierra y vuelve a abrir la app', '2. Si cambiaste de dispositivo, reactiva tus tokens', '3. Si persiste, vuelve a escanear tu carnet'], respuesta: 'Para resolver tu acceso: reactiva tus tokens o vuelve a escanear tu carnet siguiendo los pasos. Si continúa, escríbenos.' },\n  transacciones: { pasos: ['1. Verifica tu comprobante', '2. Espera el transcurso del día (conciliación)', '3. Si no se refleja, te contactamos'], respuesta: 'Tu operación está en proceso. Por favor espera el transcurso del día; se reflejará tras la conciliación.' },\n  rendimiento: { pasos: ['1. Revisa tu conexión', '2. Cierra otras apps', '3. Actualiza la app'], respuesta: 'Puede ser conexión o versión. Sigue los pasos; si continúa, avísanos.' },\n  datos: { pasos: ['1. Refresca la pantalla', '2. Espera unos minutos', '3. Si no aparece, te ayudamos'], respuesta: 'Tu información puede tardar en actualizarse. Refresca; si no aparece, escríbenos.' },\n  general: { pasos: ['1. Revisa tu conexión', '2. Actualiza la app'], respuesta: 'Gracias por tu consulta. Sigue los pasos; si continúa, escríbenos.' }\n};\nconst pb = playbook[j.categoria] || playbook.general;\nreturn { json: { ...j, resultado: 'QUEJA', estado: 'Resuelto (autoservicio)', equipo_asignado: 'Ninguno (autoservicio)', playbook_version: 'v1', respuesta_cliente: pb.respuesta, pasos: pb.pasos } };" },
      "id": "queja",
      "name": "Respuesta Queja",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1500, 600]
    },
    {
      "parameters": { "jsCode": "// === 8. REGISTRAR ESTADO + METRICAS ===\nconst j = $input.item.json;\nconst sd = $getWorkflowStaticData('global');\nsd.casos = sd.casos || [];\nsd.metricas = sd.metricas || { total: 0, quejas: 0, incidentes: 0 };\nsd.metricas.total += 1;\nif (j.resultado === 'INCIDENTE') sd.metricas.incidentes += 1; else sd.metricas.quejas += 1;\nsd.casos.push({ caso_id: j.caso_id, tipo: j.resultado, producto: j.producto, categoria: j.categoria, estado: j.estado, canal: j.canal, afectados: j.afectados, equipo: j.equipo_asignado, ia: j.clasificado_por, ts: new Date().toISOString() });\nif (sd.casos.length > 500) sd.casos = sd.casos.slice(-500);\nj.metricas = sd.metricas;\nreturn { json: j };" },
      "id": "registrar",
      "name": "Registrar Estado y Metricas",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [2220, 280]
    },
    {
      "parameters": {
        "conditions": { "options": { "caseSensitive": true, "typeValidation": "loose" }, "combinator": "and", "conditions": [ { "id": "c-demo", "leftValue": "={{ $json.origen }}", "rightValue": "demo", "operator": { "type": "string", "operation": "equals" } } ] },
        "options": {}
      },
      "id": "if-demo",
      "name": "origen = demo?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [2440, 280]
    },
    {
      "parameters": {
        "conditions": { "options": { "caseSensitive": true, "typeValidation": "loose" }, "combinator": "and", "conditions": [ { "id": "c-wa", "leftValue": "={{ $json.origen }}", "rightValue": "whatsapp", "operator": { "type": "string", "operation": "equals" } } ] },
        "options": {}
      },
      "id": "if-wa",
      "name": "origen = whatsapp?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [2660, 420]
    },
    {
      "parameters": { "options": {} },
      "id": "respond-demo",
      "name": "Responder Demo",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [2660, 180]
    },
    {
      "parameters": { "operation": "send", "phoneNumberId": "PEGAR_PHONE_NUMBER_ID", "recipientPhoneNumber": "={{ $json.remitente }}", "messageType": "text", "textBody": "={{ $json.respuesta_cliente }}", "additionalFields": {} },
      "id": "send-wa",
      "name": "Enviar WhatsApp",
      "type": "n8n-nodes-base.whatsApp",
      "typeVersion": 1,
      "position": [2880, 340]
    },
    {
      "parameters": { "method": "POST", "url": "https://graph.facebook.com/v21.0/PEGAR_PAGE_ID/messages", "sendHeaders": true, "headerParameters": { "parameters": [ { "name": "Authorization", "value": "Bearer PEGAR_META_TOKEN" }, { "name": "Content-Type", "value": "application/json" } ] }, "sendBody": true, "specifyBody": "json", "jsonBody": "={{ JSON.stringify({ recipient: { id: $json.remitente }, message: { text: $json.respuesta_cliente } }) }}", "options": {} },
      "id": "send-meta",
      "name": "Enviar Meta",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [2880, 500]
    }
  ],
  "connections": {
    "WA Trigger (produccion)": { "main": [[{ "node": "Norm. WhatsApp", "type": "main", "index": 0 }]] },
    "Meta Webhook (produccion)": { "main": [[{ "node": "Norm. Meta", "type": "main", "index": 0 }]] },
    "Webhook Demo": { "main": [[{ "node": "Norm. Demo", "type": "main", "index": 0 }]] },
    "Norm. WhatsApp": { "main": [[{ "node": "Tokenizar (PII)", "type": "main", "index": 0 }]] },
    "Norm. Meta": { "main": [[{ "node": "Tokenizar (PII)", "type": "main", "index": 0 }]] },
    "Norm. Demo": { "main": [[{ "node": "Tokenizar (PII)", "type": "main", "index": 0 }]] },
    "Tokenizar (PII)": { "main": [[{ "node": "Clasificar IA (Groq)", "type": "main", "index": 0 }]] },
    "Clasificar IA (Groq)": { "main": [[{ "node": "Interpretar Clasificacion", "type": "main", "index": 0 }]] },
    "Interpretar Clasificacion": { "main": [[{ "node": "Agrupar y Contar", "type": "main", "index": 0 }]] },
    "Agrupar y Contar": { "main": [[{ "node": "Es Incidente?", "type": "main", "index": 0 }]] },
    "Es Incidente?": { "main": [[{ "node": "Paquete Incidente", "type": "main", "index": 0 }], [{ "node": "Respuesta Queja", "type": "main", "index": 0 }]] },
    "Paquete Incidente": { "main": [[{ "node": "Enrutar a Equipo", "type": "main", "index": 0 }]] },
    "Enrutar a Equipo": { "main": [[{ "node": "Equipo Pagos/QR", "type": "main", "index": 0 }], [{ "node": "Equipo Seguridad/Accesos", "type": "main", "index": 0 }], [{ "node": "DevOps Aplicaciones", "type": "main", "index": 0 }], [{ "node": "Infraestructura/TI", "type": "main", "index": 0 }], [{ "node": "Mesa de Incidentes TI", "type": "main", "index": 0 }]] },
    "Equipo Pagos/QR": { "main": [[{ "node": "Registrar Estado y Metricas", "type": "main", "index": 0 }]] },
    "Equipo Seguridad/Accesos": { "main": [[{ "node": "Registrar Estado y Metricas", "type": "main", "index": 0 }]] },
    "DevOps Aplicaciones": { "main": [[{ "node": "Registrar Estado y Metricas", "type": "main", "index": 0 }]] },
    "Infraestructura/TI": { "main": [[{ "node": "Registrar Estado y Metricas", "type": "main", "index": 0 }]] },
    "Mesa de Incidentes TI": { "main": [[{ "node": "Registrar Estado y Metricas", "type": "main", "index": 0 }]] },
    "Respuesta Queja": { "main": [[{ "node": "Registrar Estado y Metricas", "type": "main", "index": 0 }]] },
    "Registrar Estado y Metricas": { "main": [[{ "node": "origen = demo?", "type": "main", "index": 0 }]] },
    "origen = demo?": { "main": [[{ "node": "Responder Demo", "type": "main", "index": 0 }], [{ "node": "origen = whatsapp?", "type": "main", "index": 0 }]] },
    "origen = whatsapp?": { "main": [[{ "node": "Enviar WhatsApp", "type": "main", "index": 0 }], [{ "node": "Enviar Meta", "type": "main", "index": 0 }]] }
  },
  "settings": { "executionOrder": "v1" },
  "pinData": {}
}

==========================================================================================
ARCHIVO: SolUno_n8n_cierre_ciclo.json
==========================================================================================
{
  "id": "soluno-resol",
  "name": "SolUno - Cierre del Ciclo (Resolver + Playbook + ASFI)",
  "active": false,
  "nodes": [
    {
      "parameters": { "content": "## Cierre del Ciclo\n\nEl equipo resuelve el incidente y manda la solucion.\nSolUno:\n1. Convierte la solucion en pasos\n2. La publica al Playbook Vivo (versionado)\n3. Genera el borrador de informe ASFI\n\nPOST a /webhook/soluno-resolver con:\n{caso_id, producto, categoria, severidad, afectados, duracion_min, solucion}", "height": 280, "width": 360 },
      "id": "nota-cierre",
      "name": "Notas",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [-200, 40]
    },
    {
      "parameters": { "httpMethod": "POST", "path": "soluno-resolver", "responseMode": "responseNode", "options": {} },
      "id": "wh-resolver",
      "name": "Resolver (Dev)",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [260, 320],
      "webhookId": "soluno-resolver"
    },
    {
      "parameters": { "jsCode": "// === 1. CONVERTIR LA SOLUCION DEL DEV EN PASOS ===\n// (Luego Claude lo redacta mejor; aqui partimos por frases.)\nconst j = $input.item.json.body || $input.item.json;\nconst sol = (j.solucion || '').toString().trim();\nlet pasos = sol.split(/[\\n.;]+/).map(s => s.trim()).filter(s => s.length > 3).map((s, i) => (i + 1) + '. ' + s.charAt(0).toUpperCase() + s.slice(1));\nif (pasos.length === 0) pasos = ['1. ' + (sol || 'Sin detalle de solución')];\nreturn { json: { caso_id: j.caso_id || 'INC-?', producto: j.producto || 'desconocido', categoria: j.categoria || 'general', severidad: j.severidad || 'alta', afectados: j.afectados || 0, duracion_min: j.duracion_min || null, solucion_original: sol, pasos } };" },
      "id": "convertir",
      "name": "Convertir en Pasos",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [480, 320]
    },
    {
      "parameters": { "jsCode": "// === 2. PUBLICAR AL PLAYBOOK VIVO (versionado) ===\nconst j = $input.item.json;\nconst sd = $getWorkflowStaticData('global');\nsd.playbook = sd.playbook || {};\nconst clave = (j.producto || 'x') + '|' + (j.categoria || 'general');\nconst prev = sd.playbook[clave];\nconst version = prev ? prev.version + 1 : 1;\nconst respuesta = 'Para este caso: ' + j.pasos.join(' ');\nsd.playbook[clave] = { version, pasos: j.pasos, respuesta, actualizado: new Date().toISOString(), caso_id: j.caso_id };\nreturn { json: { ...j, playbook: { clave, version, pasos: j.pasos, respuesta, estado: version > 1 ? 'actualizado (v' + version + ')' : 'creado (v1)' } } };" },
      "id": "publicar",
      "name": "Publicar al Playbook",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [700, 320]
    },
    {
      "parameters": { "jsCode": "// === 3. GENERAR BORRADOR DE INFORME ASFI (post-mortem) ===\nconst j = $input.item.json;\nconst fecha = new Date().toISOString().slice(0, 10);\nconst informe = {\n  numero: 'ASFI-' + (j.caso_id || '').replace('INC-', '') + '-' + fecha.replace(/-/g, ''),\n  fecha,\n  tipo_incidente: (j.categoria || 'general') + ' en ' + (j.producto || 'desconocido'),\n  severidad: j.severidad,\n  clientes_afectados: j.afectados,\n  duracion_minutos: j.duracion_min,\n  que_paso: 'Incidente ' + j.caso_id + ' de tipo ' + j.categoria + ' en el producto ' + j.producto + ', severidad ' + j.severidad + ', con ' + j.afectados + ' cliente(s) afectado(s).',\n  como_se_resolvio: j.solucion_original,\n  acciones_preventivas: j.pasos,\n  estado: 'BORRADOR — pendiente de aprobación humana'\n};\nreturn { json: { resultado: 'CICLO CERRADO', caso_id: j.caso_id, playbook: j.playbook, informe_asfi: informe, mensaje: 'Solución publicada al Playbook ' + j.playbook.estado + ' y borrador de informe ASFI generado (' + informe.numero + ').' } };" },
      "id": "informe",
      "name": "Generar Informe ASFI",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [920, 320]
    },
    {
      "parameters": { "options": {} },
      "id": "respond-resol",
      "name": "Responder",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [1140, 320]
    }
  ],
  "connections": {
    "Resolver (Dev)": { "main": [[{ "node": "Convertir en Pasos", "type": "main", "index": 0 }]] },
    "Convertir en Pasos": { "main": [[{ "node": "Publicar al Playbook", "type": "main", "index": 0 }]] },
    "Publicar al Playbook": { "main": [[{ "node": "Generar Informe ASFI", "type": "main", "index": 0 }]] },
    "Generar Informe ASFI": { "main": [[{ "node": "Responder", "type": "main", "index": 0 }]] }
  },
  "settings": { "executionOrder": "v1" },
  "pinData": {}
}

==========================================================================================
ARCHIVO: SolUno_Proyecto_Maestro.md
==========================================================================================
# 📘 SolUno — Documento Maestro del Proyecto
### Sistema operativo de incidencias y comunicación para BancoSol · InnovaHack 2026
*Versión integrada · 30 de mayo de 2026 · Equipo SolUno*

> Documento único que integra: el problema completo, el Problem Statement validado por el banco,
> el diseño definitivo de la solución, los canales del MVP, los criterios de privacidad, los plus,
> el alcance de 48h, el mapeo a la rúbrica y los datos con fuentes (ver `SolUno_Banco_de_Datos.md`).

---

## 0. Resumen ejecutivo

**SolUno** es un **punto único** que recibe cada queja e incidente desde todos los canales de BancoSol,
**reconoce de qué se trata, a cuántos clientes afecta y con qué datos reproducirlo**, entrega una
**respuesta única y correcta**, rutea los incidentes al equipo técnico de forma **estructurada y formal**
(reemplazando los grupos de WhatsApp informales), y **devuelve la solución a un manual vivo** que mantiene
a call center y clientes hablando el mismo idioma — todo **sin que un dato confidencial salga del banco**.

**En una frase:** *Un punto. Una respuesta. Todos los canales.*

---

## 1. El problema completo de BancoSol

### 1.1 Quién es BancoSol
Institución **pionera y líder mundial en microfinanzas** de Bolivia. **+2,5 millones de clientes**,
**+1.801 puntos de atención**, **#1 del ranking CAMEL 10 años seguidos**, cartera de **USD 2.712 millones**,
con fuerte misión social y de equidad de género (programa Avanza Mujer, **USD 52M en bonos de género**).
Opera bajo **supervisión estricta de ASFI**.

### 1.2 El detonante: el éxito digital se volvió problema operativo
- 2023: moderniza su infraestructura con la **plataforma de APIs de Sensedia**.
- Mayo 2024: lanza **altoke** (billetera QR para microempresarios). Crecimiento exponencial:
  **0 → 800 mil usuarios en el 1er año (12% de la PEA)**, hoy **1,2 millones**, abriendo
  **20.000–25.000 cuentas por semana**.
- Ese éxito disparó el **tráfico de API en +2.000% en menos de 6 meses**, tensionando los sistemas
  y **multiplicando incidencias y reclamos** en todos los canales.

### 1.3 El dolor real (lo que sangra)
**a) Canales fragmentados:** los reclamos llegan por **9 vías distintas** (WhatsApp, FonoSol, appSol,
Solnet, altoke, sucursales, chatbot, redes sociales, Punto de Reclamo web), **cada una con su propio
playbook y ritmo de actualización**.

**b) Respuestas inconsistentes:** un mismo problema recibe **respuestas distintas según el canal**.

**c) Call Center cuello de botella:** tercerizado, sin conocimiento técnico profundo; solo atiende casos
pre-mapeados y **deriva el resto a desarrollo en lotes diarios** → los **devs hacen soporte en vez de
desarrollar**, los casos no se atienden de inmediato y el análisis es masivo, no personalizado.

**d) Comunicación interna informal (lo que el banco recalcó):** *"No hay nada centralizado… y ese es el
problema que tenemos. Está ligado a la comunicación."* Los incidentes se reportan en **grupos de Teams/
WhatsApp informales**. El equipo técnico **no sabe a cuántos usuarios afecta** ni **recibe datos para
replicar el error** en ambientes de desarrollo. Y al resolver, **la solución no vuelve ordenada al playbook**.

**e) El cliente no tiene a dónde ir:** no existe un punto único para reportar, consultar estado o
auto-resolver → toda la presión cae en el Call Center, con picos de llamadas en los momentos críticos.

### 1.4 La capa regulatoria (lo que lo vuelve crítico)
ASFI obliga a operar un **"Punto de Reclamo"** con respuesta en **máximo 5 días hábiles**, escrita,
íntegra y comprensible; cada reclamante exige un **expediente individual**; y el uso de IA en la nube
tropieza con el requisito de **soberanía del dato** (la información del cliente no puede salir del banco).

### 1.5 La distinción que dio el banco: Queja vs. Incidente
| | **QUEJA / CONSULTA** | **INCIDENTE** |
|---|---|---|
| Qué es | Problema particular, **bajo impacto**, error controlado | Afectación **grave**: falla función crítica |
| Ejemplos | Extranjero no transacciona tras update (0,5%), cambio de dispositivo → reactivar token, "pagué y sigue la deuda" | Falla **login, transferencia, QR, ACH** |
| Cómo se resuelve | **Operativo**: playbook actualizado, misma respuesta | Call center **compila + prioriza info** → equipo técnico |

---

## 2. Problem Statement final (validado por el banco)

> **El equipo de atención de BancoSol —call center y equipo de desarrollo— necesita recibir en un único
> punto y de forma estructurada cada queja e incidente que hoy llega disperso por todos los canales,
> sabiendo de qué se trata, a cuántos clientes afecta y con los datos necesarios para reproducirlo, para
> entregar una respuesta única, correcta y oportuna y, una vez resuelto, devolver esa solución a un manual
> vivo que mantenga a todos —call center y cliente— hablando el mismo idioma.**
>
> **Hoy no lo logran porque** no existe ni siquiera un canal interno formal: los incidentes se reportan en
> grupos de chat informales, el equipo técnico no sabe cuántos usuarios están afectados ni recibe datos para
> replicar el error, y las soluciones no vuelven de forma ordenada a los manuales, que quedan desactualizados
> y distintos en cada canal — por lo que un mismo problema recibe respuestas diferentes, los desarrolladores
> terminan haciendo soporte y el cliente queda sin certeza.
>
> **Una solución comercial existente no lo resuelve porque** no puede operar sin que la información del
> cliente salga del banco, no entiende la lógica específica de cada canal y producto, y no mantiene un único
> manual vivo y consistente que cumpla las exigencias de privacidad y de reporte individual del regulador boliviano.

*(La oración principal no menciona tecnología — cumple la regla de oro de la metodología Cyclix.)*

**Estado:** ✅ Validado por la persona del banco ("Está bien, está bien") en la sesión de validación.

---

## 3. Los 3 actores (metodología Cyclix · Paso 2)

| Actor | Rol | Éxito para él |
|---|---|---|
| **Quién ENCARGA** | BancoSol — área técnica/desarrollo + Eduardo Flores | Dejar de hacer soporte manual, comunicación consistente, cumplir ASFI |
| **Quién PADECE** | ① Agente Call Center · ② Equipo de desarrollo · ③ Cliente | Manual correcto a mano; solo la info necesaria; respuesta clara y única |
| **Quién USA el prototipo** | Vistas interna (call center + técnico) y cliente | — |

**A quién priorizamos:** al **operador interno** (call center + desarrollo). Ahí está el cuello de botella
de comunicación; si ellos tienen el playbook correcto y vigente, el cliente recibe la respuesta consistente
como consecuencia.

---

## 4. Viabilidad (Cyclix · Semáforo) → 🟢 Verde/Amarillo

Encuadramos el problema como **proceso interno ineficiente y conocido** (reporte informal + playbooks
inconsistentes) → **zona VERDE: "proceso ineficiente conocido → mockup que simula la mejora."**
La regulación ASFI es una **restricción a respetar** (privacidad, reporte individual), **NO el problema que
resolvemos** — así evitamos la zona ROJA (regulatoria). El propio banco confirmó: el informe ASFI es un *plus*,
el core es la comunicación.

---

## 5. Diseño definitivo de la solución

### 5.1 El ciclo que resolvemos
```
        ┌──────────────────── PUNTO ÚNICO (SolUno) ────────────────────┐
  CLIENTE / CANALES                                                     │
        │  (disperso, informal)                                        │
        ▼                                                              │
  ① INGESTA + CLASIFICACIÓN ──────► ¿queja o incidente? canal/producto │
        │                                                              │
   ┌────┴───────────────┐                                              │
   ▼ QUEJA              ▼ INCIDENTE                                     │
  ② PLAYBOOK CORRECTO  ③ PAQUETE DE INCIDENTE                          │
   respuesta única      (# afectados + datos para replicar)            │
   al cliente               │                                          │
                            ▼                                          │
                     EQUIPO DESARROLLO ◄── canal interno FORMAL        │
                            │           (ya no grupos WhatsApp)        │
                            ▼                                          │
                       RESOLUCIÓN                                      │
                            │                                          │
        ┌──── ④ PLAYBOOK VIVO ◄─┘  (la solución vuelve al manual)      │
        ▼                                                              │
   CLIENTE recibe estado + respuesta consistente                       │
        └──────────────────────────────────────────────────────────────┘
   🔒 Toda la PII tokenizada on-premise · el LLM nunca ve datos reales
```

### 5.2 Arquitectura ("qué corre dónde")
- 🟢 **On-premise:** n8n (orquestador) · vault de identidad · módulo de tokenización/re-hidratación ·
  base de playbooks **versionada** · base de incidentes · registro auditable.
- 🔵 **Cloud (solo texto tokenizado, Zero Data Retention):** el LLM que clasifica, redacta y genera
  informes — **ve "usuario 1", nunca "Fernando Alén".**

### 5.3 Los 3 mandamientos del banco (esqueleto del diseño)
1. *Centralizar, PERO identificar de qué reportan y servir el playbook específico y correcto.*
2. *En incidentes, priorizar qué info pasar al equipo técnico (no toda es necesaria).*
3. *Al resolver, automatizar la actualización del playbook.*
+ Regla de oro: *mismo problema en distinto canal → misma respuesta.*

### 5.4 El CORE — 4 componentes

**① Punto Único de Ingesta + Clasificación.** Recibe los canales en n8n y etiqueta en 3 dimensiones:
¿queja o incidente? · ¿qué canal/producto? (appSol ≠ altoke ≠ Solnet) · ¿nuevo o ya existe? (agrupa y
**cuenta afectados**).

**② Playbook Correcto → Respuesta Única (camino QUEJA).** Sirve la respuesta estandarizada y vigente →
**misma respuesta en todos los canales**. El agente aprueba y envía (o automática en casos simples).

**③ Paquete de Incidente → Canal Interno Formal (camino INCIDENTE).** *La pieza que el banco dijo que
faltaba.* Reemplaza los grupos de WhatsApp por un canal formal hacia Desarrollo. Cada incidente llega como
un paquete: **qué falla · a cuántos afecta · datos para replicar (tokenizados) · severidad/prioridad · solo
lo necesario.**

**④ Playbook Vivo → Retorno de la Solución (cierra el ciclo).** El dev resuelve → SolUno convierte la
solución en **pasos 1-2-3-4** que cualquiera siga → **publica al playbook** → **se propaga a todos los
canales** al instante. Versionado y trazable.

---

## 6. Criterios de información sensible (privacidad — core)

Premisa del banco: *"Toda información del cliente tiene que quedarse en el banco."*
Tokenización **inteligente** por tipo de dato:

| Dato | Acción | Por qué |
|---|---|---|
| Nombre completo | 🟡 Tokenizar al LLM, visible al revisor | El revisor lo necesita; el LLM no |
| CI / Carnet | 🟡 Tokenizar al LLM, visible al revisor | Suficiente para identificar internamente |
| Nº de cuenta | 🔴 Tokenizar y ocultar | Irrelevante para resolver |
| Saldo / montos | 🔴 Tokenizar y ocultar | Sensible, no necesario |
| Teléfono / correo / dirección | 🔴 Tokenizar y ocultar | Irrelevante para la revisión |
| Texto del problema | 🟢 Pasa tokenizado al LLM | Lo necesario para clasificar |

**Flujo:** PII → tokens **on-premise** → el LLM opera sobre *"usuario 1"* → **re-hidratación local** dentro
del banco. Como el LLM externo no recibe datos identificables, **no hay cesión a terceros** → simplifica el
cumplimiento de privacidad y de tercerización de ASFI.

---

## 7. ➕ PLUS #1 — Generación automática de documentos (informe ASFI)
Al cerrar un incidente, el LLM redacta el **informe individualizado/post-mortem** (qué pasó, en qué servidor,
cómo se resolvió, qué acciones para que no se repita). El humano **revisa y aprueba**. Doble valor: cumple el
reporte regulatorio **y** alimenta el playbook. *(El banco: "sería un plus… aporta más que la traducción.")*

## 8. ➕ PLUS #2 — Motor de traducción (área rural + extranjero)
Traduce el reporte del cliente (**quechua, aymara, guaraní** o extranjero) → español para el operador, y la
respuesta del playbook → idioma del cliente. Encaja con la misión social de BancoSol e inclusión (ESG / EU-LAC).
*Plus secundario: se demuestra si sobra tiempo.*

---

## 9. Canales del MVP — altoke + Meta

| | **altoke** | **Meta** (WhatsApp · Facebook · Instagram) |
|---|---|---|
| Tipo | Canal **interno / transaccional** | Canales **externos / públicos / conversacionales** |
| Por qué | La plataforma **más usada** del banco (1,2M usuarios, QR) | Los 3 canales que el banco **ya tiene conectados** vía Meta |
| Qué llega | Incidentes **críticos** (QR, transferencias) + contexto transaccional | Quejas en **texto libre**, a veces otro idioma, **públicas** |
| Ingesta | Webhook / API interna de altoke | Una capa **Meta Business** → WhatsApp Cloud API + Messenger + Instagram Graph API |

**Por qué es la combinación ideal:** son dos mundos opuestos (transaccional interno vs. público
conversacional) → demuestran que SolUno da **la misma respuesta consistente sin importar el canal**.
*El gancho del demo:* un cliente reporta "QR no funciona" en **altoke** y otro por **WhatsApp/Instagram**, y
ambos reciben **la misma respuesta**, mientras el dev ve **"2 afectados, mismo incidente"**.

> *Nota honesta para el pitch:* en el demo se **simulan/mockean** las APIs con datos asumidos (el banco no da
> datos reales ni accesos a producción). En producción: altoke por su API interna, Meta por la API de Meta Business.

---

## 10. Las dos vistas del prototipo
- 👤 **Cliente:** reporta por su canal → respuesta **consistente** → estado en 3 etapas
  **Recibido → En análisis → Resuelto** (en su idioma, si aplica el plus).
- 🏢 **Interna:** *Call Center* (bandeja unificada + respuesta sugerida del playbook lista para aprobar) ·
  *Desarrollo* (Paquete de Incidente: qué falla, # afectados, datos para replicar, prioridad) ·
  *Cierre del ciclo* (convertir solución en pasos → publicar al playbook).

---

## 11. Qué queda FUERA del MVP (roadmap)
❌ Detección de fraude · ❌ Geoanalítica Haversine · ❌ Integración real con core/Sensedia ·
❌ Los 9 canales completos (demo con altoke + Meta). Se trabaja con **datos asumidos**
(~2,5M clientes, picos <2%).

---

## 12. El MVP de 48h (golden path)
1. **2 canales**: altoke + Meta (al menos WhatsApp; Facebook/Instagram como parte del mismo conector).
2. **Clasificación** queja/incidente + canal/producto + **conteo de afectados** (agrupando altoke + Meta).
3. **Camino queja**: playbook correcto → **misma respuesta** en ambos canales.
4. **Camino incidente**: **Paquete de Incidente** (# afectados + datos para replicar, con contexto
   transaccional de altoke) → equipo de desarrollo.
5. **Ciclo de retorno**: solución → pasos → **publicada al playbook** → ambos canales.
6. **Tokenización** con los criterios de info sensible (§6).
7. **2 vistas** (cliente con 3 estados + interna).
8. **Plus #1**: borrador de informe ASFI generado.
9. *(Si sobra)* **Plus #2**: traducción (ideal con un caso de cliente rural por WhatsApp).

---

## 13. Datos clave para el pitch (resumen)
*(Detalle completo y todas las fuentes en `SolUno_Banco_de_Datos.md`.)*

- **Escala:** BancoSol +2,5M clientes, #1 CAMEL 10 años. · altoke 0→1,2M usuarios, **+2.000% de API en 6 meses**.
- **El dolor:** **32%** de llamadas son repetidas · devs pierden **17h/semana** en soporte (Stripe) ·
  cada hora de caída cuesta **+USD 300K** (ITIC).
- **Presión legal:** ASFI exige responder en **5 días hábiles**; **75.805 reclamos** en 2024 (⚠ verificar).
- **La causa raíz:** **75%** de clientes espera la **misma respuesta en todo canal** (Zendesk); **66%** debe
  repetir su info (Salesforce).
- **La solución:** GenAI ahorra **30–45%** de los costos de atención (McKinsey); privacidad por tokenización.
- **Plus social:** **2,2M** de bolivianos hablan lenguas originarias (quechua 1,39M, aymara 774.874, guaraní 43.870).

---

## 14. Mapeo a la rúbrica (100 pts)
| Criterio | Pts | Por qué ganamos |
|---|---|---|
| Impacto y alineación estratégica | 25 | Resolvemos el problema **validado por el banco en su voz** (comunicación + ciclo Call Center↔Dev) |
| Viabilidad técnica y escalabilidad | 25 | Arquitectura simple, on-prem, privacidad clara, sin sobre-promesas |
| Innovación y diferenciación | 20 | Paquete de Incidente + Playbook Vivo auto-actualizable + tokenización inteligente + informe automático |
| Validación usuario + UX | 15 | Diseñado con el cliente real; 2 vistas + status; problem statement validado; datos reales |
| Pitch y trabajo en equipo | 15 | Narrativa nítida + presentación rica en datos citados |

*Desempate: mayor puntaje en "Impacto y alineación" → es donde somos más fuertes.*

---

## 15. Identidad / branding (insumo para los logos)
- **Nombre:** SolUno — *un solo punto, una sola respuesta, un solo idioma.*
- **Tagline:** **"Un punto. Una respuesta. Todos los canales."**
- **Metáfora visual:** canales que convergen en **un sol/punto único** → del que sale **una sola voz** + un
  **ciclo** que vuelve al playbook.
- **Tono:** confiable, bancario, humano, boliviano. · **Paleta:** índigo (confianza) + naranja/dorado sol
  (BancoSol) + acento que conecte los canales.

---

## 16. Entregables del hackathon (checklist TDR)
- [ ] Repositorio de código + README (instalación, dependencias, licencias)
- [ ] Demo funcional navegable (golden path §12)
- [ ] Pitch deck 6–8 slides (problema · solución · valor · demo · KPIs · próximos pasos · necesidades)
- [ ] Mockups de alta fidelidad de las 2 vistas + status page
- [ ] Licencias abiertas (MIT/Apache-2.0) en componentes genéricos

---

## Anexos / fuentes del proyecto
- `SolUno_Banco_de_Datos.md` — todos los datos con URLs de fuente.
- `AUDIO-2026-05-30-09-46-35_transcripcion.txt` — 1ª reunión con BancoSol.
- `audio_nuevo_transcripcion.txt` — sesión de recomendaciones del banco.
- `audio_3_transcripcion.txt` — validación del Problem Statement.
- `BancoSol problema.pdf` — ficha oficial del reto.
- `INNOVAHACK2026_GuiaRapida_Consuelo (1).pdf` — metodología Cyclix.
- `TDRequipos (2).pdf` — bases y rúbrica del InnovaHack.
- `Categorización simple (1).xlsx` — taxonomía operativa de incidencias.

==========================================================================================
ARCHIVO: SolUno_Banco_de_Datos.md
==========================================================================================
# 📊 SolUno — Banco de Datos para el Pitch
### Todos los datos con su fuente · InnovaHack 2026
*Compilado el 30 de mayo de 2026 · Convención: ✅ dato duro (con fuente) · ⚠️ verificar antes de publicar · 📊 benchmark/estimación*

> El banco NO entrega datos reales (son sensibles y nos pidieron asumirlos). Estos números públicos y
> verificables sirven para construir supuestos creíbles y respaldar la presentación con fuentes.

---

## 1. La empresa — BancoSol (da escala al problema)

| Dato | Cifra | Tipo | Fuente |
|---|---|---|---|
| Clientes totales | +2,5 millones (duplicó base en 2 años) | ✅ | bancosol.com.bo (CAMEL 2025) |
| Puntos de atención | +1.801 | ✅ | bancosol.com.bo |
| Ranking CAMEL | #1 del país, 10 años consecutivos (2.29) | ✅ | bancosol.com.bo |
| Cartera de crédito | USD 2.712 millones | ✅ | bancosol.com.bo |
| Prestatarios/emprendedores | +373 mil | ✅ | economy.com.bo |
| Clientes mujeres | 50% · cartera a mujeres 45,3% | ✅ | bancosol.com.bo |
| Colaboradores | 4.393 (mayor empleador privado del sistema financiero) | ✅ | bancosol.com.bo |
| Bonos sociales de género (Avanza Mujer) | USD 52 millones (único banco privado de LATAM con 2 emisiones) | ✅ | IDB Invest |
| Mujeres financiadas | 172.076 por USD 1.228 millones | ✅ | prensa / Avanza Mujer |

**Fuentes:**
- https://www.bancosol.com.bo/noticias/bancosol-consolida-10-anos-consecutivos-como-el-banco-mas-solido-del-pais-segun-el-ranking-camel-2025
- https://www.bancosol.com.bo/avanza-mujer
- https://www.bancosol.com.bo/puntos-de-atencion
- https://www.economy.com.bo/articulo/banca-finanzas/bancosol-lidera-ranking-camel-consolida-decada-solidez-solvencia/20260414182254022269.amp.html
- https://idbinvest.org/en/news-media/bancosol-and-idb-invest-promote-businesses-led-women-through-first-gender-bond-bolivia

---

## 2. altoke — el detonante del problema

| Dato | Cifra | Tipo | Fuente |
|---|---|---|---|
| Lanzamiento | Mayo 2024 (creado en 4 meses) | ✅ | bancosol.com.bo |
| Usuarios | 800 mil (1er año) → 1,2 millones (oct 2025) | ✅ | Sensedia / urgente.bo |
| % de la PEA de Bolivia | 12% | ✅ | Sensedia |
| 🌟 Aumento de tráfico de API | **+2.000% en menos de 6 meses** | ✅ | Sensedia (fuente original) |
| Apertura de cuentas | 20.000–25.000 por semana | ✅ | Sensedia |
| Transacciones | +35M (1er año) → +68M (oct 2025) | ✅ | Sensedia / urgente.bo |
| Reconocimiento | Premio Oro Inclusión Financiera, Fintech Americas 2026 | ✅ | prensa |

**Fuentes:**
- https://www.sensedia.com.es/post/innovacion-tecnologica-con-apis-3-ejemplos-de-companias-que-potenciaron-su-negocio-con-sensedia  ← (+2.000% API y 20–25k cuentas/semana)
- https://www.urgente.bo/noticia/mas-de-1-2-millones-de-usuarios-realizan-cobros-y-pagos-por-qr-con-altoke
- https://www.bancosol.com.bo/altoke/que-es-altoke
- https://www.vision360.bo/noticias/2024/05/16/4506-bancosol-lanza-altoke-una-solucion-digital-para-facilitar-a-los-negocios-los-pagos-y-cobros-a-traves-qr

---

## 3. El mercado — Bolivia es digital y paga por QR

| Dato | Cifra | Tipo | Fuente |
|---|---|---|---|
| Población Bolivia | 11.365.333 | ✅ | Censo 2024 (INE/UNFPA) |
| PEA | 5.985.494 | ✅ | INE |
| Penetración internet móvil | 99% · 12,1M líneas | ✅ | ATT |
| Usuarios de internet | 9,14 millones (73,3%) | ✅ | DataReportal/ATT |
| Pagos por QR | +4.700% en menos de 3 años | ✅ | BCB |
| QR sobre pagos digitales | 92% (9 de cada 10) | ✅ | BCB / ASFI |
| Valor operaciones QR | ×48 entre 2021–2024 (Bs 3.195M → Bs 156.926M) | ✅ | prensa (BCB) |
| Entidades reguladas por ASFI | 14 bancos múltiples + 3 PYME (~51 entidades) | ✅ | ASFI |

**Fuentes:**
- https://bolivia.unfpa.org/sites/default/files/pub-pdf/2025-10/1_BOLETIN%20CENSO%202024.pdf
- https://www.ine.gob.bo/index.php/poblacion-economicamente-activa-representa-611-de-la-poblacion-en-edad-de-trabajar/
- https://www.att.gob.bo/sites/default/files/archivos_listados_pdf/2025-10-28/Estado%20de%20situacion%20de%20las%20telecomunicaciones%20en%20Bolivia%202024.pdf
- https://rtpbolivia.com.bo/economia/bcb-destaca-que-los-pagos-por-qr-en-el-pais-crecieron-en-4-700-en-menos-de-tres-anos/
- https://www.economy.com.bo/articulo/banca-finanzas/qr-concentro-92-pagos-digitales-bolivia-primer-semestre-2025/20250819182503019791.html
- https://www.asfi.gob.bo/sites/default/files/2025-09/Listado%20general%20de%20las%20entidades%20de%20intermediaci%C3%B3n%20financiera%20con%20licencia%20de%20funcionamiento.pdf

---

## 4. El problema cuantificado

### 4.1 Regulación ASFI (presión legal)
| Dato | Cifra | Tipo | Fuente |
|---|---|---|---|
| Plazo legal de respuesta a reclamo | 5 días hábiles | ✅ | Reglamento ASFI |
| 2ª instancia (Defensoría DCF) | 15 días hábiles | ✅ | ASFI |
| Botón "Punto de Reclamo" obligatorio | en web, app y canales electrónicos | ✅ | Reglamento ASFI |
| Reclamos en 1ª instancia (2024) | 75.805 | ⚠️ | ASFI CIRS (descargar PDF para confirmar) |
| └ Tarjetas de débito | 33.303 (43,9%) | ⚠️ | ASFI |
| └ Banca por internet | 5.199 (6,9%) · Atención al cliente 6.830 (9,0%) | ⚠️ | ASFI |
| Reclamos 1er semestre 2025 | 31.472 | ⚠️ | ASFI |
| 2ª instancia 2023 favorables al consumidor | 64% (860 de 1.339) | ✅ | ASFI |
| Denuncias a la DCF | 20–30/día, 80% por conciliación | ⚠️ | prensa (URL caída, reverificar) |
| Régimen sancionatorio | desde amonestación/multa hasta revocatoria de licencia | ✅ | DS 4755 (Lexivox) |

**Fuentes:**
- https://www.asfi.gob.bo/educacionfinanciera/Punto_Reclamo.html
- https://servdmzw.asfi.gob.bo/Textos/L04T01.pdf
- https://www.asfi.gob.bo/index.php/inf-interes-dcf/reclamos-recibidos.html
- https://www.asfi.gob.bo/pb/estadisticas-reclamos
- https://www.lexivox.org/norms/BO-RE-DS-N4755.html

### 4.2 Call Center (cuello de botella)
| Dato | Cifra | Tipo | Fuente |
|---|---|---|---|
| Costo por llamada entrante | USD 2,70–7,16 | 📊 | MaestroQA / CXToday |
| Llamadas que son repetidas | hasta 32% | ✅ | Balto / Enghouse |
| Deflection con IA bien hecha | 40–80% del volumen | 📊 | Alhena AI |
| Ahorro IA vs agente humano | ~USD 0,40 vs USD 7–12 por interacción | 📊 | Gartner (vía Ringly) |
| AHT objetivo | 6–8 minutos | 📊 | Insight7 / SQM |
| Clientes que esperan respuesta instantánea | 66% | ✅ | LiveChat |

**Fuentes:**
- https://www.maestroqa.com/blog/call-center-cost-per-call
- https://www.cxtoday.com/contact-center/how-to-calculate-your-cost-per-inbound-call-and-why/
- https://www.balto.ai/blog/how-does-reducing-repeat-calls-improve-customer-experience/
- https://alhena.ai/blog/what-is-deflection-rate/
- https://www.ringly.io/blog/call-center-statistics-2026
- https://www.livechat.com/success/live-chat-average-response-time/

### 4.3 Devs haciendo soporte (Stripe Developer Coefficient, 2018 — verificado en PDF primario)
| Dato | Cifra | Tipo | Fuente |
|---|---|---|---|
| Horas/semana de dev en mantenimiento | 17,3 h de 41,1 | ✅ | Stripe |
| Pérdida de eficiencia del dev | 31,6% | ✅ | Stripe |
| Legacy/deuda técnica como freno #1 | 52% de los devs | ✅ | Stripe |
| Pérdida global de PIB por ineficiencia del dev | ~USD 300.000 millones/año | ✅ | Stripe |

**Fuente:** https://stripe.com/files/reports/the-developer-coefficient.pdf

### 4.4 Costo de las caídas (incidentes graves)
| Dato | Cifra | Tipo | Fuente |
|---|---|---|---|
| Costo de 1 hora de caída | +USD 300.000 (90% de empresas) · 41% >USD 1M | ✅ | ITIC 2024 |
| Costo horario en banca/finanzas | +USD 5 millones (promedio sectores clave) | 📊 | ITIC 2024 |
| Downtime anual en servicios financieros | USD 152 millones/año | ✅ | Splunk / Oxford Economics |

**Fuentes:**
- https://itic-corp.com/itic-2024-hourly-cost-of-downtime-part-2/
- https://thenetworkinstallers.com/blog/cost-of-it-downtime-statistics/

---

## 5. La oportunidad — por qué nuestra solución gana

| Dato | Cifra | Tipo | Fuente |
|---|---|---|---|
| Esperan misma respuesta en todos los canales | 75% | ✅ | Zendesk CX Trends 2024 |
| Deben repetir su info a distintos agentes | 66% | ✅ | Salesforce State of Service |
| Esperan empezar en un canal y terminar en otro sin repetir | 73% | ✅ | Salesforce |
| Más leales si hay consistencia entre departamentos | 83% | ✅ | Salesforce |
| GenAI en atención = ahorro | 30–45% de los costos de la función | ✅ | McKinsey |
| Valor de GenAI en banca | USD 200–340 mil millones/año | ✅ | McKinsey |
| Caso GenAI: resolución/hora +14%, AHT −9%, escalamientos −25% | — | ✅ | McKinsey |
| Solo se resuelve en autoservicio | 14% | ✅ | Gartner |
| Caso Zendesk: 8.000 tickets deflectados, FRT +83%, ahorro USD 1,3M | — | ✅ | Zendesk |

**Fuentes:**
- https://www.zendesk.com/blog/cx-trends-2024/
- https://www.salesforce.com/resources/research-reports/state-of-service/
- https://www.mckinsey.com/industries/financial-services/our-insights/capturing-the-full-value-of-generative-ai-in-banking
- https://www.mckinsey.com/capabilities/operations/our-insights/from-promising-to-productive-real-results-from-gen-ai-in-services
- https://www.cxtoday.com/contact-center/only-1-in-7-customer-service-queries-resolved-with-self-service-gartner-study-finds/

### Privacidad / tokenización (justifica "todo se queda en el banco")
| Dato | Cifra | Tipo | Fuente |
|---|---|---|---|
| Empleados que pegaron datos confidenciales en ChatGPT | 4,7% · 11% de lo enviado era confidencial | ✅ | Kiteworks |
| Secretos/API keys filtrados en datasets de entrenamiento | ~12.000 en un solo rastreo | ✅ | Truffle Security (2025) |
| Mitigación recomendada | tokenización/anonimización (PCI-DSS, ISO 27001, GDPR) | 📊 | Strac / Firstsource |

**Fuentes:**
- https://www.kiteworks.com/cybersecurity-risk-management/prevent-llm-data-leakage-controls/
- https://www.strac.io/blog/avoid-passing-customer-pii-openai
- https://www.firstsource.com/insights/blogs/when-privacy-meets-performance-smarter-way-handle-pii-llms

---

## 6. El plus social — idiomas y área rural

| Dato | Cifra | Tipo | Fuente |
|---|---|---|---|
| Hablantes de quechua | 1,39 millones | ✅ | Censo 2024 (INE) |
| Hablantes de aymara | 774.874 | ✅ | Censo 2024 |
| Hablantes de guaraní | 43.870 | ✅ | Censo 2024 |
| Lenguas originarias reconocidas | 36 (30+ en riesgo) | ✅ | Censo 2024 |
| Población indígena (proyección) | ~48% | 📊 | INE 2017 |

**Fuentes:**
- https://www.la-epoca.com.bo/2025/09/07/censo-2024-el-castellano-quechua-y-aymara-predominan-mientras-30-lenguas-estan-al-borde-de-la-desaparicion/
- https://cpv2024.ine.gob.bo/

---

## ⚠️ Datos que NO usar (sin fuente — no inventar)
1. Monto en Bs transaccionado solo por altoke (solo hay nº de operaciones).
2. % exacto de población rural y brecha digital rural (cuantitativo).
3. Multa puntual por incumplir el plazo de 5 días.
4. MTTR numérico específico para banca boliviana.
5. % de churn bancario atribuible a incidencias.
6. ATMs de BancoSol como cifra separada.

---

## 🎤 Narrativa sugerida con los datos
1. **Escala:** BancoSol = 2,5M clientes, #1 CAMEL 10 años.
2. **Detonante:** altoke explotó: +2.000% de API en 6 meses.
3. **El dolor:** 32% de llamadas repetidas + devs pierden 17h/sem en soporte + caída = +USD 300K/hora.
4. **Presión legal:** ASFI exige 5 días; 75.805 reclamos en 2024 (⚠ verificar).
5. **Causa raíz:** 75% espera misma respuesta en todo canal, pero el banco no la da.
6. **Solución (SolUno):** GenAI ahorra 30–45% en atención, con privacidad por tokenización.
7. **Plus social:** 2,2M de bolivianos hablan lenguas originarias.

---

## 7. Datos de prueba — nombres reales de agencias BancoSol
*(Extraídos del archivo interno `BancoSol SA - Santa Cruz.xlsm` — usar para que los datos simulados del MVP suenen reales y locales, ej. "incidente reportado desde la agencia Plan 3000".)*

**Santa Cruz:** Regional Casco Viejo · Virgen de Luján · Plan 3000 · El Quior · El Pari · Mutualista · Villa 1ero de Mayo · Regional Cristo Redentor · Alto San Pedro · Piraí

**Cochabamba:** BancoSol Cochabamba · Muyurina · América · Cruce Taquiña · Colcapirhua · Quillacollo · Sacaba · Villa Pagador · Panamericana · San Martín · Regional Esteban Arce

> Nota: el archivo de origen es una planilla de **gestión de residuos/reciclaje** de las agencias (otro dominio, NO incidencias). Solo aprovechamos los nombres de agencias como datos de prueba. También sirve como evidencia de que BancoSol gestiona procesos en **Excel/herramientas informales** (refuerza el argumento del problema).

==========================================================================================
ARCHIVO: SolUno_n8n_guia.md
==========================================================================================
# 🔧 SolUno — Guía del Workflow n8n (v1, reglas)

Archivo a importar: `SolUno_n8n_workflow.json`

## Cómo levantar n8n (local, sin Docker)
```
npx n8n start
```
Abrir: http://localhost:5678 (la primera vez se crea el usuario owner).

## Cómo importar el workflow
1. En n8n: menú **⋯ (arriba derecha) → Import from File**.
2. Elegí `SolUno_n8n_workflow.json`.
3. Guardá (Save) y activá (Active) o usá "Test workflow".

## Los nodos (paso a paso)

| Nodo | Qué hace |
|---|---|
| **1. Webhook (Ingesta)** | Recibe `POST` a `/webhook/soluno-reporte` con `{canal, producto, mensaje, cliente:{nombre,ci,cuenta,telefono}}` |
| **2. Tokenizar (PII)** | Reemplaza nombre/CI/cuenta/teléfono por tokens; conserva nombre+CI visibles solo para el revisor |
| **3. Clasificar (reglas)** | Por palabras clave: queja vs incidente, producto, categoría, severidad |
| **4. ¿Es Incidente?** | Bifurca: incidente → 5a · queja → 5b |
| **5a. Paquete de Incidente** | Arma paquete para Desarrollo: qué falla, # afectados, datos para reproducir, equipo, estado |
| **5b. Respuesta Queja (Playbook)** | Trae la misma respuesta + pasos del playbook según categoría |
| **6. Responder** | Devuelve el JSON final |

## Ejemplos para probar (POST al webhook)

**Incidente:**
```json
{"canal":"WhatsApp","producto":"altoke","agencia":"Plan 3000",
 "mensaje":"no puedo transferir por QR, me da error",
 "cliente":{"nombre":"Juana Mamani","ci":"8472351","cuenta":"4001-2289","telefono":"700123"}}
```
Esperado: `INC-xxxxx`, equipo "Equipo Pagos/QR", estado "En análisis".

**Queja:**
```json
{"canal":"Instagram","producto":"appSol",
 "mensaje":"cambié de celular y no me deja iniciar sesión",
 "cliente":{"nombre":"Luis Roca","ci":"123","telefono":"701"}}
```
Esperado: `QJA-xxxxx`, categoría "acceso", pasos de reactivación de tokens.

## Próximos workflows (roadmap)
- **#2**: agrupar incidentes iguales y **contar afectados** (Postgres / pgvector).
- **#3**: reemplazar la clasificación por reglas con **Claude** (nodo AI / HTTP a la API de Anthropic).
- **#4**: **cierre del ciclo** → publicar la solución al Playbook Vivo (versionado).
- **#5**: **informe ASFI** automático (plus).
- **Frontend**: 2 vistas + status (3 estados) + métricas (React / v0 / Lovable) consumiendo estos webhooks.

## Nota de seguridad
La API key de Anthropic (cuando sumemos Claude) se pega **dentro de n8n** (Credentials), nunca en el chat ni en el código del repo.

==========================================================================================
ARCHIVO: generar_informe.py
==========================================================================================
# -*- coding: utf-8 -*-
"""
Genera el INFORME PROFESIONAL de SolUno (InnovaHack 2026) en Word (.docx).
Diseno con identidad BancoSol (morado/naranja). Incluye portada, indice,
todas las secciones, tablas, callouts y figuras (diagrama de arquitectura
generado + captura real del workflow de n8n).
"""
import os
from docx import Document
from docx.shared import RGBColor, Pt, Inches
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.enum.table import WD_TABLE_ALIGNMENT
from docx.enum.style import WD_STYLE_TYPE
from docx.oxml.ns import qn
from docx.oxml import OxmlElement

BASE = os.path.dirname(os.path.abspath(__file__))

# ---------------- Paleta BancoSol ----------------
MORADO     = RGBColor(0x5B, 0x2A, 0x86)
MORADO_OSC = RGBColor(0x3D, 0x1C, 0x59)
NARANJA    = RGBColor(0xF5, 0x82, 0x20)
TINTA      = RGBColor(0x23, 0x23, 0x28)
GRIS_MEDIO = RGBColor(0x6B, 0x6B, 0x73)
GRIS_LINEA = RGBColor(0xD9, 0xD9, 0xDE)
BLANCO     = RGBColor(0xFF, 0xFF, 0xFF)
FUENTE = "Calibri"

# ===================================================================
#  Figuras con Pillow: banner degradado + diagrama de arquitectura
# ===================================================================
def generar_imagenes():
    from PIL import Image, ImageDraw, ImageFont
    def font(sz, bold=False):
        try:
            return ImageFont.truetype("arialbd.ttf" if bold else "arial.ttf", sz)
        except Exception:
            return ImageFont.load_default()

    # --- Banner degradado amanecer (morado -> magenta -> naranja) ---
    W, H = 1700, 470
    banner = Image.new("RGB", (W, H))
    px = banner.load()
    c1, c2, c3 = (91, 42, 134), (200, 30, 120), (245, 130, 32)
    for x in range(W):
        t = x / (W - 1)
        if t < 0.5:
            k = t / 0.5; r, g, b = [int(c1[i] + (c2[i] - c1[i]) * k) for i in range(3)]
        else:
            k = (t - 0.5) / 0.5; r, g, b = [int(c2[i] + (c3[i] - c2[i]) * k) for i in range(3)]
        for y in range(H):
            px[x, y] = (r, g, b)
    banner.save(os.path.join(BASE, "_banner.png"))

    # --- Diagrama de arquitectura (pipeline horizontal) ---
    W, H = 1760, 760
    img = Image.new("RGB", (W, H), (255, 255, 255))
    d = ImageDraw.Draw(img)
    PUR = (91, 42, 134); ORA = (245, 130, 32); GRY = (107, 100, 120); LINE = (210, 200, 225)

    def chip(x, y, w, h, title, sub, fill=PUR):
        d.rounded_rectangle([x, y, x + w, y + h], radius=18, fill=fill)
        d.rounded_rectangle([x, y, x + w, y + h], radius=18, outline=(255, 255, 255), width=2)
        tb = d.textbbox((0, 0), title, font=font(26, True))
        d.text((x + (w - (tb[2] - tb[0])) / 2, y + h / 2 - 26), title, font=font(26, True), fill=(255, 255, 255))
        if sub:
            sb = d.textbbox((0, 0), sub, font=font(18))
            d.text((x + (w - (sb[2] - sb[0])) / 2, y + h / 2 + 6), sub, font=font(18), fill=(240, 230, 250))

    def arrow(x1, y1, x2, y2):
        d.line([x1, y1, x2, y2], fill=ORA, width=5)
        d.polygon([(x2, y2), (x2 - 14, y2 - 7), (x2 - 14, y2 + 7)], fill=ORA)

    d.text((60, 40), "Arquitectura de SolUno", font=font(34, True), fill=PUR)
    bw, bh, ty = 250, 110, 150
    xs = [60, 365, 670, 975, 1280]
    labels = [("Cliente", "WhatsApp"), ("Meta", "Cloud API + ngrok"), ("Backend", "+ Consola web"),
              ("n8n", "Orquestador"), ("IA · Groq", "clasifica + responde")]
    for i, (x, (t, s)) in enumerate(zip(xs, labels)):
        chip(x, ty, bw, bh, t, s, fill=PUR if i != 4 else (123, 46, 150))
        if i < len(xs) - 1:
            arrow(x + bw + 6, ty + bh / 2, xs[i + 1] - 6, ty + bh / 2)
    # respuesta de vuelta
    d.line([xs[4] + bw / 2, ty + bh, xs[4] + bw / 2, ty + bh + 60], fill=LINE, width=4)
    d.line([xs[4] + bw / 2, ty + bh + 60, xs[0] + bw / 2, ty + bh + 60], fill=LINE, width=4)
    arrow(xs[0] + bw / 2, ty + bh + 60, xs[0] + bw / 2, ty + bh + 8)
    d.text((xs[2] - 40, ty + bh + 30), "Respuesta enumerada al cliente (queja)  ·  estado del caso (incidente)",
           font=font(18), fill=GRY)
    # branch a equipos
    eqy = 470
    d.line([xs[3] + bw / 2, ty + bh, xs[3] + bw / 2, eqy], fill=ORA, width=5)
    arrow(xs[3] + bw / 2, eqy, xs[3] + bw / 2, eqy)
    eq = ["Equipo Pagos/QR", "Seguridad/Accesos", "DevOps Aplicaciones", "Infraestructura/TI", "Mesa de Incidentes"]
    ew = 320
    d.rounded_rectangle([60, eqy + 10, 60 + ew * 0 + 1640, eqy + 150], radius=18, outline=PUR, width=3)
    d.text((90, eqy + 26), "Derivacion de INCIDENCIAS a los 5 equipos de TI (segun categoria y severidad)",
           font=font(22, True), fill=PUR)
    ex = 90
    for e in eq:
        d.rounded_rectangle([ex, eqy + 76, ex + 300, eqy + 130], radius=12, fill=(241, 233, 250))
        tb = d.textbbox((0, 0), e, font=font(18, True))
        d.text((ex + (300 - (tb[2] - tb[0])) / 2, eqy + 95), e, font=font(18, True), fill=PUR)
        ex += 312
    img.save(os.path.join(BASE, "_arquitectura.png"))
    print("Imagenes generadas.")

# ===================================================================
#  Helpers python-docx
# ===================================================================
def shade(cell, hexcolor):
    tcPr = cell._tc.get_or_add_tcPr()
    shd = OxmlElement('w:shd'); shd.set(qn('w:val'), 'clear'); shd.set(qn('w:color'), 'auto'); shd.set(qn('w:fill'), hexcolor)
    tcPr.append(shd)

def no_borders(table):
    tblPr = table._tbl.tblPr
    b = OxmlElement('w:tblBorders')
    for edge in ('top', 'left', 'bottom', 'right', 'insideH', 'insideV'):
        e = OxmlElement('w:' + edge); e.set(qn('w:val'), 'none'); b.append(e)
    tblPr.append(b)

def run_style(run, size=11, bold=False, italic=False, color=TINTA, name=FUENTE):
    run.font.name = name; run.font.size = Pt(size); run.font.bold = bold; run.font.italic = italic; run.font.color.rgb = color
    rpr = run._element.get_or_add_rPr(); rf = rpr.find(qn('w:rFonts'))
    if rf is None:
        rf = OxmlElement('w:rFonts'); rpr.append(rf)
    for a in ('w:ascii', 'w:hAnsi', 'w:cs'):
        rf.set(qn(a), name)

def filete(paragraph, hexcolor, size_pt=6):
    pPr = paragraph._p.get_or_add_pPr(); pbdr = OxmlElement('w:pBdr'); bottom = OxmlElement('w:bottom')
    bottom.set(qn('w:val'), 'single'); bottom.set(qn('w:sz'), str(int(size_pt * 8)))
    bottom.set(qn('w:space'), '4'); bottom.set(qn('w:color'), hexcolor); pbdr.append(bottom); pPr.append(pbdr)

def page_number(paragraph):
    run = paragraph.add_run()
    f1 = OxmlElement('w:fldChar'); f1.set(qn('w:fldCharType'), 'begin')
    instr = OxmlElement('w:instrText'); instr.set(qn('xml:space'), 'preserve'); instr.text = 'PAGE'
    f2 = OxmlElement('w:fldChar'); f2.set(qn('w:fldCharType'), 'end')
    run._r.append(f1); run._r.append(instr); run._r.append(f2); run_style(run, size=8.5, color=GRIS_MEDIO)

def H1(doc, texto):
    p = doc.add_paragraph(style='Heading 1'); r = p.add_run(texto); run_style(r, size=20, bold=True, color=MORADO)
    p.paragraph_format.space_before = Pt(18); p.paragraph_format.space_after = Pt(6); filete(p, 'F58220', 6); return p

def H2(doc, texto):
    p = doc.add_paragraph(style='Heading 2'); r = p.add_run(texto); run_style(r, size=15, bold=True, color=MORADO)
    p.paragraph_format.space_before = Pt(12); p.paragraph_format.space_after = Pt(4); return p

def body(doc, texto):
    p = doc.add_paragraph(); r = p.add_run(texto); run_style(r, size=11, color=TINTA)
    p.paragraph_format.line_spacing = 1.18; p.paragraph_format.space_after = Pt(7); return p

def bullet(doc, lead, texto=""):
    p = doc.add_paragraph(style='List Bullet')
    r = p.add_run(lead); run_style(r, size=11, bold=True, color=MORADO_OSC)
    if texto:
        r2 = p.add_run(" " + texto); run_style(r2, size=11, color=TINTA)
    p.paragraph_format.space_after = Pt(3); p.paragraph_format.line_spacing = 1.12; return p

def callout(doc, titulo, texto, borde='F58220', fondo='FEF1E6'):
    t = doc.add_table(rows=1, cols=1); c = t.cell(0, 0); shade(c, fondo)
    tcPr = c._tc.get_or_add_tcPr(); bdr = OxmlElement('w:tcBorders'); left = OxmlElement('w:left')
    left.set(qn('w:val'), 'single'); left.set(qn('w:sz'), '24'); left.set(qn('w:space'), '0'); left.set(qn('w:color'), borde)
    bdr.append(left); tcPr.append(bdr)
    p = c.paragraphs[0]; r = p.add_run(titulo + "\n"); run_style(r, size=10.5, bold=True, color=MORADO)
    r2 = p.add_run(texto); run_style(r2, size=10, color=TINTA)
    doc.add_paragraph().paragraph_format.space_after = Pt(4); return t

def figura(doc, ruta, ancho=6.0, num=1, cap=""):
    if not os.path.exists(ruta):
        return
    p = doc.add_paragraph(); p.alignment = WD_ALIGN_PARAGRAPH.CENTER
    p.paragraph_format.space_before = Pt(8)
    p.add_run().add_picture(ruta, width=Inches(ancho))
    c = doc.add_paragraph(); c.alignment = WD_ALIGN_PARAGRAPH.CENTER
    r1 = c.add_run("Figura %d. " % num); run_style(r1, size=9, bold=True, italic=True, color=MORADO)
    r2 = c.add_run(cap); run_style(r2, size=9, italic=True, color=GRIS_MEDIO)
    c.paragraph_format.space_after = Pt(12)

def tabla(doc, headers, filas):
    t = doc.add_table(rows=1, cols=len(headers)); t.alignment = WD_TABLE_ALIGNMENT.CENTER; t.style = 'Table Grid'
    for i, h in enumerate(headers):
        c = t.rows[0].cells[i]; shade(c, '5B2A86'); c.paragraphs[0].text = ''
        r = c.paragraphs[0].add_run(h); run_style(r, size=10, bold=True, color=BLANCO)
    for ri, fila in enumerate(filas, start=1):
        row = t.add_row()
        for ci, val in enumerate(fila):
            cl = row.cells[ci]
            if ri % 2 == 0:
                shade(cl, 'F4F1F7')
            cl.paragraphs[0].text = ''
            r = cl.paragraphs[0].add_run(str(val)); run_style(r, size=10, color=TINTA)
    doc.add_paragraph().paragraph_format.space_after = Pt(6)
    return t

# ===================================================================
#  Documento
# ===================================================================
def build():
    doc = Document()
    # estilos base
    normal = doc.styles['Normal']; normal.font.name = FUENTE; normal.font.size = Pt(11); normal.font.color.rgb = TINTA
    for nm, sz, col in [('Heading 1', 20, MORADO), ('Heading 2', 15, MORADO), ('Heading 3', 12.5, TINTA)]:
        st = doc.styles[nm]; st.font.name = FUENTE; st.font.size = Pt(sz); st.font.bold = True; st.font.color.rgb = col
    sec = doc.sections[0]
    sec.top_margin = Inches(1.0); sec.bottom_margin = Inches(1.0); sec.left_margin = Inches(1.1); sec.right_margin = Inches(1.1)
    sec.different_first_page_header_footer = True
    # pie con numeracion
    fp = sec.footer.paragraphs[0]; fp.alignment = WD_ALIGN_PARAGRAPH.RIGHT
    r = fp.add_run("SolUno · BancoSol · InnovaHack 2026   |   "); run_style(r, size=8.5, color=GRIS_MEDIO); page_number(fp)

    # ---------------- PORTADA ----------------
    if os.path.exists(os.path.join(BASE, "_banner.png")):
        pb = doc.add_paragraph(); pb.alignment = WD_ALIGN_PARAGRAPH.CENTER
        pb.add_run().add_picture(os.path.join(BASE, "_banner.png"), width=Inches(6.5))
    sp = doc.add_paragraph(); sp.paragraph_format.space_before = Pt(40)
    t = doc.add_paragraph(); r = t.add_run("SolUno"); run_style(r, size=44, bold=True, color=MORADO_OSC)
    s = doc.add_paragraph(); r = s.add_run("Centro de operaciones de atención al cliente con Inteligencia Artificial para BancoSol")
    run_style(r, size=16, color=GRIS_MEDIO)
    # pill evento
    pt = doc.add_table(rows=1, cols=1); pt.alignment = WD_TABLE_ALIGNMENT.LEFT
    pt.columns[0].width = Inches(2.2); cl = pt.cell(0, 0); shade(cl, 'F58220'); cl.paragraphs[0].text = ''
    rr = cl.paragraphs[0].add_run("  InnovaHack 2026  "); run_style(rr, size=12, bold=True, color=BLANCO)
    meta = doc.add_paragraph(); meta.paragraph_format.space_before = Pt(28)
    r = meta.add_run("Informe técnico del proyecto  ·  31 de mayo de 2026  ·  Versión 1.0"); run_style(r, size=11, color=GRIS_MEDIO)
    doc.add_page_break()

    # ---------------- INDICE ----------------
    H1(doc, "Índice")
    p = doc.add_paragraph(); run = p.add_run()
    f1 = OxmlElement('w:fldChar'); f1.set(qn('w:fldCharType'), 'begin')
    ins = OxmlElement('w:instrText'); ins.set(qn('xml:space'), 'preserve'); ins.text = 'TOC \\o "1-2" \\h \\z \\u'
    f2 = OxmlElement('w:fldChar'); f2.set(qn('w:fldCharType'), 'separate')
    tt = OxmlElement('w:t'); tt.text = "Hacé clic derecho aquí y elegí “Actualizar campos” para generar el índice."
    f3 = OxmlElement('w:fldChar'); f3.set(qn('w:fldCharType'), 'end')
    for el in (f1, ins, f2, tt, f3):
        run._r.append(el)
    doc.add_page_break()

    # ---------------- RESUMEN EJECUTIVO ----------------
    H1(doc, "Resumen ejecutivo")
    body(doc, "SolUno es un sistema de atención al cliente impulsado por inteligencia artificial que, a través de "
              "WhatsApp, recibe, clasifica y resuelve de forma autónoma las quejas de los clientes de BancoSol, y "
              "deriva automáticamente las incidencias al equipo de TI correspondiente según su categoría y severidad. "
              "Orquestado con n8n y operado desde una consola web de soporte, incorpora un catálogo de problemas con "
              "runbooks paso a paso y genera informes regulatorios para ASFI de manera automática, editable y trazable.")
    body(doc, "Con esto, BancoSol reduce el tiempo de primera respuesta de horas a segundos, libera a su equipo de "
              "tareas repetitivas y asegura el cumplimiento normativo sin esfuerzo manual, disponible 24/7. SolUno "
              "convierte la atención al cliente en un proceso ágil, auditable y escalable, mejorando la experiencia del "
              "usuario y la eficiencia operativa del banco.")
    callout(doc, "En una frase",
            "Un agente de IA por WhatsApp que resuelve solo las quejas, deriva las incidencias al equipo correcto y deja todo documentado para ASFI.")

    # ---------------- PROBLEMATICA ----------------
    H1(doc, "Problemática y contexto")
    body(doc, "BancoSol recibe un alto volumen de mensajes de clientes por WhatsApp en los que se mezclan dos tipos de "
              "situaciones muy distintas: quejas puntuales con solución conocida (por ejemplo, un cambio de dispositivo "
              "o un pago que aún figura como deuda) e incidencias críticas y generalizadas (caída del QR, del login o de "
              "la app). Atender todo manualmente genera los siguientes puntos de dolor:")
    bullet(doc, "Demoras:", "el tiempo de primera respuesta depende de la disponibilidad de un operador humano.")
    bullet(doc, "Falta de clasificación:", "no se distingue rápido una queja de una incidencia ni se prioriza por gravedad.")
    bullet(doc, "Derivación confusa:", "no queda claro a qué equipo de TI debe ir cada incidencia.")
    bullet(doc, "Sin trazabilidad:", "no hay registro estructurado para auditoría ni para los reportes que exige ASFI.")
    bullet(doc, "Re-trabajo:", "problemas idénticos se resuelven desde cero una y otra vez, sin un manual vivo.")

    # ---------------- OBJETIVOS ----------------
    H1(doc, "Objetivos")
    H2(doc, "Objetivo general")
    body(doc, "Automatizar la recepción, clasificación, resolución y derivación de las quejas e incidencias de los "
              "clientes de BancoSol mediante un agente de inteligencia artificial conversacional integrado a WhatsApp.")
    H2(doc, "Objetivos específicos")
    for lead, txt in [
        ("Clasificar automáticamente", "cada mensaje como queja, incidencia o consulta."),
        ("Resolver las quejas de forma autónoma", "respondiendo al cliente con una solución enumerada paso a paso."),
        ("Derivar las incidencias", "al equipo de TI correcto y agruparlas por causa raíz, contando los afectados."),
        ("Calificar la severidad", "de cada incidencia (Leve, Medio, Urgente, Crítico)."),
        ("Mantener un catálogo de problemas vivo", "con runbooks paso a paso que se actualizan al resolver."),
        ("Generar informes ASFI", "editables, aprobables y trazables por cada caso resuelto."),
    ]:
        bullet(doc, lead, txt)

    # ---------------- SOLUCION ----------------
    H1(doc, "Solución propuesta")
    body(doc, "SolUno es un agente conversacional más una consola de operaciones para el equipo de soporte. Por cada "
              "mensaje de WhatsApp, el sistema entiende la intención del cliente, decide si puede resolverlo solo o si "
              "debe escalarlo, y actúa en consecuencia, manteniendo informado al cliente y dejando todo documentado.")
    body(doc, "La distinción clave es: las QUEJAS las resuelve la IA de forma autónoma (autoservicio guiado), mientras "
              "que las INCIDENCIAS se derivan al equipo de TI responsable. Como se observa en la Figura 1, todo el "
              "recorrido —desde el mensaje del cliente hasta la respuesta o la derivación— ocurre de forma automática.")
    figura(doc, os.path.join(BASE, "_arquitectura.png"), ancho=6.4, num=1,
           cap="Visión general de SolUno: del mensaje del cliente a la resolución o derivación.")

    # ---------------- ARQUITECTURA ----------------
    H1(doc, "Arquitectura técnica")
    body(doc, "SolUno se compone de los siguientes elementos, integrados de punta a punta:")
    bullet(doc, "Canal WhatsApp (Meta Cloud API):", "recibe y envía mensajes; expuesto de forma segura mediante un túnel (ngrok).")
    bullet(doc, "Backend + consola web:", "servidor liviano (Node.js) que conecta WhatsApp con n8n y sirve la consola del operador en tiempo real.")
    bullet(doc, "Orquestación con n8n:", "motor visual y auditable que ejecuta la lógica: clasificación, agrupación, enrutado y generación de respuestas.")
    bullet(doc, "Motor de IA (Groq · llama-3.3-70b):", "clasifica el mensaje y redacta la respuesta al cliente y la recomendación técnica para el equipo.")
    bullet(doc, "Catálogo de problemas (playbooks):", "runbooks paso a paso por tipo de problema, con código único (PRB-###) y auto-alta de problemas nuevos.")
    bullet(doc, "Módulo de informes ASFI:", "genera un documento por caso resuelto, editable y aprobable, archivado para su envío.")
    body(doc, "Se eligió n8n por ser una herramienta de orquestación visual, rápida de iterar y auditable: cada paso del "
              "flujo queda explícito y verificable. Por privacidad, los datos personales se tokenizan antes de enviarse a "
              "la IA, y ninguna credencial queda escrita en el código (se leen de variables de entorno). La Figura 2 "
              "muestra el workflow real implementado en n8n.")
    figura(doc, os.path.join(BASE, "n8n_workflow.png"), ancho=6.5, num=2,
           cap="Workflow real en n8n: clasificación, enrutado a equipos y cierre del ciclo (resolver → playbook vivo → informe ASFI).")

    # ---------------- FUNCIONALIDADES ----------------
    H1(doc, "Funcionalidades clave")
    for lead, txt in [
        ("Clasificación automática", "de cada conversación en queja, incidencia o consulta."),
        ("Resolución autónoma de quejas", "con respuesta enumerada (paso 1, 2, 3…), cálida y adaptada al caso."),
        ("Agrupación por causa raíz", "para unir reportes del mismo problema y contar los clientes afectados."),
        ("Severidad", "calificada en Leve, Medio, Urgente y Crítico, resaltada en la consola."),
        ("Derivación inteligente", "a uno de los cinco equipos de TI según categoría y severidad."),
        ("Centro de derivación", "que muestra, en vivo, cuántas incidencias activas tiene cada equipo."),
        ("Catálogo de playbooks", "con runbooks de 12 a 18 pasos basados en buenas prácticas de la industria (SRE/ITIL)."),
        ("Playbook vivo", "la solución del catálogo se actualiza automáticamente cada vez que el equipo resuelve un caso."),
        ("Informes ASFI", "generados por caso, editables y aprobables desde la propia consola."),
        ("Consola en tiempo real", "con bandeja, hilos de conversación, estados y diseño con identidad BancoSol."),
    ]:
        bullet(doc, lead, txt)

    # ---------------- FLUJO ----------------
    H1(doc, "Flujo de trabajo end-to-end")
    H2(doc, "Recorrido de una QUEJA (la resuelve la IA)")
    for i, txt in enumerate([
        "El cliente escribe por WhatsApp (ej.: “cambié de celular y no entro a la app”).",
        "La IA clasifica el mensaje como queja y consulta el playbook correspondiente.",
        "El agente responde al cliente con la solución enumerada paso a paso.",
        "La conversación queda registrada en la consola; al agradecer el cliente, el caso se cierra.",
    ], 1):
        bullet(doc, "%d." % i, txt)
    H2(doc, "Recorrido de una INCIDENCIA (se deriva a TI)")
    for i, txt in enumerate([
        "El cliente reporta una falla (ej.: “no puedo transferir, la app está caída”).",
        "La IA la clasifica como incidencia, calcula la severidad y la agrupa por causa raíz.",
        "n8n la deriva al equipo de TI correcto y le asigna un código de catálogo (PRB-###).",
        "El técnico ve el caso con su severidad, el equipo asignado y el runbook desplegado paso a paso.",
        "Al resolver, se avisa al cliente por WhatsApp, se actualiza el playbook y se genera el informe ASFI.",
    ], 1):
        bullet(doc, "%d." % i, txt)

    # ---------------- RESULTADOS ----------------
    H1(doc, "Resultados y demostración")
    body(doc, "El sistema está funcionando de punta a punta: un mensaje real de WhatsApp entra, la IA lo clasifica y "
              "responde, y el caso aparece en vivo en la consola del operador con su severidad, equipo y código de "
              "catálogo. La tabla siguiente resume el cambio respecto a la atención manual (valores objetivo / estimados).")
    tabla(doc, ["Indicador", "Atención manual (hoy)", "Con SolUno"], [
        ["Tiempo de primera respuesta", "Minutos a horas", "Segundos (automático)"],
        ["Clasificación y prioridad", "Manual, inconsistente", "Automática (tipo + severidad)"],
        ["Derivación al equipo correcto", "Manual / ambigua", "Automática a 5 equipos"],
        ["Resolución de quejas frecuentes", "Repetida desde cero", "Autónoma con playbook vivo"],
        ["Trazabilidad e informe ASFI", "Manual o inexistente", "Generado por caso, auditable"],
        ["Disponibilidad", "Horario de operadores", "24/7"],
    ])
    callout(doc, "Estado actual (MVP del hackathon)",
            "Flujo WhatsApp → IA → consola operativo; clasificación, severidad, derivación, catálogo PRB con runbooks, "
            "playbook vivo e informes ASFI implementados. Demo accesible en vivo durante la evaluación.")

    # ---------------- IMPACTO ----------------
    H1(doc, "Impacto y beneficios para BancoSol")
    H2(doc, "Para el cliente")
    bullet(doc, "Respuesta inmediata", "y guiada paso a paso, 24/7, por el canal que ya usa (WhatsApp).")
    H2(doc, "Operativos")
    bullet(doc, "Menos carga repetitiva", "el equipo se enfoca en lo que realmente requiere intervención humana.")
    bullet(doc, "Derivación correcta", "cada incidencia llega al equipo indicado, con contexto y solución sugerida.")
    H2(doc, "Regulatorios y económicos")
    bullet(doc, "Cumplimiento ASFI", "con informes automáticos y trazables, sin esfuerzo manual.")
    bullet(doc, "Escalabilidad", "atiende picos de demanda sin sumar personal proporcionalmente.")

    # ---------------- ASFI / SEGURIDAD ----------------
    H1(doc, "Cumplimiento regulatorio (ASFI) y seguridad")
    bullet(doc, "Informes ASFI:", "cada caso resuelto genera un documento con código único, contexto, datos del caso y solución, editable y aprobable.")
    bullet(doc, "Trazabilidad:", "cada queja e incidencia queda registrada con su clasificación, severidad, equipo y resolución.")
    bullet(doc, "Datos sensibles:", "la información personal se tokeniza antes de procesarse con IA.")
    bullet(doc, "Secretos protegidos:", "las credenciales se leen de variables de entorno; no se almacenan en el código ni en el repositorio.")

    # ---------------- ROADMAP ----------------
    H1(doc, "Próximos pasos (roadmap)")
    tabla(doc, ["Plazo", "Iniciativa"], [
        ["Corto", "Ampliar el catálogo de playbooks y publicar a producción (número y verificación de WhatsApp)."],
        ["Medio", "Integrar con el core bancario y con la mesa de ayuda; métricas y SLA por equipo."],
        ["Largo", "Analítica predictiva de incidencias, más canales (Facebook, Instagram, web) y soporte multilingüe."],
    ])

    # ---------------- CONCLUSIONES ----------------
    H1(doc, "Conclusiones")
    body(doc, "SolUno demuestra que es posible transformar la atención al cliente de BancoSol en un proceso ágil, "
              "autónomo y auditable. Resuelve hoy el problema concreto —clasificar, resolver y derivar— y deja la base "
              "para escalar con cumplimiento regulatorio incorporado. No es una idea: es un sistema funcionando, que un "
              "cliente puede usar por WhatsApp y que un técnico puede operar desde una consola en tiempo real.")

    # ---------------- ANEXOS ----------------
    H1(doc, "Anexos")
    H2(doc, "Accesos del proyecto")
    bullet(doc, "Repositorio:", "https://github.com/bookcubers2-ux/soluno-bancosol")
    bullet(doc, "Aplicación en vivo (consola del operador):", "https://security-affair-untrimmed.ngrok-free.dev")
    H2(doc, "Glosario")
    for term, d in [
        ("n8n", "herramienta de orquestación de flujos de trabajo, visual y auditable."),
        ("IA / NLP", "inteligencia artificial / procesamiento de lenguaje natural para entender los mensajes."),
        ("Playbook / Runbook", "guía paso a paso para resolver un tipo de problema."),
        ("Severidad", "nivel de gravedad de una incidencia (Leve, Medio, Urgente, Crítico)."),
        ("ASFI", "Autoridad de Supervisión del Sistema Financiero de Bolivia."),
    ]:
        bullet(doc, term + ":", d)

    out = os.path.join(BASE, "SolUno_Informe.docx")
    doc.save(out)
    print("DOCX generado:", out)
    return out

if __name__ == "__main__":
    try:
        generar_imagenes()
    except Exception as e:
        print("Aviso: no se generaron imagenes con Pillow:", e)
    build()

==========================================================================================
ARCHIVO: generar_docx.py
==========================================================================================
# -*- coding: utf-8 -*-
"""Genera el documento Word profesional del procedimiento completo de SolUno."""

from docx import Document
from docx.shared import Pt, RGBColor, Inches
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.enum.table import WD_TABLE_ALIGNMENT
from docx.oxml.ns import qn
from docx.oxml import OxmlElement

# ---------------------------------------------------------------------------
# Paleta de marca SolUno
# ---------------------------------------------------------------------------
INDIGO = RGBColor(0x2A, 0x2A, 0x6E)      # confianza
NARANJA = RGBColor(0xE8, 0x7A, 0x16)     # sol BancoSol
GRIS = RGBColor(0x55, 0x55, 0x55)
BLANCO = RGBColor(0xFF, 0xFF, 0xFF)
INDIGO_HEX = "2A2A6E"
NARANJA_HEX = "E87A16"
GRIS_CLARO_HEX = "EDEDF4"

doc = Document()

# ---------------------------------------------------------------------------
# Estilos base
# ---------------------------------------------------------------------------
normal = doc.styles["Normal"]
normal.font.name = "Calibri"
normal.font.size = Pt(11)
normal.paragraph_format.space_after = Pt(6)
normal.paragraph_format.line_spacing = 1.15

for hname, color, size in [("Heading 1", INDIGO, 16), ("Heading 2", NARANJA, 13), ("Heading 3", INDIGO, 12)]:
    st = doc.styles[hname]
    st.font.name = "Calibri"
    st.font.color.rgb = color
    st.font.size = Pt(size)
    st.font.bold = True


def shade_cell(cell, hex_color):
    tcPr = cell._tc.get_or_add_tcPr()
    shd = OxmlElement("w:shd")
    shd.set(qn("w:val"), "clear")
    shd.set(qn("w:fill"), hex_color)
    tcPr.append(shd)


def set_cell_text(cell, text, bold=False, color=None, size=10, align=None):
    cell.text = ""
    p = cell.paragraphs[0]
    if align:
        p.alignment = align
    run = p.add_run(text)
    run.bold = bold
    run.font.size = Pt(size)
    run.font.name = "Calibri"
    if color:
        run.font.color.rgb = color


def add_table(headers, rows, col_widths=None, header_fill=INDIGO_HEX):
    table = doc.add_table(rows=1, cols=len(headers))
    table.style = "Table Grid"
    table.alignment = WD_TABLE_ALIGNMENT.CENTER
    hdr = table.rows[0].cells
    for i, h in enumerate(headers):
        set_cell_text(hdr[i], h, bold=True, color=BLANCO, size=10)
        shade_cell(hdr[i], header_fill)
    for r_idx, row in enumerate(rows):
        cells = table.add_row().cells
        for i, val in enumerate(row):
            set_cell_text(cells[i], val, size=10)
            if r_idx % 2 == 1:
                shade_cell(cells[i], GRIS_CLARO_HEX)
    if col_widths:
        for i, w in enumerate(col_widths):
            for row in table.rows:
                row.cells[i].width = Inches(w)
    doc.add_paragraph()
    return table


def bullets(items, style="List Bullet"):
    for it in items:
        p = doc.add_paragraph(style=style)
        if isinstance(it, tuple):
            run = p.add_run(it[0] + ": ")
            run.bold = True
            p.add_run(it[1])
        else:
            p.add_run(it)


def numbered(items):
    for it in items:
        p = doc.add_paragraph(style="List Number")
        if isinstance(it, tuple):
            run = p.add_run(it[0] + ": ")
            run.bold = True
            p.add_run(it[1])
        else:
            p.add_run(it)


def h1(text):
    doc.add_heading(text, level=1)


def h2(text):
    doc.add_heading(text, level=2)


def para(text, bold=False, italic=False, align=None, color=None, size=None):
    p = doc.add_paragraph()
    if align:
        p.alignment = align
    run = p.add_run(text)
    run.bold = bold
    run.italic = italic
    if color:
        run.font.color.rgb = color
    if size:
        run.font.size = Pt(size)
    return p


def quote(text):
    p = doc.add_paragraph()
    p.paragraph_format.left_indent = Inches(0.4)
    p.paragraph_format.right_indent = Inches(0.4)
    run = p.add_run(text)
    run.italic = True
    run.font.color.rgb = GRIS
    # barra lateral
    pPr = p._p.get_or_add_pPr()
    pbdr = OxmlElement("w:pBdr")
    left = OxmlElement("w:left")
    left.set(qn("w:val"), "single")
    left.set(qn("w:sz"), "18")
    left.set(qn("w:space"), "12")
    left.set(qn("w:color"), NARANJA_HEX)
    pbdr.append(left)
    pPr.append(pbdr)
    return p


# ===========================================================================
# 1. PORTADA
# ===========================================================================
for _ in range(4):
    doc.add_paragraph()

p = para("SolUno", align=WD_ALIGN_PARAGRAPH.CENTER, color=INDIGO)
p.runs[0].font.size = Pt(54)
p.runs[0].bold = True

p = para("Procedimiento Completo de la Plataforma", align=WD_ALIGN_PARAGRAPH.CENTER, color=NARANJA)
p.runs[0].font.size = Pt(22)
p.runs[0].bold = True

p = para("Un punto. Una respuesta. Todos los canales.", align=WD_ALIGN_PARAGRAPH.CENTER, color=GRIS)
p.runs[0].font.size = Pt(14)
p.runs[0].italic = True

doc.add_paragraph()
p = para("Sistema operativo de incidencias y comunicacion para BancoSol",
         align=WD_ALIGN_PARAGRAPH.CENTER, color=INDIGO)
p.runs[0].font.size = Pt(13)

for _ in range(6):
    doc.add_paragraph()

p = para("InnovaHack 2026  ·  BancoSol", align=WD_ALIGN_PARAGRAPH.CENTER, color=INDIGO)
p.runs[0].font.size = Pt(13)
p.runs[0].bold = True
p = para("Equipo SolUno", align=WD_ALIGN_PARAGRAPH.CENTER, color=GRIS)
p.runs[0].font.size = Pt(12)
p = para("30 de mayo de 2026", align=WD_ALIGN_PARAGRAPH.CENTER, color=GRIS)
p.runs[0].font.size = Pt(12)

doc.add_page_break()

# ===========================================================================
# TABLA DE CONTENIDO (manual, simple)
# ===========================================================================
h1("Contenido")
toc = [
    "1.  Resumen ejecutivo",
    "2.  El problema de BancoSol",
    "3.  Problem Statement validado por el banco",
    "4.  Arquitectura general (on-premise vs. cloud)",
    "5.  El procedimiento paso a paso",
    "6.  Tabla resumen del ruteo a equipos",
    "7.  Cumplimiento y privacidad",
    "8.  Stack tecnico",
    "9.  KPIs e impacto esperado",
    "10. Conclusion",
]
for t in toc:
    p = doc.add_paragraph(t)
    p.paragraph_format.space_after = Pt(2)
doc.add_page_break()

# ===========================================================================
# 1. RESUMEN EJECUTIVO
# ===========================================================================
h1("1. Resumen ejecutivo")
para(
    "SolUno es un punto unico que recibe cada queja e incidente desde todos los canales de BancoSol, "
    "reconoce de que se trata, a cuantos clientes afecta y con que datos reproducirlo, entrega una "
    "respuesta unica y correcta, rutea los incidentes al equipo tecnico de forma estructurada y formal "
    "(reemplazando los grupos de WhatsApp informales) y devuelve la solucion a un manual vivo que mantiene "
    "a call center y clientes hablando el mismo idioma, todo sin que un dato confidencial salga del banco."
)
quote("En una frase: Un punto. Una respuesta. Todos los canales.")
para(
    "Este documento describe el procedimiento completo de la plataforma paso a paso, con foco en el flujo "
    "web/tecnico: como ingresan los reportes, como se normalizan y tokenizan, como se clasifican, la "
    "bifurcacion clave entre queja e incidente, el ruteo a los equipos del banco, la agrupacion de "
    "reportes, el cierre del ciclo hacia el Playbook Vivo y la respuesta al cliente."
)

# ===========================================================================
# 2. EL PROBLEMA
# ===========================================================================
h1("2. El problema de BancoSol")
para(
    "BancoSol es la institucion pionera y lider mundial en microfinanzas de Bolivia: mas de 2,5 millones de "
    "clientes, +1.801 puntos de atencion, #1 del ranking CAMEL durante 10 anos consecutivos y una cartera de "
    "USD 2.712 millones, bajo supervision estricta de ASFI. En mayo de 2024 lanzo altoke (billetera QR para "
    "microempresarios), que paso de 0 a 1,2 millones de usuarios y disparo el trafico de API en +2.000% en "
    "menos de 6 meses, multiplicando incidencias y reclamos en todos los canales."
)
h2("2.1 El dolor real")
bullets([
    ("Canales fragmentados", "los reclamos llegan por 9 vias distintas (WhatsApp, FonoSol, appSol, Solnet, altoke, sucursales, chatbot, redes sociales, Punto de Reclamo web), cada una con su propio playbook y ritmo de actualizacion."),
    ("Respuestas inconsistentes", "un mismo problema recibe respuestas distintas segun el canal."),
    ("Call Center cuello de botella", "tercerizado, sin conocimiento tecnico profundo; deriva el resto a desarrollo en lotes diarios, por lo que los devs hacen soporte en vez de desarrollar."),
    ("Comunicacion interna informal", "los incidentes se reportan en grupos de Teams/WhatsApp informales; el equipo tecnico no sabe a cuantos usuarios afecta ni recibe datos para replicar el error, y al resolver la solucion no vuelve ordenada al playbook."),
    ("El cliente no tiene a donde ir", "no existe un punto unico para reportar, consultar estado o auto-resolver."),
])
h2("2.2 La distincion que dio el banco: Queja vs. Incidente")
add_table(
    ["", "QUEJA / CONSULTA", "INCIDENTE"],
    [
        ["Que es", "Problema particular, bajo impacto, error controlado y conocido", "Afectacion grave: falla una funcion critica"],
        ["Ejemplos", "Extranjero no transacciona tras update (0,5%); cambio de dispositivo a reactivar token; 'pague y sigue la deuda'", "Falla login, transferencia, QR, ACH"],
        ["Como se resuelve", "Operativo y autonomo: playbook actualizado, misma respuesta en todo canal", "Se compila y prioriza informacion y se rutea al equipo tecnico"],
    ],
    col_widths=[1.3, 2.6, 2.6],
    header_fill=NARANJA_HEX,
)

# ===========================================================================
# 3. PROBLEM STATEMENT
# ===========================================================================
h1("3. Problem Statement validado por el banco")
para("Cita textual del Problem Statement final, validado por la persona del banco en la sesion de validacion:", italic=True)
quote(
    "El equipo de atencion de BancoSol -call center y equipo de desarrollo- necesita recibir en un unico "
    "punto y de forma estructurada cada queja e incidente que hoy llega disperso por todos los canales, "
    "sabiendo de que se trata, a cuantos clientes afecta y con los datos necesarios para reproducirlo, para "
    "entregar una respuesta unica, correcta y oportuna y, una vez resuelto, devolver esa solucion a un manual "
    "vivo que mantenga a todos -call center y cliente- hablando el mismo idioma."
)
quote(
    "Hoy no lo logran porque no existe ni siquiera un canal interno formal: los incidentes se reportan en "
    "grupos de chat informales, el equipo tecnico no sabe cuantos usuarios estan afectados ni recibe datos "
    "para replicar el error, y las soluciones no vuelven de forma ordenada a los manuales, que quedan "
    "desactualizados y distintos en cada canal."
)
quote(
    "Una solucion comercial existente no lo resuelve porque no puede operar sin que la informacion del "
    "cliente salga del banco, no entiende la logica especifica de cada canal y producto, y no mantiene un "
    "unico manual vivo y consistente que cumpla las exigencias de privacidad y de reporte individual del "
    "regulador boliviano."
)
para("Estado: Validado por la persona del banco ('Esta bien, esta bien').", bold=True, color=INDIGO)

# ===========================================================================
# 4. ARQUITECTURA
# ===========================================================================
h1("4. Arquitectura general")
para(
    "SolUno se apoya en una separacion estricta entre lo que corre dentro del banco (on-premise) y lo que "
    "corre en la nube. El principio rector es que la informacion identificable del cliente nunca sale del "
    "banco: el modelo de IA en la nube solo recibe texto ya tokenizado."
)
h2("4.1 Que corre donde")
add_table(
    ["Capa", "Componentes", "Datos que maneja"],
    [
        ["On-premise (dentro del banco)", "n8n (orquestador), vault de identidad, modulo de tokenizacion / re-hidratacion, base de playbooks versionada, base de incidentes, registro auditable", "PII real: nombre, CI, cuenta, telefono, saldos"],
        ["Cloud (Zero Data Retention)", "El LLM (Claude) que clasifica, redacta respuestas y genera informes", "Solo texto tokenizado: ve 'usuario 1', nunca 'Fernando Alen'"],
    ],
    col_widths=[1.8, 3.0, 2.0],
)
h2("4.2 Los tres pilares tecnicos")
bullets([
    ("n8n (orquestador)", "recibe los canales, normaliza, tokeniza, llama al cerebro de IA, bifurca el flujo y dispara las respuestas. Es el director de orquesta on-premise."),
    ("Claude (cerebro)", "clasifica queja vs. incidente, determina producto/categoria/severidad, redacta la respuesta unica y genera el informe ASFI, operando siempre sobre datos tokenizados."),
    ("Tokenizacion", "capa de privacidad que reemplaza la PII por tokens antes de cualquier IA y la re-hidrata localmente dentro del banco."),
])

# ===========================================================================
# 5. PROCEDIMIENTO PASO A PASO
# ===========================================================================
h1("5. El procedimiento paso a paso")
para(
    "Esta es la seccion central del documento. Describe, en orden, el recorrido completo de un reporte desde "
    "que entra por cualquier canal hasta que se cierra el ciclo y el cliente recibe su respuesta.",
    italic=True,
)

# 5.a Ingesta
h2("5.a Ingesta: recepcion desde WhatsApp y los canales Meta")
para(
    "El MVP recibe reportes por dos mundos: altoke (canal interno/transaccional, via su webhook/API interna) "
    "y Meta (canales externos y publicos). Dentro de Meta conviven tres canales:"
)
bullets([
    ("WhatsApp", "ingresa por el nodo nativo de WhatsApp en n8n (WhatsApp Cloud API)."),
    ("Facebook Messenger e Instagram", "ingresan por un webhook de Meta. Un mismo webhook de Meta Business recibe tanto Messenger como Instagram (Graph API), de modo que un solo punto de entrada cubre ambos canales publicos."),
])
para(
    "En la practica conviven dos puntos de ingesta: el nodo nativo de WhatsApp y el webhook de Meta que "
    "atiende Messenger e Instagram. El webhook generico de SolUno (POST a /webhook/soluno-reporte) recibe el "
    "evento con la estructura {canal, producto, mensaje, cliente:{nombre, ci, cuenta, telefono}}.",
)

# 5.b Normalizacion
h2("5.b Normalizacion: un formato comun")
para(
    "Cada canal trae su propia estructura (WhatsApp, Messenger e Instagram tienen payloads distintos; altoke "
    "agrega contexto transaccional). El primer trabajo de n8n es convertir cada evento a un formato comun "
    "unico, con los mismos campos (canal de origen, producto, texto del problema y bloque de cliente). A "
    "partir de aqui el resto del flujo es identico, sin importar por donde haya entrado el reporte."
)

# 5.c Tokenizacion
h2("5.c Tokenizacion (privacidad)")
para(
    "Antes de que cualquier IA toque el contenido, el modulo de tokenizacion on-premise reemplaza la PII por "
    "tokens. La tokenizacion es inteligente segun el tipo de dato: parte se conserva visible solo para el "
    "revisor interno y nada identificable viaja al LLM."
)
add_table(
    ["Dato", "Accion", "Por que"],
    [
        ["Nombre completo", "Tokenizar al LLM, visible al revisor", "El revisor lo necesita; el LLM no"],
        ["CI / Carnet", "Tokenizar al LLM, visible al revisor", "Suficiente para identificar internamente"],
        ["Nº de cuenta", "Tokenizar y ocultar", "Irrelevante para resolver"],
        ["Saldo / montos", "Tokenizar y ocultar", "Sensible, no necesario"],
        ["Telefono / correo / direccion", "Tokenizar y ocultar", "Irrelevante para la revision"],
        ["Texto del problema", "Pasa tokenizado al LLM", "Lo necesario para clasificar"],
    ],
    col_widths=[1.9, 2.4, 2.5],
)
para(
    "Se conserva nombre + CI visibles unicamente para el revisor interno; el resto (cuenta, saldos, telefono) "
    "se oculta. El LLM externo opera sobre 'usuario 1' y la re-hidratacion se hace localmente dentro del "
    "banco. Como el modelo nunca recibe datos identificables, no hay cesion a terceros."
)

# 5.d Clasificacion
h2("5.d Clasificacion")
para(
    "Con el texto ya tokenizado, SolUno clasifica el reporte en varias dimensiones a la vez:"
)
bullets([
    ("Tipo", "es una QUEJA o un INCIDENTE."),
    ("Producto", "appSol, altoke, Solnet, etc. (cada uno tiene su logica)."),
    ("Categoria", "acceso/login, pagos/QR, transferencias, rendimiento, etc."),
    ("Severidad", "para priorizar el caso si es incidente."),
])
para(
    "En el MVP la clasificacion arranca por reglas (palabras clave) y en el roadmap se reemplaza por Claude "
    "para mayor precision. El resultado de esta clasificacion es lo que decide la bifurcacion del siguiente paso."
)

# 5.e BIFURCACION
h2("5.e Bifurcacion clave: queja vs. incidente")
para(
    "Este es el corazon del procedimiento. Segun la clasificacion, el flujo se separa en dos caminos "
    "completamente distintos:",
    bold=True,
)
h3 = doc.add_heading("Camino QUEJA (resolucion operativa y autonoma)", level=3)
para(
    "Si es una queja (bajo impacto, error conocido), SolUno la resuelve de forma operativa y autonoma con el "
    "Playbook Vivo. Sirve la respuesta estandarizada y vigente, de modo que el cliente recibe la misma "
    "respuesta correcta en todos los canales y puede resolverlo el mismo (autoservicio). No se pasa a ningun "
    "equipo: el caso se cierra en este punto."
)
doc.add_heading("Camino INCIDENTE (ruteo al equipo correcto del banco)", level=3)
para(
    "Si es un incidente (falla grave de una funcion critica: login, transferencia, QR, ACH), SolUno arma un "
    "Paquete de Incidente y lo rutea al equipo correcto del banco. El Paquete de Incidente contiene:"
)
bullets([
    "Que falla (descripcion del problema).",
    "A cuantos clientes afecta (conteo de afectados).",
    "Datos para reproducirlo (tokenizados, con contexto transaccional cuando viene de altoke).",
    "Severidad / prioridad.",
    "Solo lo necesario: no toda la informacion se pasa al equipo tecnico.",
])
para(
    "Este Paquete de Incidente reemplaza los grupos de WhatsApp informales por un canal interno formal hacia "
    "el equipo responsable. El ruteo depende del tipo de incidente (ver tabla completa en la seccion 6):"
)
bullets([
    ("Pagos/QR (altoke, transacciones)", "Equipo Pagos/QR."),
    ("Acceso/login", "Equipo Seguridad/Accesos."),
    ("Apps caidas (appSol/Solnet)", "DevOps Aplicaciones."),
    ("Rendimiento/infraestructura", "Infraestructura/TI."),
    ("Otros", "Mesa de Incidentes TI."),
])

# 5.f Agrupacion
h2("5.f Agrupacion de reportes")
para(
    "Muchos reportes del mismo incidente se agrupan en UN solo incidente con conteo de afectados. Si 200 "
    "clientes reportan 'el QR no funciona' por distintos canales, SolUno no genera 200 incidentes sino 1 "
    "incidente con 200 afectados ('200 reclamos a 1 incidente'). Asi el equipo tecnico ve el problema real y "
    "su magnitud, en lugar de cientos de tickets sueltos."
)

# 5.g Cierre del ciclo
h2("5.g Cierre del ciclo: retorno al Playbook Vivo")
para(
    "Cuando el equipo resuelve el incidente, la solucion no se queda en un chat. SolUno la convierte en pasos "
    "1-2-3-4 que cualquiera pueda seguir y la publica al Playbook Vivo, que es versionado y trazable. De esta "
    "forma, el proximo caso identico ya tiene respuesta lista y se propaga a todos los canales al instante. "
    "Esto cierra el ciclo: una queja futura del mismo tipo se resuelve sola por el camino operativo."
)

# 5.h Respuesta al cliente
h2("5.h Respuesta al cliente")
para(
    "La respuesta se envia al cliente por el mismo canal por el que reporto (WhatsApp, Messenger, Instagram o "
    "altoke). El cliente sigue el estado de su caso en tres estados claros:"
)
add_table(
    ["Estado", "Significado"],
    [
        ["Recibido", "SolUno recibio el reporte y lo registro."],
        ["En analisis", "El caso esta clasificado y, si es incidente, en manos del equipo correcto."],
        ["Resuelto", "Se aplico la solucion del playbook o el equipo cerro el incidente."],
    ],
    col_widths=[2.0, 4.5],
    header_fill=NARANJA_HEX,
)

# 5.i Registro y metricas
h2("5.i Registro de estado y metricas")
para(
    "Cada paso queda registrado en la base de incidentes y en el registro auditable on-premise. A partir de "
    "ese registro, SolUno produce metricas operativas:"
)
bullets([
    "Tiempos (de recepcion a resolucion).",
    "Casos por equipo (carga de cada area).",
    "Recurrencia (problemas que se repiten y deberian ir al playbook).",
    "Picos (momentos criticos de volumen, util para detectar incidentes masivos temprano).",
])

# 5.j Plus
h2("5.j Plus: informe ASFI y traduccion de idiomas")
para(
    "SolUno suma dos capacidades extra que multiplican su valor:",
)
bullets([
    ("Informe automatico para ASFI (Plus #1)", "al cerrar un incidente, el LLM redacta el informe individualizado/post-mortem (que paso, en que servidor, como se resolvio, que acciones evitan que se repita). El humano revisa y aprueba. Cumple el reporte regulatorio y alimenta el playbook."),
    ("Traduccion de idiomas (Plus #2)", "traduce el reporte del cliente (quechua, aymara, guarani o un idioma extranjero) al espanol para el operador, y la respuesta del playbook al idioma del cliente. Encaja con la mision social e inclusiva de BancoSol."),
])

# ===========================================================================
# 6. TABLA DE RUTEO
# ===========================================================================
h1("6. Tabla resumen del ruteo a equipos")
para(
    "Cuando un reporte se clasifica como incidente, el Paquete de Incidente se rutea al equipo responsable "
    "segun el tipo. Esta tabla resume la logica de ruteo:"
)
add_table(
    ["Tipo de incidente", "Ejemplos", "Equipo responsable"],
    [
        ["Pagos / QR", "altoke, transacciones por QR, cobros/pagos", "Equipo Pagos/QR"],
        ["Acceso / login", "no puede iniciar sesion, reactivacion de token", "Equipo Seguridad/Accesos"],
        ["Apps caidas", "appSol o Solnet no abren / caidas", "DevOps Aplicaciones"],
        ["Rendimiento / infraestructura", "lentitud, timeouts, saturacion de servidores", "Infraestructura/TI"],
        ["Otros", "casos no categorizados", "Mesa de Incidentes TI"],
    ],
    col_widths=[2.0, 2.7, 2.0],
)
para(
    "Una queja, en cambio, no se rutea a ningun equipo: se resuelve de forma operativa con el Playbook Vivo y "
    "se responde directamente al cliente.",
    italic=True,
)

# ===========================================================================
# 7. CUMPLIMIENTO Y PRIVACIDAD
# ===========================================================================
h1("7. Cumplimiento y privacidad")
para(
    "La premisa del banco es clara: 'Toda informacion del cliente tiene que quedarse en el banco.' SolUno la "
    "respeta por diseno:"
)
bullets([
    "La PII se tokeniza on-premise antes de cualquier llamada a la IA.",
    "El LLM en la nube opera sobre 'usuario 1' y nunca recibe datos identificables (Zero Data Retention).",
    "La re-hidratacion (volver del token al dato real) ocurre localmente dentro del banco.",
    "Como el modelo externo no recibe datos identificables, no hay cesion a terceros, lo que simplifica el cumplimiento de privacidad y de tercerizacion ante ASFI.",
    "Cada reporte genera un registro auditable, alineado con el requisito de expediente individual del regulador.",
])
para(
    "El marco regulatorio (ASFI: respuesta en 5 dias habiles, Punto de Reclamo, expediente individual, "
    "soberania del dato) es una restriccion a respetar, no el problema que SolUno resuelve. El core es la "
    "comunicacion; el informe ASFI automatico es un plus que ayuda a cumplir el reporte regulatorio."
)

# ===========================================================================
# 8. STACK TECNICO
# ===========================================================================
h1("8. Stack tecnico")
add_table(
    ["Componente", "Rol", "Detalle"],
    [
        ["n8n", "Orquestador (on-premise)", "Webhook de ingesta, normalizacion, tokenizacion, clasificacion por reglas, bifurcacion queja/incidente, armado del Paquete de Incidente, respuesta. Nodos nativos de WhatsApp y webhook de Meta para Messenger/Instagram."],
        ["Claude (Anthropic)", "Cerebro de IA (cloud, ZDR)", "Clasifica, redacta la respuesta unica y genera el informe ASFI sobre datos tokenizados. La API key se guarda dentro de n8n (Credentials), nunca en el repo."],
        ["Frontend", "Vistas de usuario", "Dos vistas (cliente con 3 estados; interna: call center, desarrollo y cierre del ciclo) + status + metricas, consumiendo los webhooks (React / v0 / Lovable)."],
        ["Base de datos", "Persistencia on-premise", "Base de playbooks versionada, base de incidentes (agrupacion y conteo de afectados, Postgres/pgvector) y registro auditable."],
    ],
    col_widths=[1.5, 1.8, 3.4],
)
h2("8.1 Flujo de nodos en n8n (workflow v1)")
numbered([
    ("Webhook (Ingesta)", "recibe POST a /webhook/soluno-reporte."),
    ("Tokenizar (PII)", "reemplaza nombre/CI/cuenta/telefono por tokens; conserva nombre+CI para el revisor."),
    ("Clasificar (reglas)", "queja vs. incidente, producto, categoria, severidad."),
    ("¿Es Incidente?", "bifurca: incidente al paso 5a; queja al paso 5b."),
    ("Paquete de Incidente (5a)", "qué falla, # afectados, datos para reproducir, equipo, estado."),
    ("Respuesta Queja / Playbook (5b)", "trae la misma respuesta y pasos del playbook segun categoria."),
    ("Responder", "devuelve el JSON final y dispara la respuesta al canal de origen."),
])

# ===========================================================================
# 9. KPIs E IMPACTO
# ===========================================================================
h1("9. KPIs e impacto esperado")
para(
    "Los siguientes datos son publicos y verificables; el banco no entrega cifras reales, por lo que se usan "
    "para construir supuestos creibles. Cada fila indica su fuente.",
    italic=True,
)
add_table(
    ["Indicador / dato", "Cifra", "Fuente"],
    [
        ["Escala de BancoSol", "+2,5M clientes, #1 CAMEL 10 anos", "bancosol.com.bo"],
        ["Crecimiento de altoke", "0 a 1,2M usuarios; +2.000% de API en 6 meses", "Sensedia"],
        ["Llamadas repetidas al call center", "hasta 32%", "Balto / Enghouse"],
        ["Tiempo de dev en soporte/mantenimiento", "17,3 h de 41,1 por semana", "Stripe Developer Coefficient"],
        ["Costo de 1 hora de caida", "+USD 300.000 (90% de empresas)", "ITIC 2024"],
        ["Plazo legal ASFI de respuesta", "5 dias habiles", "Reglamento ASFI"],
        ["Reclamos 1a instancia 2024", "75.805 (a verificar)", "ASFI CIRS"],
        ["Esperan misma respuesta en todo canal", "75%", "Zendesk CX Trends 2024"],
        ["Deben repetir su info a distintos agentes", "66%", "Salesforce State of Service"],
        ["Ahorro de GenAI en atencion", "30-45% de los costos de la funcion", "McKinsey"],
        ["Hablantes de lenguas originarias", "2,2M (quechua 1,39M, aymara 774.874, guarani 43.870)", "Censo 2024 (INE)"],
    ],
    col_widths=[2.7, 2.2, 1.8],
)
h2("9.1 Impacto esperado de SolUno")
bullets([
    ("Menos respuestas inconsistentes", "una sola respuesta correcta por canal gracias al Playbook Vivo."),
    ("Devs liberados", "los incidentes llegan estructurados y agrupados, no como tickets sueltos; el equipo deja de hacer soporte manual."),
    ("Resolucion mas rapida de incidentes masivos", "la agrupacion y el conteo de afectados muestran la magnitud real temprano."),
    ("Cumplimiento mas simple", "PII que no sale del banco e informe ASFI automatico."),
    ("Inclusion", "atencion en lenguas originarias y a clientes extranjeros."),
])

# ===========================================================================
# 10. CONCLUSION
# ===========================================================================
h1("10. Conclusion")
para(
    "SolUno convierte un proceso hoy disperso e informal en un procedimiento unico, estructurado y trazable. "
    "Recibe cada reporte por cualquier canal, protege la informacion del cliente con tokenizacion on-premise, "
    "clasifica y bifurca entre quejas (autoservicio operativo) e incidentes (ruteo formal al equipo correcto), "
    "agrupa los reportes en incidentes reales con su conteo de afectados, cierra el ciclo devolviendo cada "
    "solucion al Playbook Vivo y responde al cliente por su mismo canal con estados claros."
)
para(
    "El resultado es un banco que deja de improvisar la comunicacion: call center y clientes hablan el mismo "
    "idioma, los desarrolladores vuelven a desarrollar y el regulador recibe lo que exige, todo sin que un "
    "dato confidencial salga del banco.",
)
quote("SolUno: un punto, una respuesta, todos los canales.")

# ---------------------------------------------------------------------------
out = r"c:\Users\ASUS\3D Objects\HACKATON\SolUno_Procedimiento_Completo.docx"
doc.save(out)
print("OK ->", out)

==========================================================================================
ARCHIVO: transcribir.py
==========================================================================================
import sys
from datetime import timedelta
from faster_whisper import WhisperModel

AUDIO = r"c:\Users\ASUS\3D Objects\HACKATON\AUDIO-2026-05-30-09-46-35.m4a"
OUT = r"c:\Users\ASUS\3D Objects\HACKATON\AUDIO-2026-05-30-09-46-35_transcripcion.txt"

def fmt(s):
    return str(timedelta(seconds=int(s)))

print("Cargando modelo 'base'...", flush=True)
model = WhisperModel("base", device="cpu", compute_type="int8")

print("Transcribiendo...", flush=True)
segments, info = model.transcribe(AUDIO, language="es", vad_filter=True, beam_size=5)

print(f"Idioma detectado: {info.language} (prob {info.language_probability:.2f})", flush=True)

lines = []
full = []
for seg in segments:
    stamp = f"[{fmt(seg.start)} -> {fmt(seg.end)}]"
    text = seg.text.strip()
    print(stamp, text, flush=True)
    lines.append(f"{stamp} {text}")
    full.append(text)

with open(OUT, "w", encoding="utf-8") as f:
    f.write("TRANSCRIPCION - AUDIO-2026-05-30-09-46-35.m4a\n")
    f.write("=" * 60 + "\n\n")
    f.write("TEXTO COMPLETO:\n\n")
    f.write(" ".join(full).strip() + "\n\n")
    f.write("=" * 60 + "\n\n")
    f.write("CON MARCAS DE TIEMPO:\n\n")
    f.write("\n".join(lines) + "\n")

print("\nLISTO ->", OUT, flush=True)

==========================================================================================
ARCHIVO: transcribir2.py
==========================================================================================
import sys
from datetime import timedelta
from faster_whisper import WhisperModel

AUDIO = r"c:\Users\ASUS\3D Objects\HACKATON\audio nuevo.m4a"
OUT = r"c:\Users\ASUS\3D Objects\HACKATON\audio_nuevo_transcripcion.txt"

def fmt(s):
    return str(timedelta(seconds=int(s)))

print("Cargando modelo 'small'...", flush=True)
model = WhisperModel("small", device="cpu", compute_type="int8")

print("Transcribiendo...", flush=True)
segments, info = model.transcribe(AUDIO, language="es", vad_filter=True, beam_size=5)

print(f"Idioma detectado: {info.language} (prob {info.language_probability:.2f})", flush=True)

lines = []
full = []
for seg in segments:
    stamp = f"[{fmt(seg.start)} -> {fmt(seg.end)}]"
    text = seg.text.strip()
    print(stamp, text, flush=True)
    lines.append(f"{stamp} {text}")
    full.append(text)

with open(OUT, "w", encoding="utf-8") as f:
    f.write("TRANSCRIPCION - audio nuevo.m4a\n")
    f.write("=" * 60 + "\n\n")
    f.write("TEXTO COMPLETO:\n\n")
    f.write(" ".join(full).strip() + "\n\n")
    f.write("=" * 60 + "\n\n")
    f.write("CON MARCAS DE TIEMPO:\n\n")
    f.write("\n".join(lines) + "\n")

print("\nLISTO ->", OUT, flush=True)

==========================================================================================
ARCHIVO: transcribir3.py
==========================================================================================
import sys
from datetime import timedelta
from faster_whisper import WhisperModel

AUDIO = r"c:\Users\ASUS\3D Objects\HACKATON\audio 3.m4a"
OUT = r"c:\Users\ASUS\3D Objects\HACKATON\audio_3_transcripcion.txt"

def fmt(s):
    return str(timedelta(seconds=int(s)))

print("Cargando modelo 'small'...", flush=True)
model = WhisperModel("small", device="cpu", compute_type="int8")

print("Transcribiendo...", flush=True)
segments, info = model.transcribe(AUDIO, language="es", vad_filter=True, beam_size=5)

print(f"Idioma detectado: {info.language} (prob {info.language_probability:.2f})", flush=True)

lines = []
full = []
for seg in segments:
    stamp = f"[{fmt(seg.start)} -> {fmt(seg.end)}]"
    text = seg.text.strip()
    print(stamp, text, flush=True)
    lines.append(f"{stamp} {text}")
    full.append(text)

with open(OUT, "w", encoding="utf-8") as f:
    f.write("TRANSCRIPCION - audio 3.m4a\n")
    f.write("=" * 60 + "\n\n")
    f.write("TEXTO COMPLETO:\n\n")
    f.write(" ".join(full).strip() + "\n\n")
    f.write("=" * 60 + "\n\n")
    f.write("CON MARCAS DE TIEMPO:\n\n")
    f.write("\n".join(lines) + "\n")

print("\nLISTO ->", OUT, flush=True)

==========================================================================================
ARCHIVO: .env.example
==========================================================================================
# ============================================================
# SolUno — variables de entorno (plantilla)
# Copiá este archivo como ".env" y completá con tus claves.
# NUNCA subas el .env real al repositorio.
# ============================================================

# --- IA: Groq (gratis, compatible con OpenAI) ---
GROQ_API_KEY=tu_clave_groq_aqui

# --- WhatsApp Cloud API (Meta) ---
WHATSAPP_TOKEN=tu_token_permanente_de_meta
WA_PHONE_ID=tu_phone_number_id
WA_VERIFY_TOKEN=soluno_verify_123

# --- Backend ---
PORT=3000
N8N_URL=http://localhost:5678

==========================================================================================
ARCHIVO: .gitignore
==========================================================================================
# ===== Secretos / entorno (NUNCA subir claves reales) =====
.env
.env.*
!.env.example
*.local
ngrok.yml

# ===== Dependencias =====
node_modules/

# ===== Logs / temporales =====
_*.txt
*.log
*.tmp

# ===== Datos generados en tiempo de ejecucion =====
app/informes/*.txt

# ===== Medios pesados (no necesarios para correr) =====
*.m4a
*.mp3
*.wav

# ===== Materiales de origen de terceros (no redistribuir) =====
INNOVAHACK2026_GuiaRapida_Consuelo*.pdf
TDRequipos*.pdf
FEAFR.pdf
BancoSol problema.pdf
audio nuevo.pdf
*.xlsm
*.xlsx
*_transcripcion.txt

# ===== Sistema operativo =====
.DS_Store
Thumbs.db

